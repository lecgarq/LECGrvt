# Project TODO List

- [x] Investigate ideal structure for placing the custom `LecgUI` Component dictionary in the WPF solution (e.g., `src/Resources/Themes/`).
- [x] Determine how to pipe the required 9-color hex palette through static resources efficiently.
- [x] Implement `LecgButton` and `LecgTextBox` standard styles.
- [x] Implement `LecgTreeView` custom control with batch expansion/collapse and standardized styling.
- [x] Audit existing `Views/` for local style overrides or hardcoded colors that conflict with `LecgColors.xaml` (Primary views complete).
- [x] Optimize relative grid bindings and auto-resizing for responsive layout (Phase 4).
