# Architecture

> Auto-generated by /map on 2026-04-29

## Overview

A Revit 2026 Add-in system designed with a service-oriented MVVM architecture. The system leverages strong geometry libraries for complex spatial operations and provides a robust UI via WPF.

```
┌─────────────────────────────────────────┐
│        [Revit External Commands]        │
├─────────────────────────────────────────┤
│            [ViewModels / UI]            │
├─────────────────────────────────────────┤
│          [Service Layer (DI)]           │
├─────────────────────────────────────────┤
│      [Geometry & Data Layer (Core)]     │
└─────────────────────────────────────────┘
```

## Components

### src/Commands
- **Purpose:** Entry points for Revit. Implements `IExternalCommand`.
- **Location:** `src/Commands/`
- **Pattern:** Base classes for shared logic (e.g., `AlignCommandBase`).

### src/ViewModels & src/Views
- **Purpose:** User interface management using MVVM.
- **Library:** CommunityToolkit.Mvvm.

### src/Services
- **Purpose:** Core business and geometry logic.
- **Location:** `src/Services/`
- **Pattern:** Interface-based services with constructor injection, managed by Scrutor.

### src/Core
- **Purpose:** Shared abstractions, constants, and fundamental types.

## Data Flow

1. **Revit Ribbon Click:** Triggers an `ExternalCommand`.
2. **Command Execution:** Initializes the UI (if needed) or calls a `Service` directly.
3. **UI Interaction:** ViewModels communicate with Services to perform Revit database operations.
4. **Service Logic:** Performs complex geometry calculations using Revit API and external libraries (Clipper2, geometry3Sharp).
5. **Revit DB Update:** Services modify the `Document` within a `Transaction`.

## Technical Debt

- [ ] **High Service Count:** 310 files in `src/Services` suggest potential for consolidation or better sub-categorization.
- [ ] **Test Coverage:** `LECG.Tests` exists but coverage for all 310 services needs verification.
- [ ] **Geometry Complexity:** Heavy reliance on external libraries for geometry suggests potential maintenance burden.

## Conventions

- **Naming:** `IService` / `Service` pattern for DI.
- **Structure:** Clean separation of UI, Commands, and Services.
- **Logging:** Structured logging using Serilog.
