---
phase: 05-verification-and-polish
plan: 01
type: tdd
wave: 1
depends_on:
  - "05-00"
files_modified:
  - LECG.Tests/Services/RenameRulePipelineServiceTests.cs
  - LECG.Tests/Services/BaseElementCollectionServiceTests.cs
  - src/Services/Renaming/BaseElementCollectionService.cs
  - .planning/phases/05-verification-and-polish/05-VERIFICATION.md
autonomous: true
requirements:
  - REQ-05
must_haves:
  truths:
    - "RenameRulePipelineServiceTests has ≥ 5 behavioural GREEN tests covering rule composition order, index propagation, null-throw guards, inactive-rules pass-through, constructor null-guards"
    - "BaseElementCollectionServiceTests has ≥ 8 deepening GREEN tests via extracted pure-data helpers (TryGetGroupLabel, DispatchScopeFlags, MergeParamScanResults)"
    - "Each extracted helper is ≤ 30 LOC and is `internal static`"
    - "Revit-FilteredElementCollector-bound tests stay skip-gated with reason naming the manual verification fallback"
    - "Matrix rows 2 (RenameRulePipelineService) and 5 (BaseElementCollectionService) flip from ❌/⚠ to ✅ in 05-VERIFICATION.md"
    - "Full xUnit suite stays 100% GREEN"
  artifacts:
    - path: "LECG.Tests/Services/RenameRulePipelineServiceTests.cs"
      provides: "Pipeline composition coverage"
      contains: "ApplyRules_ComposesRulesInOrder"
    - path: "LECG.Tests/Services/BaseElementCollectionServiceTests.cs"
      provides: "Deepened coverage via extracted helpers"
      contains: "TryGetGroupLabel"
    - path: "src/Services/Renaming/BaseElementCollectionService.cs"
      provides: "≤ 30 LOC pure-data helper extractions (TryGetGroupLabel, DispatchScopeFlags, MergeParamScanResults)"
      contains: "internal static"
  key_links:
    - from: "src/Services/Renaming/BaseElementCollectionService.cs:234"
      to: "BaseElementCollectionService.TryGetGroupLabel"
      via: "internal static pure helper wrapping LabelUtils.GetLabelForGroup try/catch"
      pattern: "TryGetGroupLabel\\("
    - from: "src/Services/Renaming/BaseElementCollectionService.cs (scope dispatch)"
      to: "BaseElementCollectionService.DispatchScopeFlags"
      via: "internal static flag→mask mapping"
      pattern: "DispatchScopeFlags\\("
---

<objective>
Wave 1 (TDD): implement the RenameRulePipelineService coverage (new fixture body) and deepen the BaseElementCollectionService coverage via three minimal pure-data helper extractions, all ≤ 30 LOC each. RED tests (skip-gated from Wave 0) flip to GREEN as helpers ship.

Purpose: Close matrix rows 2 and 5. Establish the deepening pattern reused in Wave 2.

Output: 5+ GREEN tests in RenameRulePipelineServiceTests, 8+ GREEN deepening tests in BaseElementCollectionServiceTests, three new internal static helpers in BaseElementCollectionService.cs, matrix updated.
</objective>

<execution_context>
@C:/Users/LECG Arquitectura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LECG Arquitectura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-verification-and-polish/05-CONTEXT.md
@.planning/phases/05-verification-and-polish/05-RESEARCH.md
@.planning/phases/05-verification-and-polish/05-VERIFICATION.md
@src/Services/Renaming/RenameRulePipelineService.cs
@src/Services/Renaming/RenameRules.cs
@src/Services/Renaming/BaseElementCollectionService.cs
@LECG.Tests/Services/BaseElementCollectionServiceTests.cs
@LECG.Tests/Services/RenameRulePipelineServiceTests.cs

<interfaces>
<!-- From RESEARCH.md: RenameRulePipelineService 22 LOC, 5-rule composition -->
<!-- Rules apply in order: Remove → Replace → Case → Add → Numbering -->
<!-- Each IRenameRule.Apply(text, context, index) returns string -->

From src/Services/Renaming/BaseElementCollectionService.cs (existing surface, Phase 03):
- Public methods rely on Revit FilteredElementCollector → NOT testable without harness
- Phase 03-03 strategy: test via ElementLabelService.GetLabelsFromRaw SSoT
- ParamGroup try/catch at :234 and :304 (RESEARCH §Pitfall 4 — DO NOT dedup; dedup is v1.2)
- This plan adds three internal static pure helpers to enable direct testing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement RenameRulePipelineServiceTests GREEN body (5+ tests)</name>
  <files>LECG.Tests/Services/RenameRulePipelineServiceTests.cs</files>
  <behavior>
    - ApplyRules composes rules in order Remove→Replace→Case→Add→Numbering: each substituted IRenameRule.Apply returns a marker; concatenation order observable
    - ApplyRules passes correct `index` argument to every rule (Received calls capture the arg)
    - Null context throws ArgumentNullException (or whatever the production code does — confirm by reading)
    - Inactive rules (rule returns input unchanged) pass through; final text equals input
    - Constructor null rule-list throws ArgumentNullException
  </behavior>
  <action>
Un-Skip the 5 RED tests created in Wave 0 (`05-00-PLAN.md` Task 1). Write GREEN bodies using NSubstitute on `IRenameRule` (no Revit API dependency — pure pipeline).

`RenameRulePipelineService.ApplyRules(text, context, index)` composes 5 rules in fixed order: Remove → Replace → Case → Add → Numbering. Read `src/Services/Renaming/RenameRulePipelineService.cs` and `src/Services/Renaming/RenameRules.cs` first to confirm constructor signature + rule ordering before writing tests.

Test pattern (FluentAssertions + NSubstitute):
```csharp
var remove = Substitute.For<IRenameRule>();
remove.Apply("input", Arg.Any<RenameRuleContext>(), 0).Returns("after-remove");
var replace = Substitute.For<IRenameRule>();
replace.Apply("after-remove", Arg.Any<RenameRuleContext>(), 0).Returns("after-replace");
// ... 3 more substitutes
var pipeline = new RenameRulePipelineService(remove, replace, /*case*/, /*add*/, /*numbering*/);
var result = pipeline.ApplyRules("input", new RenameRuleContext(...), 0);
result.Should().Be("final");
Received.InOrder(() => { remove.Apply(...); replace.Apply(...); /* etc */ });
```

If `RenameRulePipelineService` takes a `List<IRenameRule>` or composite type instead of 5 ctor args, adjust accordingly. If it lazily resolves rules from DI, prefer testing via the public composition surface and asserting the final returned string.

Use `Received.InOrder` to assert rule ordering. For null-context test: `Action act = () => pipeline.ApplyRules("x", null!, 0); act.Should().Throw<ArgumentNullException>();` — if production code doesn't currently guard, ADD a guard (one-line `?? throw new ArgumentNullException(nameof(context))`) per Phase 4 style.

Keep tests pure xUnit; no Revit API.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --filter "FullyQualifiedName~RenameRulePipelineServiceTests"</automated>
  </verify>
  <done>≥ 5 GREEN tests in RenameRulePipelineServiceTests covering ordering, index propagation, null-throws, inactive pass-through, ctor null-guard. All previously skip-gated rows un-Skipped. Anchor remains. 0 Failed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract 3 pure helpers in BaseElementCollectionService + flip RED rows to GREEN</name>
  <files>src/Services/Renaming/BaseElementCollectionService.cs, LECG.Tests/Services/BaseElementCollectionServiceTests.cs, .planning/phases/05-verification-and-polish/05-VERIFICATION.md</files>
  <behavior>
    - `TryGetGroupLabel(Func<ForgeTypeId> getId, Func<ForgeTypeId, string> getLabel) → string` returns label when both delegates succeed, "" when either throws
    - `DispatchScopeFlags(bool types, bool families, bool materials, bool parameters, ...) → ScopeMask` (or enum bitfield) maps individual scope booleans to a deterministic combined mask; covers Types-only / Families-only / Materials-only / Parameters-only / Multi-scope cases
    - `MergeParamScanResults(IReadOnlyList<ElementData> scanA, IReadOnlyList<ElementData> scanB, ...) → List<ElementData>` deduplicates by `(familyId, paramName)`; empty inputs → empty output
  </behavior>
  <action>
Read `src/Services/Renaming/BaseElementCollectionService.cs` in full (335 LOC). Locate:
- ParamGroup try/catch at `:234` and `:304` (RESEARCH.md §Sources)
- The 9-branch scope dispatch cascade (Types/Families/Views/Sheets/Materials/FillPatterns/Object-styles/Line-styles/FamilyParameters)
- Phase A/B param scan dedup block (FamilySymbol then FamilyInstance)

**Extract three internal static helpers**, each ≤ 30 LOC:

1. `TryGetGroupLabel(Func<ForgeTypeId> getId, Func<ForgeTypeId, string> getLabel) → string` (~10 LOC)
   - Catches both `getId()` and `getLabel(id)` exceptions; returns `""` on failure.
   - Replace the try/catch wrapper at `:234` AND `:304` with calls to this helper (PARTIAL dedup — observable contract unified; do NOT collapse the two call-sites further; CONTEXT defers full ParamGroup dedup to v1.2).
   - Document `// NOTE: full dedup of ParamGroup branches deferred to v1.2 Phase 6` in code.

2. `DispatchScopeFlags(...)` (~15 LOC)
   - Pure flag→mask mapping. Define a small `[Flags] internal enum ScopeMask` if none exists.
   - Production scope-dispatch code remains in place but calls `DispatchScopeFlags` for the mask used downstream (or, if cleaner refactor risk is too high, leave production untouched and just expose the helper that mirrors the dispatch logic — pure data, no Revit API).

3. `MergeParamScanResults(IReadOnlyList<ElementData> scanA, IReadOnlyList<ElementData> scanB) → List<ElementData>` (~25 LOC)
   - Deduplicates by `(familyId, paramName)` using `Dictionary<long, HashSet<string>>` per `seenParamsByFamily` semantics observed at the Phase A/B param scan site.
   - Replace inline dedup loop with helper call.

**Constraint check:** if any extraction would exceed ~30 LOC or require invasive surgery, STOP and leave the corresponding skip-gated RED test naming `"requires Revit harness — covered by manual verification"`. Update matrix row 5 status accordingly (⚠ partial vs ✅ full).

**Then un-Skip the 10 deepening RED rows** added in Wave 0 (`05-00-PLAN.md` Task 2) and write GREEN bodies:

```csharp
[Fact]
public void TryGetGroupLabel_GetIdSucceeds_GetLabelReturnsString_ReturnsLabel()
{
    var id = new ForgeTypeId(); // or stub
    var result = BaseElementCollectionService.TryGetGroupLabel(() => id, _ => "Identity Data");
    result.Should().Be("Identity Data");
}

[Fact]
public void TryGetGroupLabel_GetIdThrows_ReturnsEmptyString()
{
    var result = BaseElementCollectionService.TryGetGroupLabel(
        () => throw new InvalidOperationException(),
        _ => "should-not-be-called");
    result.Should().Be("");
}
```

Mirror this style for `DispatchScopeFlags` (5 scenarios) and `MergeParamScanResults` (2 scenarios — dedup + empty).

**Leave the 2 Revit-iteration tests skip-gated** with reason `"requires Revit FilteredElementCollector harness — covered by manual Revit verification per 05-VERIFICATION.md"`.

**Update `05-VERIFICATION.md`:**
- Row 2 (RenameRulePipelineService): `❌ → Wave 1` → `✅`
- Row 5 (BaseElementCollectionService): `⚠ → Wave 1` → `✅` (note in scenarios col: "Revit-iteration branches manually verified")

Build command: `dotnet build LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true`.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --filter "FullyQualifiedName~BaseElementCollectionServiceTests"</automated>
  </verify>
  <done>3 internal static helpers added to BaseElementCollectionService.cs; ≤ 30 LOC each. ≥ 8 new GREEN deepening tests (TryGetGroupLabel × 3, DispatchScopeFlags × 5, MergeParamScanResults × 2). 2 Revit-iteration tests remain Skipped with manual-verification reason. Original 4 GREEN tests unchanged. Matrix rows 2 and 5 flipped to ✅. Full suite 100% GREEN.</done>
</task>

</tasks>

<verification>
- Full suite GREEN; new tests count up by ≥ 13.
- Each extracted helper independently verified by ≥ 2 tests.
- No production behavior change beyond the helper extraction refactor.
- Matrix rows 2 and 5 ✅ in 05-VERIFICATION.md.
</verification>

<success_criteria>
- [ ] RenameRulePipelineServiceTests: 5+ GREEN, anchor remains
- [ ] BaseElementCollectionServiceTests: 8+ new GREEN, 4 existing GREEN, 2 Revit-bound Skipped with manual reason
- [ ] 3 internal static helpers added, each ≤ 30 LOC
- [ ] Production scope dispatch / ParamGroup try/catch refactored minimally to call new helpers (or helpers added without refactor if invasive)
- [ ] 05-VERIFICATION.md rows 2 + 5 flipped to ✅
- [ ] Full suite 100% GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/05-verification-and-polish/05-01-SUMMARY.md`.
</output>
