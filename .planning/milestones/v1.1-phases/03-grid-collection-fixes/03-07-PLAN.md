---
phase: 03-grid-collection-fixes
plan: 07
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-05"]
files_modified:
  - src/ViewModels/SplitBoundariesViewModel.cs
  - src/Views/SplitBoundariesView.xaml
  - src/ViewModels/ConvertToposolidToFloorViewModel.cs
  - src/Views/ConvertToposolidToFloorView.xaml
  - src/ViewModels/ConvertFloorToToposolidViewModel.cs
  - src/Views/ConvertFloorToToposolidView.xaml
autonomous: true
requirements:
  - REQ-01
must_haves:
  truths:
    - "SplitBoundaries, ConvertToposolidToFloor, ConvertFloorToToposolid screens render ElementGridControl"
    - "Each row shows non-blank Name and Category resolved via ElementLabelService"
    - "Status column carries per-screen outcome text previously embedded in summary strings"
  artifacts:
    - path: "src/ViewModels/SplitBoundariesViewModel.cs"
      provides: "RowItems collection + ElementLabelService integration"
      contains: "ElementRowViewModel"
    - path: "src/ViewModels/ConvertToposolidToFloorViewModel.cs"
      provides: "RowItems collection + ElementLabelService integration"
      contains: "ElementRowViewModel"
    - path: "src/ViewModels/ConvertFloorToToposolidViewModel.cs"
      provides: "RowItems collection + ElementLabelService integration"
      contains: "ElementRowViewModel"
  key_links:
    - from: "Each migrated VM"
      to: "ElementLabelService.GetLabels"
      via: "Element-to-row mapping"
      pattern: "ElementLabelService\\.GetLabels"
---

<objective>
Migrate the second batch of text-summary screens — SplitBoundaries, ConvertToposolidToFloor, ConvertFloorToToposolid — using the same pattern proven in Plan 03-06. Parallel-safe with Plan 03-06 (disjoint file sets).

Purpose: REQ-01 — eliminate text-summary anti-pattern in the remaining 3 of 6 affected VMs.
Output: 3 VMs + 3 Views migrated; build clean.
</objective>

<execution_context>
@C:/Users/LECG Arquitectura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LECG Arquitectura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-grid-collection-fixes/03-CONTEXT.md
@.planning/phases/03-grid-collection-fixes/03-RESEARCH.md
@.planning/phases/03-grid-collection-fixes/03-01-SUMMARY.md
@.planning/phases/03-grid-collection-fixes/03-02-SUMMARY.md
@.planning/phases/03-grid-collection-fixes/03-05-SUMMARY.md
@src/ViewModels/SplitBoundariesViewModel.cs
@src/Views/SplitBoundariesView.xaml
@src/ViewModels/ConvertToposolidToFloorViewModel.cs
@src/Views/ConvertToposolidToFloorView.xaml
@src/ViewModels/ConvertFloorToToposolidViewModel.cs
@src/Views/ConvertFloorToToposolidView.xaml

<interfaces>
ElementLabelService, ElementRowViewModel, ElementGridControl — same as Plan 03-06.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SplitBoundaries + ConvertToposolidToFloor</name>
  <files>
    src/ViewModels/SplitBoundariesViewModel.cs,
    src/Views/SplitBoundariesView.xaml,
    src/ViewModels/ConvertToposolidToFloorViewModel.cs,
    src/Views/ConvertToposolidToFloorView.xaml
  </files>
  <action>
    Apply the migration pattern documented in Plan 03-06 §Task 1 to both VMs:

    1. Swap `ObservableCollection<string> SelectedElementSummaries` → `ObservableCollection<ElementRowViewModel> RowItems`.
    2. Rewrite `UpdateSelectedElementSummaries` to call `ElementLabelService.GetLabels(element)` and populate `ElementRowViewModel`.
    3. Per-screen Status column content:
       - **SplitBoundaries**: extract whatever the existing summary's trailing segment carries (likely segment count or boundary count) into Status.
       - **ConvertToposolidToFloor**: extract conversion-eligibility status (e.g., "Eligible", "Already a Floor", "Has hosts — review").
    4. In each View.xaml, replace the ListBox/ItemsControl over strings with `<controls:ElementGridControl RowItems="{Binding RowItems}"/>`. Preserve surrounding layout.

    Verify after this task: both VMs no longer reference `ObservableCollection<string>` and both Views render `ElementGridControl`.
  </action>
  <verify>
    <automated>dotnet build LECG.sln --no-restore</automated>
    Expected: clean build.

    <automated>dotnet test LECG.Tests/LECG.Tests.csproj --no-restore</automated>
    Expected: full suite green.
  </verify>
  <done>
    Both VMs and Views migrated; no string-summary collection remains in either; build clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate ConvertFloorToToposolid</name>
  <files>src/ViewModels/ConvertFloorToToposolidViewModel.cs, src/Views/ConvertFloorToToposolidView.xaml</files>
  <action>
    Same migration pattern. Status column carries per-row floor-to-toposolid eligibility outcome (e.g., "Eligible", "Has openings — review", "Mass-host — unsupported").

    Final cross-VM grep after this task to confirm both batches are clean:
    ```
    grep -rn "ObservableCollection<string> SelectedElementSummaries" src/ViewModels/
    ```
    Expected hits: zero across the six text-summary VMs (DivideToposolid, FixPoints, ConvertCad, SplitBoundaries, ConvertToposolidToFloor, ConvertFloorToToposolid).
  </action>
  <verify>
    <automated>dotnet build LECG.sln --no-restore</automated>
    Expected: clean build.

    <automated>dotnet test LECG.Tests/LECG.Tests.csproj --no-restore</automated>
    Expected: full suite green.
  </verify>
  <done>
    Cross-VM grep returns zero hits; both files migrated; build clean.
  </done>
</task>

</tasks>

<verification>
- Three VMs migrated; three Views migrated.
- Build + full unit suite green.
- Manual Revit verification deferred to Plan 03-09.
</verification>

<success_criteria>
All six text-summary screens migrated to interactive grids. Manual verification consolidated into the phase-end checkpoint (Plan 03-09).
</success_criteria>

<output>
After completion, create `.planning/phases/03-grid-collection-fixes/03-07-SUMMARY.md` documenting per-screen Status content and any quirks. Note final cross-VM grep result.
</output>
