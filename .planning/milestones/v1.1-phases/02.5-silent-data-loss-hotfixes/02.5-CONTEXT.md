# Phase 2.5: Silent Data-Loss Hotfixes — Context

**Gathered:** 2026-05-08
**Updated:** 2026-05-10
**Status:** Code complete (3/3 plans) — ConvertFamily + Phase 2 regression manual Revit verify pending; REQ-08/09 trusted by code review.
**Source:** `.planning/research/SYNTHESIS.md` (5-plugin audit) + user decisions via /gsd:discuss-phase

<domain>
## Phase Boundary

Stop three silent partial-success / data-loss bugs surfaced by the 5-plugin audit. Contained, fast. Files locked to:

- `src/Services/PurgeAndCompaction/TextStyleCompactionService.cs:176` (REQ-08, locale)
- `src/Commands/CategoryChangerCommand.cs:190` → `SwapInstances` (REQ-09, instance location types)
- `src/Services/FamilyConversion/FamilyConversionService.cs` (REQ-10, host/curve/symbol/load-order)

Out of scope: cross-cutting infrastructure (logging unification, dialog whitelist, progress contract), per-plugin polish, and any v1.2 "Plugin Maturity" work. Service over-decomposition cleanup deferred to v1.2.

</domain>

<decisions>
## Implementation Decisions

### A. Failure UX — refuse-all everywhere

- **Category Changer (Plan 2.5-2):** Refuse-all. If ANY instance has unsupported geometry (`LocationCurve`, hosted, face-hosted not handled by `SwapInstances`), abort the entire command. No partial migrations. Log every unsupported instance with element ID + reason.
- **Convert Family (Plan 2.5-3):** All-or-nothing. Partial migration failure → roll back transaction; originals untouched, new family unloaded.
- **Compact Styles (Plan 2.5-1):** When a parameter is unreadable via the locale-safe path, treat as **distinct** — never merge a style whose signature can't be fully read. Safe-by-default; may leave near-duplicates for the user to resolve manually.
- **Logging surface:** All refusals/skips go to `LogView` only. No TaskDialog popups, no end-of-run summary dialog.

### B. Convert Family — pre-flight + rollback semantics

- **Pre-flight validator** runs BEFORE any destructive op. Walks all selected instances and verifies:
  1. Original symbol's type name exists in the new family (Q-B3).
  2. Host is reachable for hosted instances (Q-B4) — fail if host deleted, in linked file, or category-mismatched.
  3. `LocationCurve` instances are handled by the migration path.
- **Symbol pick:** Match new-family type by exact name. If new family lacks that type name → fail pre-flight for that instance. **No first-symbol fallback.**
- **Host fail:** Refuse the instance at pre-flight. Per Q-B1 + Q-A1 refuse-all, any failed instance cascades to refusing the whole batch.
- **Order:** Pre-flight → load new family → migrate instances → delete originals. NEVER delete originals before new family is loaded and migrations succeed.

### C. Plan order, commits, branching

- **Execution order:** Compact Styles (2.5-1) → Category Changer (2.5-2) → Convert Family (2.5-3). Smallest-to-largest per SYNTHESIS recommendation. *(Done 2026-05-09; all three code-complete.)*
- **Commits:** 3 independent commits, one per plan. Each plan self-contained; if Convert Family slips it does not block the other two from being committed.
- **Branch:** Stay on `codex/review` (matches Phase 2 pattern).
- **Release:** Wait for full Phase 2.5 verify (per §F below) before tagging/releasing. Single release entry covers all 3 fixes.

### D. Phase 2 interaction

- **Code work:** Phase 2.5 proceeds in parallel with Phase 2 — no file overlap. *(Done.)*
- **Verify cadence — superseded by §F.** Original "all three plans verified in one Revit session at phase end" replaced by a narrowed must-pass set after code review.

### E. Test strategy

- **Bar:** Manual Revit checklist only. No unit-test harness in this phase. Test infra deferred to v1.2.
- **Checklist location:** Each plan's `SUMMARY.md` carries its own "Verification" section (matches Phase 2 pattern).
- **Fixtures:** User has real-project fixtures. Claude specifies what's needed; user supplies — see §F for the narrowed must-pass fixture set.
- **Regression:** Phase 2 (FormulaAutoGrouping) re-verified in the same Revit session as Phase 2.5 ConvertFamily verify. Single pass covers both.

### F. Phase-end Revit verify — narrowed must-pass (added 2026-05-10)

- **Schedule:** Next session. Run before any Phase 4 planning.
- **Must-pass scenarios:**
  1. **ConvertFamily host preservation (REQ-10).** Wall-hosted family with multiple non-default type names. Verify originals keep host after migration; new family loaded before any delete; missing type name aborts pre-flight cleanly.
  2. **Phase 2 regression (REQ-07).** FormulaAutoGrouping re-verified in the same Revit session — formula parameters move to "Other" group without rollback, no silent failure.
- **Trusted by code review (no Revit demo required):**
  - **Compact Styles (REQ-08).** Locale-safe BIP signature + orientation sentinel — code-reviewed, unit reasoning sufficient. Same trust pattern as Phase 2 / REQ-07. REQ-08 status remains Complete.
  - **CategoryChanger refuse-all (REQ-09).** Detect-and-refuse path code-reviewed; unit reasoning sufficient. REQ-09 status remains Complete.
- **Opportunistic checks:** If a Spanish-locale fixture or mixed-batch CategoryChanger scenario happens to be loaded during the ConvertFamily session, sanity-check; otherwise skip.
- **Build context:** Revit is open during local builds — use `dotnet build LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true` to bypass MSB3027 file-lock.

### Claude's Discretion

- Exact refactor shape inside `SwapInstances` to detect-and-refuse unsupported geometry. *(Implemented; see STATE.)*
- Whether the pre-flight validator in Convert Family lives in `FamilyConversionService` or a sibling class. *(Implemented; see STATE.)*
- ForgeTypeId vs `BuiltInParameter` for the locale-safe text-style signature lookup. *(Resolved: BIP TEXT_ALIGNMENT + orientation sentinel.)*
- Log message format/tags for face-hosted refusal in CategoryChanger — within existing log conventions, no v1.2 roadmap leakage to end-user log.

</decisions>

<specifics>
## Specific References

- **Plan 2.5-1 file:** `src/Services/PurgeAndCompaction/TextStyleCompactionService.cs` line ~176 — locale-safe BIP-based signature implemented.
- **Plan 2.5-2 file:** `src/Commands/CategoryChangerCommand.cs` line ~190 — `SwapInstances` refuse-all pre-flight implemented.
- **Plan 2.5-3 file:** `src/Services/FamilyConversion/FamilyConversionService.cs` — pre-flight validator + load-before-delete + exact symbol-name match + hosted/curve overloads implemented.
- **Source audit:** `.planning/research/SYNTHESIS.md` (Critical Findings #1, #2, #3, #4, #5, #6).
- **Per-plugin reports:** `.planning/research/plugin-{compact-styles,category-changer,convert-family}.md`.

</specifics>

<code_context>
## Existing Code Insights

### Reusable Assets
- `FamilyLoadOptionsFactory` — verified to force overwrite; load-before-delete safe with live instances (REQ-10).
- `FamilyInstanceData` — captures HostId + LocationCurve, but lacks HostFace capture (face-hosted preservation deferred).
- `Logger.Instance.LogWarning` / `LogView` — single logging surface for all refusals/skips this phase. No TaskDialog.

### Established Patterns
- Phase 2 trust pattern (REQ-07) — code-complete + xUnit green is sufficient when manual Revit demo would gate progress unreasonably. Re-applied to REQ-08 and REQ-09 in §F.
- Refuse-all over partial-success — explicit invariant for both CategoryChanger (REQ-09) and ConvertFamily (REQ-10).
- Locale-safe parameter access via `BuiltInParameter` (not English `LookupParameter` strings).

### Integration Points
- ConvertFamily verify must run before Phase 4 planning (per §F) — REQ-10 acceptance gates v1.1 close-out.
- Phase 2 regression piggybacks on the ConvertFamily Revit session — single fixture session covers both REQ-07 sanity and REQ-10 acceptance.

</code_context>

<deferred>
## Deferred Ideas

### To v1.2 — Plugin Maturity
- Logging unification (single severity-preserving interface) — v1.2 Phase 1.
- Dialog whitelist replacing blanket `e.OverrideResult(2)` — v1.2 Phase 1.
- ConvertFamily preview UI showing instance blast radius — v1.2 Phase 4.
- ConvertFamily service over-decomposition cleanup (dead `FamilyConversionNamingService`, inert `customName`/`isTemporary`, ~10 single-call wrappers) — v1.2 Phase 4.
- CategoryChanger service extraction from 200-LOC `CategoryChangerEventHandler` — v1.2 Phase 5.
- Compact Styles real view + scope toggles + dry-run — v1.2 Phase 3.
- Unit test harness for ConvertFamily / CategoryChanger / Compact Styles — v1.2.

### Scope-trimmed during 2.5 execution (data-loss-prevention preserved; capability deferred)
- **CategoryChanger LocationCurve / hosted / face-hosted placement paths** — refuse-all path shipped; actual transplant for non-LocationPoint instances deferred to v1.2.
- **ConvertFamily face-hosted preservation** — `FamilyInstanceData` lacks HostFace capture; Branch 3 fallback documented as known limitation. Wall-hosted + LocationCurve preservation shipped.

### Explicitly rejected for 2.5
- TaskDialog summary at command end ("Migrated N, skipped M") — LogView only; reconsider in v1.2 plugin polish.
- Per-instance interactive prompts during failure.
- Compact Styles ES-locale and CategoryChanger refuse-all in must-pass Revit verify — trusted by code review per §F instead.

</deferred>

---

*Phase: 02.5-silent-data-loss-hotfixes*
*Context gathered: 2026-05-08 via /gsd:discuss-phase*
*Updated: 2026-05-10 — phase-end verify narrowed; deferred items captured; status flipped to code-complete.*
