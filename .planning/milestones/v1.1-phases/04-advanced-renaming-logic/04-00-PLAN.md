---
phase: 04-advanced-renaming-logic
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - LECG.Tests/Services/RenameSkipDetectorTests.cs
  - LECG.Tests/Services/FormulaUpdateServiceTests.cs
  - LECG.Tests/Services/BatchRenameSafeRenameTests.cs
  - LECG.Tests/Services/SearchReplacePreviewServiceTests.cs
  - src/ViewModels/Components/ElementRowViewModel.cs
  - .planning/phases/04-advanced-renaming-logic/04-VALIDATION.md
autonomous: true
requirements: [REQ-02, REQ-03, REQ-04]
must_haves:
  truths:
    - "Wave 0 test files exist and compile"
    - "Skip-gated RED tests reference implementing plan IDs (04-01, 04-02, 04-03, 04-04)"
    - "`dotnet test --filter Category=Renaming` runs without compile errors"
    - "`ElementRowViewModel.IsRenameable` property exists, defaults true"
    - "VALIDATION.md flags `nyquist_compliant: true`, `wave_0_complete: true`"
  artifacts:
    - path: "LECG.Tests/Services/RenameSkipDetectorTests.cs"
      provides: "RED tests for narrowed GetRenameSkipReason + new GetStandardItemSkipReason"
      contains: "Skip = \"Implement in plan 04-01\""
    - path: "LECG.Tests/Services/FormulaUpdateServiceTests.cs"
      provides: "RED tests for IFormulaUpdateService wiring (REQ-02)"
      contains: "Skip = \"Implement in plan 04-03\""
    - path: "LECG.Tests/Services/BatchRenameSafeRenameTests.cs"
      provides: "RED tests for safe-rename formula + dimension paths"
      contains: "Skip = \"Implement in plan 04-03\""
    - path: "src/ViewModels/Components/ElementRowViewModel.cs"
      provides: "IsRenameable property"
      contains: "IsRenameable"
  key_links:
    - from: "LECG.Tests/Services/SearchReplacePreviewServiceTests.cs"
      to: "ElementRowViewModel.IsRenameable"
      via: "extended tests asserting IsRenameable propagation"
      pattern: "IsRenameable"
---

<objective>
Establish Wave 0 test scaffolds + ViewModel property that downstream Phase 4 plans flip from RED to GREEN.

Purpose: Provide a Nyquist-compliant feedback loop for REQ-02/03/04 so every implementing task has an existing `<automated>` verify command. Carry forward the Phase 03-00 skip-gated RED test pattern.

Output: 3 new test files, 1 extended test file, 1 ViewModel property addition, VALIDATION.md flagged complete.
</objective>

<execution_context>
@C:/Users/LECG Arquitectura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LECG Arquitectura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-advanced-renaming-logic/04-CONTEXT.md
@.planning/phases/04-advanced-renaming-logic/04-RESEARCH.md
@.planning/phases/04-advanced-renaming-logic/04-VALIDATION.md
@LECG.Tests/Services/FormulaNameUpdaterTests.cs
@LECG.Tests/Services/SearchReplacePreviewServiceTests.cs
@src/ViewModels/Components/ElementRowViewModel.cs
@src/Services/Renaming/BatchRenameExecutionService.cs
@src/Services/Renaming/FormulaUpdateService.cs

<interfaces>
<!-- Existing patterns from Phase 03-00 to follow exactly -->

Skip-gated RED test pattern (from Phase 03-00):
```csharp
[Fact(Skip = "Implement in plan 04-XX")]
[Trait("Category", "Renaming")]
public void Name_DescribingExpectedBehavior_Phase4Active() { /* RED */ }
```

One non-skipped anchor test per fixture (so `dotnet test --filter` discovers the class):
```csharp
[Fact]
[Trait("Category", "Renaming")]
public void Fixture_Anchor_Exists() => Assert.True(true);
```

ElementRowViewModel current shape (src/ViewModels/Components/ElementRowViewModel.cs):
- `[ObservableProperty]` only on `IsChecked` (Phase 03 decision)
- Identity/display fields are plain auto-properties set at row creation
- `public string Status { get; set; } = "";`
- Phase 4 ADD: `public bool IsRenameable { get; set; } = true;` (plain auto-property, default true)

FormulaUpdateService current shape (zero consumers):
```csharp
public sealed class FormulaUpdateService : IFormulaUpdateService
{
    public string UpdateFormula(string formula, string oldName, string newName)
        => FormulaNameUpdater.UpdateFormula(formula, oldName, newName);
}
```

Existing methods in BatchRenameExecutionService Phase 4 will touch:
- `GetRenameSkipReason(FamilyParameter fp, string newName, FamilyManager mgr, HashSet<string> dimensionLabels, HashSet<string> formulaReferenced, HashSet<string> elementAssociated)` — lines 334-376
- `BuildDimensionLabelNames(Document famDoc)` — lines 381-404
- `BuildFormulaReferencedNames(...)` — line 406
- `RenameFamilyParameters(...)` — lines 247-286
- EditFamily/LoadFamily cycle — lines 142-216
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IsRenameable to ElementRowViewModel</name>
  <files>src/ViewModels/Components/ElementRowViewModel.cs</files>
  <action>
    Add a public auto-property `IsRenameable` to `ElementRowViewModel`:
    - Type: `bool`
    - Default: `true`
    - Plain auto-property (NOT `[ObservableProperty]`) — follows the Phase 03 decision that only `IsChecked` is observable; identity/display fields are set once at row creation.
    - Place adjacent to the `Status` property (around line 25).
    - XML doc comment: `/// <summary>False when the row was skipped during dry-run pre-flight (formula/dimension/standard-item conditions). Drives WPF checkbox IsEnabled and muted-row Style trigger.</summary>`

    Do NOT modify any other property. Do NOT touch existing consumers — they will continue to read the default `true` until Wave 1 sets it.
  </action>
  <verify>
    <automated>dotnet build LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --nologo -v minimal</automated>
  </verify>
  <done>
    - `ElementRowViewModel.IsRenameable` exists and compiles
    - No other property changed
    - Solution still builds 0 errors / 0 warnings with `-p:SkipRevitDeploy=true`
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RenameSkipDetectorTests.cs (skip-gated RED)</name>
  <files>LECG.Tests/Services/RenameSkipDetectorTests.cs</files>
  <action>
    Create new xUnit fixture covering both the narrowed `GetRenameSkipReason` and the new `GetStandardItemSkipReason`. All tests must be `[Fact(Skip = "Implement in plan 04-01")]` except one anchor test.

    Required test names (one `[Fact]` each, all `[Trait("Category", "Renaming")]`):

    Anchor (NOT skipped):
    - `Fixture_Anchor_Exists` returning `Assert.True(true)` — DisplayName = "anchor — delete when plan 04-01 fills this fixture"

    Skip-gated RED tests for narrowed `GetRenameSkipReason` (REQ-02/03/04):
    - `GetRenameSkipReason_FormulaReferenced_ReturnsNull_AfterPhase4Narrowing` — Skip reason: "Implement in plan 04-01"
    - `GetRenameSkipReason_DimensionLabel_ReturnsNull_AfterPhase4Narrowing` — Skip reason: "Implement in plan 04-01"
    - `GetRenameSkipReason_ElementAssociated_ReturnsNull_AfterPhase4Narrowing` — Skip reason: "Implement in plan 04-01"
    - `GetRenameSkipReason_BuiltIn_StillReturnsReason_NegativeId` — Skip reason: "Implement in plan 04-01"
    - `GetRenameSkipReason_Reporting_StillReturnsReason` — Skip reason: "Implement in plan 04-01"
    - `GetRenameSkipReason_NameConflict_StillReturnsReason_NoAutoSuffix` — Skip reason: "Implement in plan 04-01"

    Skip-gated RED tests for new `GetStandardItemSkipReason` (REQ-04):
    - `GetStandardItemSkipReason_ReadOnlyType_ReturnsReason` — Skip reason: "Implement in plan 04-01"
    - `GetStandardItemSkipReason_NameConflictInSameScope_ReturnsReason` — Skip reason: "Implement in plan 04-01"
    - `GetStandardItemSkipReason_SystemFamily_IsSystemFamilyTrue_ReturnsReason` — Skip reason: "Implement in plan 04-01"
    - `GetStandardItemSkipReason_SheetNumberLocked_ReturnsReason` — Skip reason: "Implement in plan 04-01"
    - `GetStandardItemSkipReason_RenameableSheet_ReturnsNull` — Skip reason: "Implement in plan 04-01"

    Body of every skip-gated test: leave empty `// RED — fills in plan 04-01` comment (the Skip attribute prevents execution).

    Namespace: `LECG.Tests.Services`. Class: `public class RenameSkipDetectorTests`.
    Do NOT add any using of Revit DB types that prevent compilation without Revit; use NSubstitute interfaces or string-shaped placeholders. Use `using Xunit; using FluentAssertions; using NSubstitute;` only.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --filter "FullyQualifiedName~RenameSkipDetectorTests" --nologo -v minimal</automated>
  </verify>
  <done>
    - File compiles
    - `Fixture_Anchor_Exists` runs and passes
    - All other tests appear as Skipped with the named plan reason
    - `dotnet test --filter Category=Renaming` discovers the fixture
  </done>
</task>

<task type="auto">
  <name>Task 3: Create FormulaUpdateServiceTests.cs + BatchRenameSafeRenameTests.cs (skip-gated RED)</name>
  <files>LECG.Tests/Services/FormulaUpdateServiceTests.cs, LECG.Tests/Services/BatchRenameSafeRenameTests.cs</files>
  <action>
    Create two new xUnit fixtures. Both follow Phase 03-00 skip-gated RED pattern with one anchor test each.

    FILE A — `LECG.Tests/Services/FormulaUpdateServiceTests.cs`:
    Namespace `LECG.Tests.Services`, class `FormulaUpdateServiceTests`.
    All `[Trait("Category", "Renaming")]`.
    - Anchor (NOT skipped): `Fixture_Anchor_Exists` → `Assert.True(true)`, DisplayName "anchor — delete when plan 04-03 fills this fixture"
    - `UpdateFormula_DelegatesTo_FormulaNameUpdater_WithExactArgs` — Skip "Implement in plan 04-03"
    - `UpdateFormula_EmptyFormulaInput_ReturnsEmpty_WithoutThrowing` — Skip "Implement in plan 04-03"
    - `IFormulaUpdateService_IsInjectableIntoBatchRenameExecutionService_ViaConstructor` — Skip "Implement in plan 04-03"

    FILE B — `LECG.Tests/Services/BatchRenameSafeRenameTests.cs`:
    Namespace `LECG.Tests.Services`, class `BatchRenameSafeRenameTests`.
    All `[Trait("Category", "Renaming")]`.
    - Anchor (NOT skipped): `Fixture_Anchor_Exists` → `Assert.True(true)`, DisplayName "anchor — delete when plans 04-03/04-04 fill this fixture"

    REQ-02 RED tests (Skip = "Implement in plan 04-03"):
    - `RenameFamilyParameters_FormulaReferenced_UpdatesAllReferencingFormulas`
    - `RenameFamilyParameters_FormulaReferenced_LogsSuccessWithFormulaCount`
    - `RenameFamilyParameters_PerParamSubTransaction_RollsBackOneFailure_OtherParamsCommit`
    - `RenameFamilyParameters_CrossBatchNameCollision_DetectedInDryRun_FlippedToSkip` (covers Pitfall 5 from RESEARCH)

    REQ-03 RED tests (Skip = "Implement in plan 04-04"):
    - `RenameFamilyParameters_DimensionLabel_ReassignsAllMatchingDimensions`
    - `RenameFamilyParameters_DimensionLabel_LogsSuccessWithDimensionCount`
    - `RenameFamilyParameters_DimensionLabel_StaleParamReference_ReFetchedByNewName` (covers Pitfall 2)

    REQ-04 RED tests (Skip = "Implement in plan 04-01"):
    - `RenameStandardItems_PreFlightDryRun_SetsStatusAndUnchecks_OnSkipCondition`
    - `RenameStandardItems_PreFlightDryRun_LogsOneWarningPerSkippedRow`

    Body of every skip-gated test: leave `// RED — fills in plan 04-0X` comment.

    Usings: `using Xunit; using FluentAssertions; using NSubstitute;` only. Do NOT add Revit-DB usings that would prevent test-project compilation. Stub interfaces only.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --filter "FullyQualifiedName~FormulaUpdateServiceTests|FullyQualifiedName~BatchRenameSafeRenameTests" --nologo -v minimal</automated>
  </verify>
  <done>
    - Both files compile
    - Both anchor tests pass
    - All other tests Skipped with plan ID in reason
    - Full suite still GREEN (`dotnet test ... -p:SkipRevitDeploy=true` — no regressions)
  </done>
</task>

<task type="auto">
  <name>Task 4: Extend SearchReplacePreviewServiceTests.cs with IsRenameable RED stubs</name>
  <files>LECG.Tests/Services/SearchReplacePreviewServiceTests.cs</files>
  <action>
    Read the existing file first. APPEND (do not modify existing tests) the following skip-gated RED tests at end of the class. All `[Trait("Category", "Renaming")]`.

    REQ-04 RED tests (Skip = "Implement in plan 04-02"):
    - `ProcessPreview_FamilyParameterSkipReason_PopulatesStatus_WithReasonString`
    - `ProcessPreview_FamilyParameterSkipReason_SetsIsRenameableFalse_AndIsCheckedFalse`
    - `ProcessPreview_SafeRenameRow_PopulatesStatus_WithSideEffectCount` (e.g. status reads "+2 formulas, +1 dimension")
    - `ProcessPreview_RenameableRow_LeavesIsRenameableTrue_AndKeepsUserCheckedState`
    - `ProcessPreview_CrossBatchNameCollision_FlipsSecondRowToSkip_WithReason` (covers Pitfall 5)

    Bodies: leave `// RED — fills in plan 04-02` comment.

    Do not touch any pre-existing assertions or arrange code — the existing tests must continue to pass.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --filter "FullyQualifiedName~SearchReplacePreviewServiceTests" --nologo -v minimal</automated>
  </verify>
  <done>
    - All pre-existing tests still GREEN
    - 5 new tests appear as Skipped with "Implement in plan 04-02"
    - Full suite still GREEN
  </done>
</task>

<task type="auto">
  <name>Task 5: Update VALIDATION.md with per-task map + flip Wave 0 flags</name>
  <files>.planning/phases/04-advanced-renaming-logic/04-VALIDATION.md</files>
  <action>
    Edit `.planning/phases/04-advanced-renaming-logic/04-VALIDATION.md`:

    1. Frontmatter:
       - `status: ready`
       - `nyquist_compliant: true`
       - `wave_0_complete: true`

    2. Replace the "TBD" row in the Per-Task Verification Map with concrete rows covering every Phase 4 task. Format (one row per implementing task):
       | 04-01-T{N} | 04-01 | 1 | REQ-04 | unit | `dotnet test ... --filter "FullyQualifiedName~RenameSkipDetectorTests"` | ✅ | ⬜ pending |
       | 04-02-T{N} | 04-02 | 1 | REQ-04 | unit | `dotnet test ... --filter "FullyQualifiedName~SearchReplacePreviewServiceTests"` | ✅ | ⬜ pending |
       | 04-03-T{N} | 04-03 | 2 | REQ-02 | unit | `dotnet test ... --filter "FullyQualifiedName~FormulaUpdateServiceTests|FullyQualifiedName~BatchRenameSafeRenameTests"` | ✅ | ⬜ pending |
       | 04-04-T{N} | 04-04 | 3 | REQ-03 | unit | `dotnet test ... --filter "FullyQualifiedName~BatchRenameSafeRenameTests"` | ✅ | ⬜ pending |
       | 04-05-T1   | 04-05 | 4 | REQ-02/03/04 | manual | Phase-end Revit verify session | — | ⬜ pending |

    3. Tick the Wave 0 Requirements checklist (all 4 items checked).

    4. Tick all 6 Validation Sign-Off boxes.

    5. Change "**Approval:** pending" → "**Approval:** approved 2026-05-10 — Wave 0 complete".
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/04-advanced-renaming-logic/04-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/04-advanced-renaming-logic/04-VALIDATION.md && echo OK</automated>
  </verify>
  <done>
    - Frontmatter flags flipped to true
    - Per-task map has concrete rows (no TBD)
    - Wave 0 Requirements checked
    - Validation Sign-Off all checked
  </done>
</task>

</tasks>

<verification>
- All 4 new/extended test files compile
- Full suite (`dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true`) is GREEN — no regressions; new tests appear as Skipped
- `ElementRowViewModel.IsRenameable` is bindable from XAML (verified by build, not runtime)
- VALIDATION.md flags `nyquist_compliant: true` + `wave_0_complete: true`
</verification>

<success_criteria>
- 5 file additions/extensions land
- Every Wave 1+ plan can cite an `<automated>` command pointing to a RED test that flips GREEN when its work is done
- Anchor test per fixture so `--filter` discovers RED-only fixtures
- Solution builds clean, suite still 100/100 GREEN (Skip count climbs by ~20)
</success_criteria>

<output>
Create `.planning/phases/04-advanced-renaming-logic/04-00-SUMMARY.md` listing:
- Test files created and the plan ID each flips
- IsRenameable property location + default value
- VALIDATION.md state transition
- Confirmation that build was run with `-p:SkipRevitDeploy=true`
</output>
