---
phase: 04-advanced-renaming-logic
plan: 03
type: execute
wave: 2
depends_on: [00, 01, 02]
files_modified:
  - src/Services/Renaming/BatchRenameExecutionService.cs
  - LECG.Tests/Services/FormulaUpdateServiceTests.cs
  - LECG.Tests/Services/BatchRenameSafeRenameTests.cs
autonomous: true
requirements: [REQ-02]
must_haves:
  truths:
    - "BatchRenameExecutionService accepts IFormulaUpdateService via constructor injection (DI-registered in Bootstrapper.cs:131)"
    - "RenameFamilyParameters wraps each per-parameter rename + formula-update sequence in its own SubTransaction inside the EditFamily cycle"
    - "After a successful rename, every other FamilyParameter whose formula `ContainsReference(oldName)` has its formula rewritten via `IFormulaUpdateService.UpdateFormula` and committed via `FamilyManager.SetFormula`"
    - "If any sub-step throws, the offending parameter's SubTransaction rolls back; other params in the same family continue"
    - "Success log line reads `Renamed '{old}' to '{new}' (updated N formulas)` when formulaCount > 0"
    - "The `renamedCount` counter increments ONLY after a confirmed SubTransaction.Commit (no speculative pre-increment — Pitfall 3)"
    - "All FormulaUpdateServiceTests and REQ-02 BatchRenameSafeRenameTests pass"
  artifacts:
    - path: "src/Services/Renaming/BatchRenameExecutionService.cs"
      provides: "IFormulaUpdateService consumer + per-param SubTransaction + formula-update loop"
      contains: "IFormulaUpdateService"
  key_links:
    - from: "BatchRenameExecutionService constructor"
      to: "IFormulaUpdateService"
      via: "constructor parameter injected by Autofac/Bootstrapper"
      pattern: "IFormulaUpdateService"
    - from: "Per-param SubTransaction body"
      to: "FormulaNameUpdater.ContainsReference + IFormulaUpdateService.UpdateFormula + FamilyManager.SetFormula"
      via: "for-each over manager.Parameters with `string.IsNullOrEmpty(other.Formula)` guard"
      pattern: "FormulaNameUpdater\\.ContainsReference"
---

<objective>
Wire `IFormulaUpdateService` into `BatchRenameExecutionService` and rename formula-referenced FamilyParameters atomically. After each rename, rewrite every formula in the family that mentions the old name; commit/rollback per-parameter via SubTransaction.

Purpose: Close REQ-02. The user's mental model — "rename means rename, fix the references" — is finally honored.

Output: Modified BatchRenameExecutionService with injected `IFormulaUpdateService` and per-param SubTransaction loop. All REQ-02 RED tests GREEN.
</objective>

<execution_context>
@C:/Users/LECG Arquitectura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LECG Arquitectura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-advanced-renaming-logic/04-CONTEXT.md
@.planning/phases/04-advanced-renaming-logic/04-RESEARCH.md
@.planning/phases/04-advanced-renaming-logic/04-01-PLAN.md
@.planning/phases/04-advanced-renaming-logic/04-02-PLAN.md
@src/Services/Renaming/BatchRenameExecutionService.cs
@src/Services/Renaming/FormulaUpdateService.cs
@src/Bootstrapper.cs
@LECG.Core/Rename/FormulaNameUpdater.cs

<interfaces>
<!-- Existing service (zero consumers) -->
public interface IFormulaUpdateService
{
    string UpdateFormula(string formula, string oldName, string newName);
}

<!-- FormulaNameUpdater helpers (already used at line 426 + FormulaAutoGroupingCommand.cs:687) -->
public static class FormulaNameUpdater
{
    public static bool ContainsReference(string formula, string oldName);
    public static string UpdateFormula(string formula, string oldName, string newName);
}

<!-- Phase 02 SubTransaction pattern (proven in FormulaAutoGroupingCommand.MoveParamsToGroup) -->
using var subTx = new SubTransaction(famDoc);
subTx.Start();
try {
    // mutate FamilyManager state
    subTx.Commit();
    // increment counters AFTER commit
} catch (Exception ex) {
    if (subTx.HasStarted() && !subTx.HasEnded()) subTx.RollbackIfNotClosed();
    logger.LogWarning($"Skipped '{name}' in '{familyName}': {ex.Message}");
}

<!-- Bootstrapper.cs:131 — FormulaUpdateService already registered -->
builder.RegisterType<FormulaUpdateService>().As<IFormulaUpdateService>().SingleInstance();  // exact form may differ; verify

<!-- RenameFamilyParameters current call site (lines 247-286, signature inferred from RESEARCH) -->
private void RenameFamilyParameters(FamilyManager manager, Document famDoc, IEnumerable<ElementRowViewModel> rows, /* ... */)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject IFormulaUpdateService into BatchRenameExecutionService + GREEN FormulaUpdateServiceTests</name>
  <files>src/Services/Renaming/BatchRenameExecutionService.cs, LECG.Tests/Services/FormulaUpdateServiceTests.cs</files>
  <behavior>
    - `BatchRenameExecutionService` constructor accepts an `IFormulaUpdateService` parameter alongside its existing dependencies
    - The service is stored in a `private readonly IFormulaUpdateService _formulaUpdateService;` field
    - `Bootstrapper.cs` DI registration unchanged (already wires `FormulaUpdateService` → `IFormulaUpdateService`); the addition of the constructor parameter is automatically resolved by Autofac
    - `FormulaUpdateService.UpdateFormula` is a pure delegate to `FormulaNameUpdater.UpdateFormula(formula, oldName, newName)` — already implemented; tests assert delegation behavior
    - Empty-formula input returns empty string without throwing (FormulaNameUpdater short-circuits)
  </behavior>
  <action>
    Step A — Un-Skip the 3 RED tests in `LECG.Tests/Services/FormulaUpdateServiceTests.cs`:
    - `UpdateFormula_DelegatesTo_FormulaNameUpdater_WithExactArgs` — assert behavioral equivalence with `FormulaNameUpdater.UpdateFormula` for ≥3 representative inputs (matching cases already exercised in `FormulaNameUpdaterTests.cs`)
    - `UpdateFormula_EmptyFormulaInput_ReturnsEmpty_WithoutThrowing`
    - `IFormulaUpdateService_IsInjectableIntoBatchRenameExecutionService_ViaConstructor` — instantiate `BatchRenameExecutionService` with NSubstitute mocks for all dependencies including `Substitute.For<IFormulaUpdateService>()`, assert it constructs without throwing
    For the third test, the mocks must match the existing dependency list (read the current constructor first — likely `ITransactionService`, logger, `IFamilyEditorService` or similar; provide NSubstitute mocks for each).

    Step B — Modify `BatchRenameExecutionService.cs` constructor:
    - Add `IFormulaUpdateService formulaUpdateService` to the parameter list (place at the end to minimize call-site churn, OR after the related rename services to match code-locality conventions — read existing parameter order and choose accordingly).
    - Add `private readonly IFormulaUpdateService _formulaUpdateService;`
    - Assign in constructor body: `_formulaUpdateService = formulaUpdateService ?? throw new ArgumentNullException(nameof(formulaUpdateService));`
    - Verify Bootstrapper.cs:131 (or thereabouts) already registers `FormulaUpdateService` as `IFormulaUpdateService`. If the registration is `ServiceCollection`/Autofac style, no change is needed; DI auto-resolves. If a manual `new BatchRenameExecutionService(...)` call exists anywhere, update it.

    Step C — Build and run the targeted tests to confirm GREEN.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --filter "FullyQualifiedName~FormulaUpdateServiceTests" --nologo -v minimal</automated>
  </verify>
  <done>
    - All 3 FormulaUpdateServiceTests GREEN
    - `BatchRenameExecutionService` constructs without DI errors
    - Full suite still GREEN (other RED tests still skipped until Task 2)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-param SubTransaction + formula-update loop in RenameFamilyParameters</name>
  <files>src/Services/Renaming/BatchRenameExecutionService.cs, LECG.Tests/Services/BatchRenameSafeRenameTests.cs</files>
  <behavior>
    Inside the EditFamily/LoadFamily cycle (~lines 142-216), for each checked FamilyParameter row whose dry-run pass left it as a safe-rename candidate:
    1. Open `using var subTx = new SubTransaction(famDoc); subTx.Start();`
    2. Call `manager.RenameParameter(paramToRename, item.NewValue)`
    3. Iterate `manager.Parameters`; for each `other` with non-empty Formula and `FormulaNameUpdater.ContainsReference(other.Formula, item.OriginalValue) == true`:
       - `string updated = _formulaUpdateService.UpdateFormula(other.Formula, item.OriginalValue, item.NewValue);`
       - `manager.SetFormula(other, updated);`
       - `formulaCount++` (local to this SubTransaction)
    4. `subTx.Commit();`
    5. AFTER commit: `logger.LogSuccess($"Renamed '{item.OriginalValue}' to '{item.NewValue}' (updated {formulaCount} formulas)");` if formulaCount > 0, else `logger.LogSuccess($"Renamed '{item.OriginalValue}' to '{item.NewValue}'");`
    6. AFTER commit: `renamedCount++` (no speculative increment — Pitfall 3)
    7. On any exception: `subTx.RollbackIfNotClosed()` in catch, `logger.LogWarning($"Skipped '{item.OriginalValue}' in '{familyName}': {ex.Message}")`, do NOT increment renamedCount
    Cross-batch collision: already handled in the preview/dry-run pass (Plans 04-01/04-02); this loop trusts that survivors are non-colliding.
  </behavior>
  <action>
    Step A — Un-Skip and implement the REQ-02 RED tests in `LECG.Tests/Services/BatchRenameSafeRenameTests.cs`:
    - `RenameFamilyParameters_FormulaReferenced_UpdatesAllReferencingFormulas` — mock `IFormulaUpdateService.UpdateFormula` to return a sentinel; assert it was called once per referencing formula
    - `RenameFamilyParameters_FormulaReferenced_LogsSuccessWithFormulaCount` — assert `LogSuccess` was invoked with the "(updated N formulas)" suffix when N > 0
    - `RenameFamilyParameters_PerParamSubTransaction_RollsBackOneFailure_OtherParamsCommit` — arrange three params; mock `manager.RenameParameter` to throw on the middle one; assert renamedCount == 2 and a LogWarning for the failed one
    - `RenameFamilyParameters_CrossBatchNameCollision_DetectedInDryRun_FlippedToSkip` — verify the loop receives a row with `IsChecked == false` (collision-flipped by preview/dry-run) and skips it without entering SubTransaction

    NSubstitute for `FamilyManager`, `FamilyParameter`, `IFormulaUpdateService`, the logger. For tests that need to iterate `manager.Parameters`, return an `IEnumerable<FamilyParameter>` of mocks with Formula strings set via NSubstitute `.Returns(...)`.

    Step B — Modify `RenameFamilyParameters` (or its inner loop) in `BatchRenameExecutionService.cs`:
    - Locate the per-param body within the EditFamily/LoadFamily cycle. Search for the existing `manager.RenameParameter(` call (RESEARCH says it lives around lines 247-286).
    - Wrap each rename in the SubTransaction pattern from `<interfaces>`.
    - Insert the formula-update loop INSIDE the SubTransaction body, AFTER `RenameParameter` but BEFORE `Commit`.
    - Use the `formulaReferenced` HashSet (already built by `BuildFormulaReferencedNames` at line 426) only to OPT INTO the formula-update loop (i.e., skip the loop entirely if `row.Original` is not in the set — performance win, no behavior change).
    - Move `renamedCount++` to AFTER `subTx.Commit()` — eliminate the Pitfall 3 pattern at line ~185 for the per-param scope. (Note: the outer `count += renamedInFamily` at line 185 is the pre-existing v1.2-deferred bug; do NOT touch the outer increment — only ensure the inner increment is post-commit-guarded.)
    - Logging: use `_logger.LogSuccess` / `_logger.LogWarning` matching the existing field name in the service. Format strings exactly per the behavior contract.

    Step C — Pitfall 1 mitigation: confirm every catch path calls `subTx.RollbackIfNotClosed()` (the `using` declaration handles `Dispose`, but explicit rollback in catch is required so the SubTransaction is closed before exception propagates beyond the loop iteration).

    Step D — Do NOT touch the dimension-label loop yet — Plan 04-04 owns that. The REQ-03 RED tests in this fixture remain Skipped after this plan.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --filter "FullyQualifiedName~BatchRenameSafeRenameTests&Category=Renaming" --nologo -v minimal</automated>
  </verify>
  <done>
    - All 4 REQ-02 RED tests in `BatchRenameSafeRenameTests` flipped to GREEN
    - REQ-03 tests in same fixture still Skipped (owned by 04-04)
    - `renamedCount` increments ONLY after a confirmed `subTx.Commit()`
    - All SubTransactions have a guaranteed rollback path on exception
    - Full suite GREEN
  </done>
</task>

</tasks>

<verification>
- `dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --nologo -v minimal` GREEN
- `IFormulaUpdateService` is injected (Autofac resolves at runtime — verified via test that constructs the service)
- Per-param SubTransaction wraps rename + formula-update atomically
- Pitfall 3 not re-introduced in the new code
- Manual Revit verification of actual formula rewrite deferred to Plan 04-05
</verification>

<success_criteria>
- 3 FormulaUpdateServiceTests + 4 REQ-02 BatchRenameSafeRenameTests GREEN
- `FormulaUpdateService` no longer has zero consumers (`grep -r "IFormulaUpdateService" src/` returns the BatchRenameExecutionService field + the Bootstrapper registration at minimum)
- Solution builds 0 errors / 0 warnings with `-p:SkipRevitDeploy=true`
</success_criteria>

<output>
Create `.planning/phases/04-advanced-renaming-logic/04-03-SUMMARY.md` covering:
- Constructor signature change (parameter position, DI verification)
- SubTransaction pattern location in RenameFamilyParameters
- formula-update loop guard (`formulaReferenced.Contains` opt-in)
- Logging format actually emitted
- Test count flipped RED → GREEN (3 + 4)
- Note that Pitfall 3 outer-loop bug remains deferred to v1.2 Phase 6
</output>
