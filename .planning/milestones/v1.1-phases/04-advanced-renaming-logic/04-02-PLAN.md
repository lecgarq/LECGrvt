---
phase: 04-advanced-renaming-logic
plan: 02
type: execute
wave: 1
depends_on: [00]
files_modified:
  - src/Services/Renaming/SearchReplacePreviewService.cs
  - src/Views/SearchReplaceView.xaml
  - LECG.Tests/Services/SearchReplacePreviewServiceTests.cs
autonomous: true
requirements: [REQ-04]
must_haves:
  truths:
    - "SearchReplacePreviewService.ProcessPreview populates ElementRowViewModel.Status with the skip reason for FamilyParameter rows that hit a remaining skip condition (built-in / reporting / name-conflict)"
    - "ProcessPreview sets row.IsRenameable = false and row.IsChecked = false for skipped FamilyParameter rows"
    - "ProcessPreview populates Status with side-effect count (e.g. '+2 formulas, +1 dimension') for FamilyParameter rows that will go through the safe-rename path"
    - "Cross-batch name collisions are detected during ProcessPreview using a claimed-NewValue HashSet, flipping the second row to skip with reason"
    - "SearchReplaceView.xaml binds checkbox IsEnabled to row.IsRenameable"
    - "SearchReplaceView.xaml row Style trigger reduces opacity to ~0.45 and switches to muted foreground when IsRenameable == false"
    - "Status column cell has a tooltip bound to row.Status for full-text overflow"
  artifacts:
    - path: "src/Services/Renaming/SearchReplacePreviewService.cs"
      provides: "Status + IsRenameable population during preview, including side-effect count for safe-rename rows"
      contains: "IsRenameable"
    - path: "src/Views/SearchReplaceView.xaml"
      provides: "Muted-row Style trigger, disabled checkbox binding, Status tooltip"
      contains: "IsRenameable"
  key_links:
    - from: "SearchReplaceView.xaml Sel CheckBox"
      to: "row.IsRenameable"
      via: "IsEnabled binding"
      pattern: "IsEnabled=\"\\{Binding IsRenameable"
    - from: "SearchReplaceView.xaml row Style"
      to: "row.IsRenameable"
      via: "DataTrigger Value=False → Opacity 0.45"
      pattern: "DataTrigger Binding=\"\\{Binding IsRenameable\\}\" Value=\"False\""
---

<objective>
Surface skip reasons and side-effect counts in the Batch Rename grid. Bind the row checkbox + visual style to `IsRenameable` so skipped rows are visibly inert.

Purpose: Eliminate "Apply does nothing" confusion (CONTEXT.md). User sees the skip reason inline in the existing Status column; disabled checkbox prevents accidental ticks.

Output: ProcessPreview populates Status/IsRenameable; XAML disables and mutes skipped rows; tests for both behaviors GREEN.
</objective>

<execution_context>
@C:/Users/LECG Arquitectura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LECG Arquitectura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-advanced-renaming-logic/04-CONTEXT.md
@.planning/phases/04-advanced-renaming-logic/04-RESEARCH.md
@.planning/phases/04-advanced-renaming-logic/04-00-PLAN.md
@src/Services/Renaming/SearchReplacePreviewService.cs
@src/Views/SearchReplaceView.xaml
@src/ViewModels/Components/ElementRowViewModel.cs

<interfaces>
<!-- ElementRowViewModel after Plan 04-00 -->
public partial class ElementRowViewModel : ObservableObject
{
    public long Id { get; set; }
    public string Type { get; set; } = "";
    public string Category { get; set; } = "";
    public string Name { get; set; } = "";
    public string Status { get; set; } = "";
    public bool IsRenameable { get; set; } = true;   // Plan 04-00 addition
    [ObservableProperty] private bool isChecked;     // Observable
    // Batch-Rename-specific: Original, NewValue, ParamGroup, IsInstance, IsReadOnly (set in ProcessPreview)
}

<!-- ProcessPreview signature (confirmed in src/Services/Renaming/SearchReplacePreviewService.cs:110-118) -->
public List<ElementRowViewModel> ProcessPreview(/* search/replace context */)

<!-- LecgDataGrid Sel checkbox binding (Phase 03-05 baseline) -->
<DataGridTemplateColumn Header="">
    <DataGridTemplateColumn.CellTemplate>
        <DataTemplate>
            <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </DataTemplate>
    </DataGridTemplateColumn.CellTemplate>
</DataGridTemplateColumn>

<!-- Side-effect count format -->
"+N formulas, +M dimension labels"  // both nonzero
"+N formulas"                        // only formulas
"+M dimension labels"                // only dims
""                                   // neither (plain rename, no Status text)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire Status + IsRenameable + side-effect count in ProcessPreview</name>
  <files>src/Services/Renaming/SearchReplacePreviewService.cs, LECG.Tests/Services/SearchReplacePreviewServiceTests.cs</files>
  <behavior>
    For each row produced by ProcessPreview:
    1. If `GetRenameSkipReason(...)` returns a non-null reason → set `row.Status = reason`, `row.IsRenameable = false`, `row.IsChecked = false`.
    2. If the row is a safe-rename candidate (FamilyParameter with non-empty `formulaReferenced` or `dimensionLabels` membership for its name):
       - Compute formulaCount = count of params in family whose `Formula` contains a reference to `row.Original` (via `FormulaNameUpdater.ContainsReference`).
       - Compute dimensionCount = count of dimensions in family whose `FamilyLabel.Definition.Name == row.Original`.
       - Format Status per the side-effect format rules in `<interfaces>`.
       - Leave `IsRenameable = true`, `IsChecked` unchanged.
    3. If neither skip nor safe-rename applies → `Status = ""`, `IsRenameable = true`.
    4. Cross-batch name-collision pass: after building the row list, walk it; build a `HashSet<string>` of NewValue strings already claimed by earlier checked rows in the same family scope; the FIRST repeat collision flips the second occurrence to `Status = "name '{NewValue}' already claimed by another row in this batch"`, `IsRenameable = false`, `IsChecked = false`.
  </behavior>
  <action>
    Step A — Un-Skip the 5 REQ-04 tests added in Plan 04-00 to `SearchReplacePreviewServiceTests.cs`:
    - `ProcessPreview_FamilyParameterSkipReason_PopulatesStatus_WithReasonString`
    - `ProcessPreview_FamilyParameterSkipReason_SetsIsRenameableFalse_AndIsCheckedFalse`
    - `ProcessPreview_SafeRenameRow_PopulatesStatus_WithSideEffectCount`
    - `ProcessPreview_RenameableRow_LeavesIsRenameableTrue_AndKeepsUserCheckedState`
    - `ProcessPreview_CrossBatchNameCollision_FlipsSecondRowToSkip_WithReason`
    Reuse existing test fixture conventions (NSubstitute mocks, FluentAssertions). Where the existing tests mock `IFormulaUpdateService` / family enumeration, follow the same pattern.

    Step B — Modify `SearchReplacePreviewService.ProcessPreview` (`src/Services/Renaming/SearchReplacePreviewService.cs`, current row-construction block ~lines 110-118). Read the current method first so the addition is purely additive — do not break Phase 03-04's `Category`/`Id`/`Name` propagation contract.

    For FamilyParameter rows:
    - Compute the existing `dimensionLabels` and `formulaReferenced` HashSets (already done in `BatchRenameExecutionService`; this service may need a small helper or to receive them via parameter — match existing service collaboration; if separation is awkward, add a lightweight inline collector since BatchRenameExecutionService already has `BuildDimensionLabelNames` and `BuildFormulaReferencedNames` static methods — reuse them).
    - Call the narrowed `GetRenameSkipReason` (per Plan 04-01) — if non-null, populate Status/IsRenameable/IsChecked per the behavior contract.
    - Otherwise compute formulaCount / dimensionCount and populate Status with the side-effect format.

    For standard-item rows: leave Status empty here — the dry-run pass in `BatchRenameExecutionService` (Plan 04-01) is the authoritative skip-reason populator for standard items at execute time. ProcessPreview can still surface a "predicted" skip reason for cross-batch collisions and read-only types if the data is cheap to compute, but failure to detect every standard-item skip in preview is acceptable (dry-run will catch it).

    Cross-batch collision walk: after row construction completes, group rows by family scope, then within each group enforce the claimed-NewValue HashSet rule.

    Locale-safe: use ordinal `StringComparer.Ordinal` for the HashSet.

    Do NOT mutate `row.Category` (Phase 03-04 contract: pass-through only).

    Step C — Confirm the 150ms debounce in the preview pipeline (CONTEXT.md mentions it exists) still triggers ProcessPreview on every search/replace edit. Do not introduce caching keyed on input strings.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --filter "FullyQualifiedName~SearchReplacePreviewServiceTests" --nologo -v minimal</automated>
  </verify>
  <done>
    - All 5 REQ-04 RED tests in `SearchReplacePreviewServiceTests` flipped to GREEN
    - Pre-existing tests in the fixture still GREEN
    - Phase 03-04's Category pass-through still holds
    - Full suite GREEN
  </done>
</task>

<task type="auto">
  <name>Task 2: SearchReplaceView.xaml — IsEnabled binding + muted Style trigger + Status tooltip</name>
  <files>src/Views/SearchReplaceView.xaml</files>
  <action>
    Edit `src/Views/SearchReplaceView.xaml`. Three targeted changes:

    Change 1 — Sel checkbox `IsEnabled` binding:
    Find the DataGridTemplateColumn that holds the Sel checkbox (Phase 03-05 placed `Sel | Type | Category | Original | New` and Plan 04-00 retained `Status`). Add `IsEnabled="{Binding IsRenameable}"` to the CheckBox. Keep the existing `IsChecked` two-way binding intact.

    Change 2 — Muted-row Style trigger:
    Locate the LecgDataGrid `RowStyle` (or add one if absent — match the pattern used elsewhere in the project; check `ElementGridControl.xaml` for the muted-row precedent if a project-wide pattern exists). Add or extend the row Style:
    ```xml
    <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsRenameable}" Value="False">
                <Setter Property="Opacity" Value="0.45"/>
                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    ```
    Confirm `TextSecondaryBrush` exists in the app's resource dictionary by grepping `App.xaml` / theme files. If a different muted-text brush is used elsewhere (e.g. `TextMutedBrush`, `DisabledForegroundBrush`), use that one instead — match the existing project convention.

    Change 3 — Status tooltip on the Status cell:
    Find the Status DataGridTextColumn (or DataGridTemplateColumn) and add an ElementStyle that sets ToolTip to the bound Status text:
    ```xml
    <DataGridTextColumn.ElementStyle>
        <Style TargetType="TextBlock">
            <Setter Property="ToolTip" Value="{Binding Status}"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
    </DataGridTextColumn.ElementStyle>
    ```

    Do NOT alter column order, sort behaviors, or filter plumbing — Phase 03-05 locked those.

    Build verification (Revit may be open):
    `dotnet build LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true` — this transitively builds the main LECG project's XAML compilation. If XAML compilation errors surface (e.g., missing brush key), fall back to a hardcoded `#88888888` Brush color and log the resource gap in the SUMMARY for v1.2 follow-up.
  </action>
  <verify>
    <automated>dotnet build LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --nologo -v minimal</automated>
  </verify>
  <done>
    - `SearchReplaceView.xaml` compiles 0 errors / 0 warnings
    - Sel CheckBox has `IsEnabled="{Binding IsRenameable}"`
    - Row Style has `DataTrigger` on `IsRenameable == false` setting `Opacity` + muted `Foreground`
    - Status column TextBlock has ToolTip bound to Status
    - No change to column order or sort/filter behavior
    - Visual confirmation deferred to Plan 04-05 phase-end Revit verify
  </done>
</task>

</tasks>

<verification>
- `dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --nologo -v minimal` GREEN
- All REQ-04 preview tests flipped GREEN
- XAML build clean
- Bindings reference real properties (`IsRenameable`, `IsChecked`, `Status`) — no typos
</verification>

<success_criteria>
- ProcessPreview populates Status with skip reason OR side-effect count OR empty (never null or "{NULL}")
- IsRenameable correctly flips for skipped rows; IsChecked auto-clears for them
- XAML binds without runtime binding errors (verified visually in Plan 04-05)
- No regression in Phase 03 column-order or filter API
</success_criteria>

<output>
Create `.planning/phases/04-advanced-renaming-logic/04-02-SUMMARY.md` covering:
- ProcessPreview modifications (skip reason vs side-effect count branches)
- Cross-batch collision walk implementation
- XAML changes (3 spots) with the brush resource actually used
- Test count flipped RED → GREEN
- Any resource gap (e.g. brush fallback) noted for v1.2
</output>
