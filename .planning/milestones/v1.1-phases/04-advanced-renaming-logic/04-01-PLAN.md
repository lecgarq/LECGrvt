---
phase: 04-advanced-renaming-logic
plan: 01
type: execute
wave: 1
depends_on: [00]
files_modified:
  - src/Services/Renaming/BatchRenameExecutionService.cs
  - LECG.Tests/Services/RenameSkipDetectorTests.cs
  - LECG.Tests/Services/BatchRenameSafeRenameTests.cs
autonomous: true
requirements: [REQ-02, REQ-03, REQ-04]
must_haves:
  truths:
    - "GetRenameSkipReason no longer returns a skip for formula-referenced, dimension-label, or element-associated params"
    - "GetRenameSkipReason still returns a skip reason for built-in (Id.Value < 0), reporting, and name-conflict params"
    - "A new GetStandardItemSkipReason returns a reason for read-only types, name conflict in scope, system families (Family.IsSystemFamily == true), and sheet-number-locked rows; returns null for renameable rows"
    - "Pre-flight standard-item dry-run runs OUTSIDE the main rename transaction, mutates only ElementRowViewModel state (Status + IsRenameable + IsChecked), and emits one LogWarning per skipped row"
    - "Cross-batch name-collision detection uses a HashSet<string> of NewValue strings already claimed by other checked rows in the same family batch"
    - "All RenameSkipDetectorTests skip-gated RED tests for REQ-02/03/04 detection now run GREEN"
    - "REQ-04 dry-run tests in BatchRenameSafeRenameTests run GREEN"
  artifacts:
    - path: "src/Services/Renaming/BatchRenameExecutionService.cs"
      provides: "Narrowed GetRenameSkipReason + new GetStandardItemSkipReason + pre-flight dry-run for standard items"
      contains: "GetStandardItemSkipReason"
  key_links:
    - from: "BatchRenameExecutionService standard-item code path"
      to: "GetStandardItemSkipReason"
      via: "pre-flight loop BEFORE _transactionService.Run(...)"
      pattern: "GetStandardItemSkipReason"
    - from: "BatchRenameExecutionService skipped standard-item row"
      to: "LogView"
      via: "logger.LogWarning(\"Skipped '{name}' ({context}): {reason}\")"
      pattern: "Skipped.*\\("
---

<objective>
Narrow `GetRenameSkipReason` (drop three categories) and add `GetStandardItemSkipReason` + standard-item pre-flight dry-run. Address REQ-04's detection layer for standard items and prepare the rename pipeline for safe-rename wiring in Wave 2.

Purpose: Decouple "what should skip" from "what should rename" before plans 04-03/04-04 add the safe-rename mechanics. Flips REQ-04 detection-side RED tests to GREEN.

Output: Modified BatchRenameExecutionService, GREEN RenameSkipDetectorTests fixture, GREEN REQ-04 dry-run tests.
</objective>

<execution_context>
@C:/Users/LECG Arquitectura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LECG Arquitectura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-advanced-renaming-logic/04-CONTEXT.md
@.planning/phases/04-advanced-renaming-logic/04-RESEARCH.md
@.planning/phases/04-advanced-renaming-logic/04-00-PLAN.md
@src/Services/Renaming/BatchRenameExecutionService.cs

<interfaces>
<!-- Current GetRenameSkipReason signature (lines 334-376) -->
private static string? GetRenameSkipReason(
    FamilyParameter fp,
    string newName,
    FamilyManager mgr,
    HashSet<string> dimensionLabels,
    HashSet<string> formulaReferenced,
    HashSet<string> elementAssociated)

<!-- New method to add -->
internal static string? GetStandardItemSkipReason(
    Element el,
    string newValue,
    Document doc,
    HashSet<string> claimedNewNames /* cross-batch collision guard, Pitfall 5 */)

<!-- LogWarning convention (Phase 02.5 decision) -->
logger.LogWarning($"Skipped '{originalName}' ({context}): {reason}");
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Narrow GetRenameSkipReason — drop three categories</name>
  <files>src/Services/Renaming/BatchRenameExecutionService.cs, LECG.Tests/Services/RenameSkipDetectorTests.cs</files>
  <behavior>
    - `GetRenameSkipReason` returns `null` for a FamilyParameter that is formula-referenced (matches `formulaReferenced` set)
    - `GetRenameSkipReason` returns `null` for a FamilyParameter that is dimension-labeled (matches `dimensionLabels` set)
    - `GetRenameSkipReason` returns `null` for an element-associated FamilyParameter (matches `elementAssociated` set)
    - `GetRenameSkipReason` STILL returns a non-null reason string for `fp.Id.Value < 0` (built-in)
    - `GetRenameSkipReason` STILL returns a non-null reason for `fp.IsReporting`
    - `GetRenameSkipReason` STILL returns a non-null reason when `newName` already exists in `mgr.Parameters` (name conflict, no auto-suffix)
  </behavior>
  <action>
    Step A — Un-Skip and implement the 6 GetRenameSkipReason tests in `RenameSkipDetectorTests.cs` (created Wave 0). Use NSubstitute mocks for `FamilyManager`/`FamilyParameter` where needed; the method signature only depends on `FamilyParameter`, `string`, `FamilyManager`, and the three `HashSet<string>`s — mockable without a live Document.

    Step B — Modify `GetRenameSkipReason` in `BatchRenameExecutionService.cs` (current lines 334-376):
    - DELETE the branches that return reasons for `formulaReferenced.Contains(fp.Definition.Name)`, `dimensionLabels.Contains(fp.Definition.Name)`, and `elementAssociated.Contains(fp.Definition.Name)`.
    - KEEP the branches for built-in (`fp.Id.Value < 0`), reporting (`fp.IsReporting`), name conflict (existing param in `mgr.Parameters` with the same name as `newName`, ordinal comparison).
    - Reason strings stay as their existing text for the kept branches.
    - Update the XML doc comment to reflect the narrowed responsibility: "Returns the user-facing skip reason ONLY for the three remaining un-rename-able conditions: built-in, reporting, name-conflict. Formula-referenced, dimension-label, and element-associated params are renamed via the safe-rename path in plans 04-03/04-04."

    Step C — Call sites: search for `GetRenameSkipReason(` in the codebase. The signature stays the same (still takes the three HashSets) so callers don't change, but the callers in `RenameFamilyParameters` MUST continue to pass those HashSets — plans 04-03/04-04 consume them for the safe-rename loops. Add a TODO comment at each call site: `// TODO 04-03: hand formulaReferenced set to safe-rename loop; TODO 04-04: hand dimensionLabels set to dimension reassignment loop`.

    Do NOT touch the `count` increment pattern at line 185 (deferred to v1.2; see Pitfall 3).
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --filter "FullyQualifiedName~RenameSkipDetectorTests" --nologo -v minimal</automated>
  </verify>
  <done>
    - All 6 narrowing tests for `GetRenameSkipReason` GREEN (no longer Skipped)
    - The Wave 0 anchor "delete when plan 04-01 fills this fixture" removed (or DisplayName updated)
    - Full suite GREEN
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add GetStandardItemSkipReason + pre-flight dry-run loop</name>
  <files>src/Services/Renaming/BatchRenameExecutionService.cs, LECG.Tests/Services/RenameSkipDetectorTests.cs, LECG.Tests/Services/BatchRenameSafeRenameTests.cs</files>
  <behavior>
    - `GetStandardItemSkipReason(Element el, string newValue, Document doc, HashSet<string> claimedNewNames)` returns a non-null string for:
      a) Read-only element name (`el.Name` setter throws, or use `ElementType`/built-in check) — reason: `"read-only or built-in type"`
      b) Name conflict within scope (another element of same type+scope already named `newValue`) — reason: `"name '{newValue}' already in use in scope"`
      c) `Family.IsSystemFamily == true` for elements that resolve to a system family — reason: `"system family — names are restricted"`
      d) Sheets where `newValue` violates numbering scheme (caught via probe or numbering-scheme metadata) — reason: `"sheet number locked or restricted by numbering scheme"`
      e) Cross-batch collision: `claimedNewNames` already contains `newValue` (Pitfall 5) — reason: `"name '{newValue}' already claimed by another row in this batch"`
    - Returns `null` for a freely-renameable row.
    - Pre-flight loop: for each `IsChecked` standard-item row, calls `GetStandardItemSkipReason`; if non-null, sets `row.Status = reason`, `row.IsRenameable = false`, `row.IsChecked = false`, and logs `LogWarning($"Skipped '{row.Original}' ({row.Type}): {reason}")`. After processing each row, if it's renameable, adds `row.NewValue` to `claimedNewNames`.
    - Loop runs BEFORE `_transactionService.Run(...)` — read-only walk, no transaction.
  </behavior>
  <action>
    Step A — Un-Skip the 5 `GetStandardItemSkipReason_*` tests in `RenameSkipDetectorTests.cs` and the 2 REQ-04 dry-run tests in `BatchRenameSafeRenameTests.cs` (`RenameStandardItems_PreFlightDryRun_SetsStatusAndUnchecks_OnSkipCondition`, `RenameStandardItems_PreFlightDryRun_LogsOneWarningPerSkippedRow`). Use NSubstitute for `Element`, `Document`, `ILogger`/`ILogView` (whichever the existing service uses — confirm by grepping `BatchRenameExecutionService` for the logger field).

    Step B — Add `internal static string? GetStandardItemSkipReason(Element el, string newValue, Document doc, HashSet<string> claimedNewNames)` to `BatchRenameExecutionService.cs` adjacent to `GetRenameSkipReason`. Implementation:
    - Check `claimedNewNames.Contains(newValue)` first → return cross-batch reason
    - Use `el is FamilySymbol fs && fs.Family?.IsSystemFamily == true` → return system-family reason
    - Use `FilteredElementCollector(doc).OfClass(el.GetType()).Cast<Element>().Any(e => e.Id != el.Id && e.Name == newValue)` (ordinal compare via default) → return name-conflict reason
    - For sheets: check `el is ViewSheet vs && vs.GetSheetNumberingScheme()`... if the API isn't directly available, attempt `vs.SheetNumber = newValue` inside a probe SubTransaction wrapped in try/catch — on `Autodesk.Revit.Exceptions.InvalidOperationException` or `ArgumentException`, return the sheet reason. Always roll back the probe (never commit).
    - Default check: try to set `el.Name = newValue` inside a probe SubTransaction; on `InvalidOperationException` from a read-only Name property, return the read-only reason. Always roll back.
    - Locale-safe: use `BuiltInParameter`/`LabelUtils` rather than English string comparisons (Phase 02.5 decision).

    Step C — Add the pre-flight dry-run loop in `BatchRenameExecutionService`. Locate the entry point for the standard-item rename path (search for where `_transactionService.Run` is invoked with a non-FamilyParameter scope). Insert BEFORE that call:
    ```csharp
    var claimedNewNames = new HashSet<string>(StringComparer.Ordinal);
    foreach (var row in standardItems.Where(r => r.IsChecked))
    {
        var el = doc.GetElement(new ElementId((int)row.Id));  // or long, matching ElementRowViewModel.Id
        if (el == null) continue;
        string? reason = GetStandardItemSkipReason(el, row.NewValue, doc, claimedNewNames);
        if (reason != null)
        {
            row.Status = reason;
            row.IsRenameable = false;
            row.IsChecked = false;
            _logger.LogWarning($"Skipped '{row.Original}' ({row.Type}): {reason}");
        }
        else
        {
            claimedNewNames.Add(row.NewValue);
        }
    }
    ```
    Confirm exact property names (`Original`, `NewValue`, `Type`, `Id`) by reading `ElementRowViewModel.cs` and `SearchReplacePreviewService.cs` — adjust if Phase 03-04 used different names.

    Probe-SubTransaction guard (Pitfall 4): wrap every probe in try/finally to call `RollbackIfNotClosed`. Never commit a probe.

    Do NOT modify the FamilyParameter rename path in this plan — plan 04-03 owns that wiring.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --filter "FullyQualifiedName~RenameSkipDetectorTests|FullyQualifiedName~BatchRenameSafeRenameTests" --nologo -v minimal</automated>
  </verify>
  <done>
    - All 5 `GetStandardItemSkipReason_*` tests GREEN
    - Both REQ-04 dry-run tests in `BatchRenameSafeRenameTests` GREEN
    - Full suite GREEN
    - All probe SubTransactions roll back (no commits) — verified by code review
  </done>
</task>

</tasks>

<verification>
- `dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --nologo -v minimal` is GREEN
- `GetRenameSkipReason` is the narrowed version (three branches removed)
- `GetStandardItemSkipReason` exists and is internally visible to tests
- Pre-flight dry-run loop sits OUTSIDE `_transactionService.Run`
- No probe SubTransaction commits
- One LogWarning emitted per skipped standard-item row
</verification>

<success_criteria>
- All RED tests in `RenameSkipDetectorTests` flipped to GREEN
- 2 REQ-04 dry-run tests in `BatchRenameSafeRenameTests` flipped to GREEN
- No regression in pre-existing tests
- Skip-reason text strings are user-readable (no stack-trace fragments, no English-only Revit API labels)
</success_criteria>

<output>
Create `.planning/phases/04-advanced-renaming-logic/04-01-SUMMARY.md` covering:
- Narrowed branches removed from `GetRenameSkipReason`
- `GetStandardItemSkipReason` signature + detection branches
- Pre-flight dry-run loop location and behavior
- Logging format used
- Test count flipped RED → GREEN
</output>
