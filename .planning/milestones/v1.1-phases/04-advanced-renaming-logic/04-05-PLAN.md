---
phase: 04-advanced-renaming-logic
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/04-advanced-renaming-logic/04-VERIFICATION.md
autonomous: false
requirements: [REQ-02, REQ-03, REQ-04]
must_haves:
  truths:
    - "Manual Revit verification session executed against a real family-rich test model"
    - "REQ-02 verified live: renaming a formula-referenced parameter renames it + updates every referencing formula; family reloads without errors; computed values still resolve"
    - "REQ-03 verified live: renaming a dimension-labelled parameter renames it + reassigns every matching Dimension's FamilyLabel; dimension still drives the geometry correctly on reload"
    - "REQ-04 verified live: skipped rows render muted with disabled checkbox; Status column shows the reason; tooltip displays full text; LogView shows one LogWarning per skipped row"
    - "Each verification item is recorded as PASS/FAIL in 04-VERIFICATION.md"
    - "REQ-02, REQ-03, REQ-04 statuses flipped to Complete in REQUIREMENTS.md once verification PASSes"
  artifacts:
    - path: ".planning/phases/04-advanced-renaming-logic/04-VERIFICATION.md"
      provides: "Phase-end Revit verification checklist with PASS/FAIL per item + screenshots/notes"
      contains: "REQ-02"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Status table — REQ-02/03/04 flipped to Complete on PASS"
      contains: "Complete"
  key_links:
    - from: "04-VERIFICATION.md"
      to: "REQUIREMENTS.md status table"
      via: "manual flip of REQ-02/03/04 from Pending to Complete after PASS sign-off"
      pattern: "REQ-0[234].*Complete"
---

<objective>
Phase-end manual Revit verification session. Drive every REQ-02/03/04 behavior against a real family and confirm the live Revit API behaves as the unit tests projected. On full PASS, close Phase 4.

Purpose: Unit tests cover service-layer logic; Revit's actual EditFamily/LoadFamily/Dimension.FamilyLabel/FamilyManager.SetFormula behavior can only be confirmed in-process. This is the gate before REQ-02/03/04 flip to Complete.

Output: 04-VERIFICATION.md with PASS/FAIL per checklist item; REQUIREMENTS.md updated on PASS; STATE.md updated to reflect Phase 4 closure.
</objective>

<execution_context>
@C:/Users/LECG Arquitectura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LECG Arquitectura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-advanced-renaming-logic/04-CONTEXT.md
@.planning/phases/04-advanced-renaming-logic/04-RESEARCH.md
@.planning/phases/04-advanced-renaming-logic/04-00-PLAN.md
@.planning/phases/04-advanced-renaming-logic/04-01-PLAN.md
@.planning/phases/04-advanced-renaming-logic/04-02-PLAN.md
@.planning/phases/04-advanced-renaming-logic/04-03-PLAN.md
@.planning/phases/04-advanced-renaming-logic/04-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 04-VERIFICATION.md checklist + build/install for verification session</name>
  <files>.planning/phases/04-advanced-renaming-logic/04-VERIFICATION.md</files>
  <action>
    Step A — Create `04-VERIFICATION.md` with the following checklist sections. Use the same shape as `.planning/phases/03-grid-collection-fixes/*-VERIFICATION.md` (Phase 03 precedent). Status column for each item: ⬜ pending / ✅ PASS / ❌ FAIL / 🟡 N-A.

    Section A — REQ-02 (Formula Safe Rename), against a wall-hosted family with ≥2 parameters where param `A` is referenced in two formulas (one in `Width = A * 2`, one in `Visibility = A > 100`):
    A1. Tick row for param `A` in Batch Rename, set NewValue `A_renamed`, hit Apply
    A2. After Apply, open the family in Revit; confirm parameter exists as `A_renamed`
    A3. Confirm `Width` formula now reads `A_renamed * 2`
    A4. Confirm `Visibility` formula now reads `A_renamed > 100`
    A5. Confirm family loads back into the project without errors
    A6. Confirm LogView line `Renamed 'A' to 'A_renamed' (updated 2 formulas)`

    Section B — REQ-03 (Dimension-Label Safe Rename), against a family with one parameter `L` driving two Dimension labels:
    B1. Tick row for param `L`, set NewValue `L_renamed`, hit Apply
    B2. Open the family; confirm parameter is `L_renamed`
    B3. Confirm both Dimensions now show their FamilyLabel as `L_renamed` (visually in the family editor — open dimension properties, check Label dropdown)
    B4. Confirm dimensions still drive geometry on the family preview (e.g. change a type's `L_renamed` value, geometry updates)
    B5. Confirm family reloads into project; existing instances still hold their values
    B6. Confirm LogView line includes `(updated 0 formulas, 2 dimension labels)` or the dim-only variant

    Section C — REQ-03 edge — Open Question 1 (Dimension.FamilyLabel setter behavior):
    C1. If any FamilyLabel reassignment threw a Revit exception, note it and the rolled-back state in the checklist. If the param ended up renamed but dimension unaffected (rollback), confirm Revit state matches expectation. If the setter required null-clear-first to succeed, log this as a discovered constraint and OPEN A FOLLOW-UP TASK in STATE.md "Next Steps" to add a null-clear step in 04-04 before reassignment.

    Section D — REQ-04 (Skip Reason UI/Logs):
    D1. Set up a family with: one built-in param (e.g. `Id < 0` like `Type Mark`), one reporting param, one renameable param. Open Batch Rename.
    D2. Confirm built-in row: muted opacity, checkbox disabled, Status = "built-in parameter" (or whatever exact text Plan 04-01 emits), tooltip on hover reveals the full Status string
    D3. Confirm reporting row: muted, disabled, Status = "reporting parameter"
    D4. Set NewValue on the renameable row to a name already in use → Status flips to `name '{X}' already in use`, IsChecked auto-clears, row mutes
    D5. Use cross-batch collision: set rows A→C and B→C → first row Status empty, second row Status = `name 'C' already claimed by another row in this batch`, IsChecked clears on second row, both muted-respectively
    D6. Standard-item scope: pick a Wall type (system family), set new name → muted, disabled, Status = system-family reason
    D7. LogView confirms one LogWarning per skipped row with `Skipped '{name}' ({context}): {reason}` format
    D8. Side-effect count: pick a formula-referenced row with safe-rename path → Status shows `+N formulas` (or composite) BEFORE clicking Apply (preview-time)

    Section E — Regression:
    E1. Phase 02 FormulaAutoGrouping still works (open a family with formula param, run FormulaAutoGrouping, confirm move to "Other" group succeeds)
    E2. Phase 03 grid behaviors intact: column order Sel | Type | Category | Original | New | Status, default Category-asc sort, FilterCategory dropdown, no blank rows
    E3. Phase 02.5 ConvertFamily host preservation still works on a wall-hosted family
    E4. Build/Deploy: `dotnet build` deploys to %APPDATA%\Autodesk\Revit\Addins\<version>\ correctly (no `-p:SkipRevitDeploy=true` for this run — full install)

    Step B — Build & install for verification:
    - Close Revit if open
    - Run `dotnet build LECG.sln` (full deploy, no skip flag)
    - Confirm 0 errors / 0 warnings
    - Launch Revit, load a test project, execute verification sections A→E in order

    Step C — Record outcomes in 04-VERIFICATION.md as session progresses. Attach screenshots in `.planning/phases/04-advanced-renaming-logic/screenshots/` if any visual evidence helps disambiguate a FAIL.

    All sections must reach PASS before Task 2.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-advanced-renaming-logic/04-VERIFICATION.md && grep -q "REQ-02" .planning/phases/04-advanced-renaming-logic/04-VERIFICATION.md && grep -q "REQ-03" .planning/phases/04-advanced-renaming-logic/04-VERIFICATION.md && grep -q "REQ-04" .planning/phases/04-advanced-renaming-logic/04-VERIFICATION.md && echo OK</automated>
  </verify>
  <done>
    - 04-VERIFICATION.md exists with all 5 sections (A–E) and ≥22 checklist items
    - Build clean for full install
    - File committed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute manual Revit verification session</name>
  <what-built>
    REQ-02 (formula safe rename), REQ-03 (dimension-label safe rename), REQ-04 (skip-reason UI + log surfacing + side-effect count + cross-batch collision + standard-item skip detection) all wired across plans 04-00..04-04.
  </what-built>
  <how-to-verify>
    1. Walk through every checklist item in `.planning/phases/04-advanced-renaming-logic/04-VERIFICATION.md` Sections A–E.
    2. Mark each item ✅ PASS or ❌ FAIL with a one-line note.
    3. For any FAIL: do NOT proceed; report back to gsd-orchestrator with the failing items so a gap-closure planning round can address them.
    4. If C1 reveals a Dimension.FamilyLabel null-clear requirement, mark C1 as 🟡 N-A + log a follow-up task; this is not a Phase 4 blocker if reassignment succeeded for the test cases.
    5. Once ALL items are ✅ or properly dispositioned, type `approved 04 phase` to resume.
  </how-to-verify>
  <resume-signal>Type "approved 04 phase" or describe failing items (FAIL list with item IDs).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Flip REQ-02/03/04 to Complete + close Phase 4 in STATE/ROADMAP</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    Only proceed if Task 2 returned APPROVED.

    Step A — `.planning/REQUIREMENTS.md`:
    - REQ-02 row: Status `Pending` → `Complete (Phase 4 Plans 04-00..04-05; manual Revit acceptance {date} — formula reference rewriting verified live on N-formula family)`
    - REQ-03 row: Status `Pending` → `Complete (Phase 4 Plans 04-00..04-05; manual Revit acceptance {date} — dimension FamilyLabel reassignment verified live)`
    - REQ-04 row: Status `Pending` → `Complete (Phase 4 Plans 04-00..04-05; manual Revit acceptance {date} — skip reasons + side-effect counts surface in Status, muted rows + disabled checkboxes confirmed)`

    Step B — `.planning/STATE.md`:
    - `progress.completed_phases` += 1
    - `Current Position` updated to phase 4 COMPLETE
    - Add a `## Phase 4 Progress` section summarizing all 6 plans (00..05) with one-line outcomes
    - Append to `## Decisions` any per-plan decisions captured in the 5 SUMMARY files (read them; merge unique items prefixed `[Phase 04]:`)
    - `## Next Steps`: remove "Phase 4 planning"; promote "Phase 5 (Verification & Polish — REQ-05) planning" to position 1
    - Append "v1.2 follow-up: investigate Dimension.FamilyLabel null-clear requirement if Section C1 flagged it"

    Step C — `.planning/ROADMAP.md`:
    - Phase 4 status: `Not Started` → `Complete (6/6 plans complete; REQ-02/03/04 accepted {date})`
    - Add a Plans list under Phase 4 (mirror Phase 3's format) with all 6 plan files marked `[x]`

    Step D — Commit all three files in a single commit.
  </action>
  <verify>
    <automated>grep -q "REQ-02.*Complete" .planning/REQUIREMENTS.md && grep -q "REQ-03.*Complete" .planning/REQUIREMENTS.md && grep -q "REQ-04.*Complete" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <done>
    - REQUIREMENTS.md REQ-02/03/04 all show Complete
    - STATE.md reflects Phase 4 closure
    - ROADMAP.md Phase 4 marked Complete with 6/6 plans listed
    - Single commit lands all three files
  </done>
</task>

</tasks>

<verification>
- 04-VERIFICATION.md exists with full session log
- All checklist items resolved (PASS / FAIL / N-A with rationale)
- REQUIREMENTS.md / STATE.md / ROADMAP.md updated only if APPROVED
</verification>

<success_criteria>
- Live Revit confirms REQ-02 formula rewrite, REQ-03 dimension reassignment, REQ-04 UI + log surfacing
- No Phase 02/02.5/03 regression
- Build deploys clean without `-p:SkipRevitDeploy=true`
- Phase 4 closed; v1.1 milestone only has REQ-05 left (Phase 5)
</success_criteria>

<output>
Create `.planning/phases/04-advanced-renaming-logic/04-05-SUMMARY.md` covering:
- Verification session outcomes (PASS/FAIL counts per section)
- Any deferred follow-ups added to STATE.md Next Steps
- Dimension.FamilyLabel setter behavior actually observed (resolves Open Question 1)
- v1.1 milestone status after Phase 4 (REQ-05 remaining)
</output>
