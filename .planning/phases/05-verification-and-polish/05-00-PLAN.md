---
phase: 05-verification-and-polish
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - LECG.Tests/Services/RenameRulePipelineServiceTests.cs
  - LECG.Tests/Services/SearchReplaceServiceTests.cs
  - LECG.Tests/Services/BatchRenameExecutionServiceTests.cs
  - LECG.Tests/Services/BaseElementCollectionServiceTests.cs
  - .planning/phases/05-verification-and-polish/05-VERIFICATION.md
  - .planning/phases/05-verification-and-polish/05-VALIDATION.md
autonomous: true
requirements:
  - REQ-05
must_haves:
  truths:
    - "New fixture files exist and are discovered by `dotnet test`"
    - "Each new fixture has exactly one non-skipped anchor `[Fact]` so the runner enumerates it"
    - "All behavioural tests are `[Fact(Skip = \"Implemented by plan 05-XX\")]` with reason strings naming the implementing plan ID"
    - "Existing `BaseElementCollectionServiceTests` 4 GREEN tests remain GREEN; deepening rows appended as skip-gated RED naming plan 05-01"
    - "`05-VERIFICATION.md` exists with the service→fixture coverage matrix scaffold rows (one per service)"
    - "`05-VALIDATION.md` frontmatter flips `nyquist_compliant: true`, `wave_0_complete: true`"
    - "Full xUnit suite runs GREEN with 0 failures (skip-gated tests count as Skipped, not Failed)"
  artifacts:
    - path: "LECG.Tests/Services/RenameRulePipelineServiceTests.cs"
      provides: "Wave 1 RED anchor for RenameRulePipelineService coverage"
      min_lines: 30
      contains: "Fixture_Anchor_Exists"
    - path: "LECG.Tests/Services/SearchReplaceServiceTests.cs"
      provides: "Wave 2 RED anchor for SearchReplaceService facade coverage"
      min_lines: 30
      contains: "Fixture_Anchor_Exists"
    - path: "LECG.Tests/Services/BatchRenameExecutionServiceTests.cs"
      provides: "Wave 2 + Wave 3 RED anchor for direct BatchRenameExecutionService coverage and the 3 polish fixes"
      min_lines: 50
      contains: "Fixture_Anchor_Exists"
    - path: "LECG.Tests/Services/BaseElementCollectionServiceTests.cs"
      provides: "Existing fixture appended with Wave 1 deepening RED rows"
      contains: "Implemented by plan 05-01"
    - path: ".planning/phases/05-verification-and-polish/05-VERIFICATION.md"
      provides: "Service→fixture coverage matrix scaffold (6 rows)"
      min_lines: 30
      contains: "Service→Fixture Coverage Matrix"
  key_links:
    - from: "LECG.Tests/Services/BatchRenameExecutionServiceTests.cs"
      to: "src/Services/Renaming/BatchRenameExecutionService.cs"
      via: "InternalsVisibleTo + internal static helper access"
      pattern: "BatchRenameExecutionService\\.(EvaluateFamilyParamSkipReason|EvaluateStandardItemSkipReason|FormatSafeRenameLog|GroupCheckedFamilyParameterItemsForTest|AccumulateCommittedFamilyCount|BuildProgressSequence|ExecuteDimensionReassignments)"
    - from: ".planning/phases/05-verification-and-polish/05-VALIDATION.md"
      to: "all Wave 0/1/2/3 fixtures"
      via: "Per-task verification map task IDs (5-00-XX, 5-01-XX, ...)"
      pattern: "5-0\\d-0\\d"
---

<objective>
Wave 0 scaffolds: create the three new xUnit fixtures, deepen the existing collection fixture with skip-gated RED rows naming Wave 1, scaffold `05-VERIFICATION.md` with the service→fixture coverage matrix, and flip `05-VALIDATION.md` to `wave_0_complete: true` / `nyquist_compliant: true`. Suite must stay GREEN — every new behavioural test is skip-gated naming its implementing plan; each new fixture has exactly one non-skipped anchor for discovery.

Purpose: Provide TDD anchors for Waves 1–3. Lock the matrix structure that becomes the REQ-05 acceptance artifact. No production code touched this wave.

Output: 4 fixture files (3 new + 1 extended), 05-VERIFICATION.md matrix scaffold, 05-VALIDATION.md frontmatter flip.
</objective>

<execution_context>
@C:/Users/LECG Arquitectura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LECG Arquitectura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-verification-and-polish/05-CONTEXT.md
@.planning/phases/05-verification-and-polish/05-RESEARCH.md
@.planning/phases/05-verification-and-polish/05-VALIDATION.md
@LECG.Tests/Services/BatchRenameSafeRenameTests.cs
@LECG.Tests/Services/FormulaUpdateServiceTests.cs
@LECG.Tests/Services/BaseElementCollectionServiceTests.cs
@LECG.Tests/Services/SearchReplacePreviewServiceTests.cs
@src/InternalsVisibleTo.Tests.cs

<interfaces>
<!-- Skip-gated RED + anchor pattern (Phase 03-00 / 04-00 precedent) -->

From LECG.Tests/Services/BatchRenameSafeRenameTests.cs (anchor pattern):
```csharp
[Fact(DisplayName = "anchor — fixture exists; behavioural tests skip-gated by implementing plan")]
public void Fixture_Anchor_Exists()
{
    Assert.NotNull(typeof(BatchRenameExecutionService));
}

[Fact(Skip = "Implemented by plan 04-03: REQ-02 formula-update wiring")]
public void RenameFamilyParameters_FormulaReferenced_UpdatesAllReferencingFormulas() { /* ... */ }
```

From src/InternalsVisibleTo.Tests.cs:
```csharp
[assembly: InternalsVisibleTo("LECG.Tests")]
```
Internal static helpers in src/Services/Renaming/ are visible to the LECG.Tests project.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three new fixture files with anchor + skip-gated RED rows</name>
  <files>LECG.Tests/Services/RenameRulePipelineServiceTests.cs, LECG.Tests/Services/SearchReplaceServiceTests.cs, LECG.Tests/Services/BatchRenameExecutionServiceTests.cs</files>
  <action>
Create three new xUnit fixture files under `LECG.Tests/Services/`. Each file MUST:

1. Have a `namespace LECG.Tests.Services` declaration matching neighbours.
2. Contain exactly ONE non-skipped `[Fact(DisplayName = "anchor — ...")]` named `Fixture_Anchor_Exists` that does `Assert.NotNull(typeof({TargetService}));` so `dotnet test --filter` discovers the fixture (Phase 03-00 decision).
3. Contain skip-gated RED rows using `[Fact(Skip = "Implemented by plan 05-XX: <one-line behavior>")]` per the list below.

**RenameRulePipelineServiceTests.cs** — target service `RenameRulePipelineService`. Skip-gated RED rows naming plan 05-01:
- `ApplyRules_ComposesRulesInOrder_RemoveThenReplaceThenCaseThenAddThenNumbering`
- `ApplyRules_PassesIndexArgumentToEveryRule`
- `ApplyRules_NullContext_Throws`
- `ApplyRules_InactiveRules_PassesThroughUnchanged`
- `Constructor_NullRuleList_Throws`

**SearchReplaceServiceTests.cs** — target service `SearchReplaceService`. Skip-gated RED rows naming plan 05-02:
- `CollectBaseElements_DelegatesToBaseElementCollectionService`
- `GetUniqueCategories_DelegatesToCollectionService_AndReturnsItsResult`
- `ProcessPreview_DelegatesToPreviewService_AndPassesCancellationToken`
- `ExecuteBatchRename_ProgressReporterOverload_DelegatesToExecutionService`
- `ExecuteBatchRename_ActionCallbackOverload_DelegatesToExecutionService`
- `Constructor_NullDependency_Throws` (one per ctor arg, 3 ctor args → 3 tests OK to fold into a Theory)

**BatchRenameExecutionServiceTests.cs** — target service `BatchRenameExecutionService`. Skip-gated RED rows naming plan 05-02 (direct-coverage tests):
- `EvaluateFamilyParamSkipReason_BuiltInParam_ReturnsBuiltInReason`
- `EvaluateFamilyParamSkipReason_ReportingParam_ReturnsReportingReason`
- `EvaluateFamilyParamSkipReason_NameConflict_ReturnsConflictReason`
- `EvaluateFamilyParamSkipReason_SafeToRename_ReturnsNull`
- `EvaluateStandardItemSkipReason_CrossBatchCollision_ReturnsCollisionReason`
- `EvaluateStandardItemSkipReason_SystemFamily_ReturnsSystemFamilyReason`
- `EvaluateStandardItemSkipReason_NameInScope_ReturnsCollisionReason`
- `EvaluateStandardItemSkipReason_SheetLocked_ReturnsLockedReason`
- `EvaluateStandardItemSkipReason_ReadOnly_ReturnsReadOnlyReason`
- `EvaluateStandardItemSkipReason_FreelyRenameable_ReturnsNull`
- `FormatSafeRenameLog_BothCountsPositive_RendersCompositeMessage`
- `FormatSafeRenameLog_FormulasOnly_RendersFormulaSuffix`
- `FormatSafeRenameLog_DimsOnly_RendersDimSuffix`
- `FormatSafeRenameLog_NeitherSideEffect_RendersBaseMessage`
- `GroupCheckedFamilyParameterItems_FiltersUnchecked_PreservesOrder`
- `GroupCheckedFamilyParameterItems_EmptyInput_ReturnsEmpty`
- `LegacyProgressReporter_NullCallback_AllFiveMethods_NoThrow`
- `Constructor_NullFormulaUpdateService_Throws`

Plus skip-gated RED rows naming plan 05-03 (the 3 polish fixes):
- `AccumulateCommittedFamilyCount_Committed_AddsRenamedToCurrentCount` (Skip = "Implemented by plan 05-03: Polish #1 count-on-rollback fix")
- `AccumulateCommittedFamilyCount_NotCommitted_ReturnsCurrentCountUnchanged` (same Skip reason)
- `ExecuteDimensionReassignments_PreClearsLabel_BeforeReassigning` (Skip = "Implemented by plan 05-03: Polish #2 Dimension.FamilyLabel null-clear")
- `BatchRenameProgress_MixedBatch_MaxBeforeDoneEquals100` (Skip = "Implemented by plan 05-03: Polish #3 standard-item progress capping")

DO NOT add `using` statements that depend on types not yet present (e.g., do not import `AccumulateCommittedFamilyCount` — it doesn't exist yet; the test body in a skip-gated fact may use `Assert.True(false, "see plan 05-03")` placeholder so the file compiles).

Reason: anchor + skip-gated RED is the established pattern (Phase 03-00 / 04-00). Suite stays GREEN; plan 05-01/02/03 authors flip Skip to active and write the body.

Build command while Revit is open: `dotnet build LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true`.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --filter "FullyQualifiedName~RenameRulePipelineServiceTests|FullyQualifiedName~SearchReplaceServiceTests|FullyQualifiedName~BatchRenameExecutionServiceTests"</automated>
  </verify>
  <done>Three new fixture files exist under LECG.Tests/Services/. Each enumerates via `dotnet test --filter` (anchor discoverable). All behavioural tests are Skipped. Zero Failed. Build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Append Wave 1 deepening RED rows to existing BaseElementCollectionServiceTests</name>
  <files>LECG.Tests/Services/BaseElementCollectionServiceTests.cs</files>
  <action>
Open `LECG.Tests/Services/BaseElementCollectionServiceTests.cs`. The existing 4 GREEN tests remain unchanged and serve as the anchor.

Append the following skip-gated RED rows naming plan 05-01:

- `TryGetGroupLabel_GetIdSucceeds_GetLabelReturnsString_ReturnsLabel` — Skip = "Implemented by plan 05-01: ParamGroup label resolution helper"
- `TryGetGroupLabel_GetIdThrows_ReturnsEmptyString` — same Skip
- `TryGetGroupLabel_GetLabelThrows_ReturnsEmptyString` — same Skip
- `DispatchScopeFlags_TypesOnly_ReturnsTypesMask` — Skip = "Implemented by plan 05-01: scope dispatch helper"
- `DispatchScopeFlags_FamiliesOnly_ReturnsFamiliesMask` — same Skip
- `DispatchScopeFlags_MaterialsOnly_ReturnsMaterialsMask` — same Skip
- `DispatchScopeFlags_ParametersOnly_ReturnsParametersMask` — same Skip
- `DispatchScopeFlags_MultipleScopes_ReturnsCombinedMask` — same Skip
- `MergeParamScanResults_DeduplicatesByFamilyIdAndParamName` — Skip = "Implemented by plan 05-01: Phase A/B param scan merge helper"
- `MergeParamScanResults_EmptyInputs_ReturnsEmpty` — same Skip

Also append one skip-gated RED row for the Revit-iteration branches that CANNOT be unit-tested:
- `CollectByScope_TypesPath_ProducesRowPerType` — Skip = "Revit FilteredElementCollector path — covered by manual Revit verification in 05-VERIFICATION.md"
- `CollectByScope_GraphicsStyleNullCategory_ProducesFallbackRow` — Skip = "Revit GraphicsStyle path — covered by manual Revit verification in 05-VERIFICATION.md"

Test bodies can be `Assert.True(false, "see plan 05-01")` placeholders — they are Skipped so they never run.

Existing 4 GREEN tests serve as the anchor — do NOT add a separate `Fixture_Anchor_Exists` here (the fixture already enumerates via the GREEN tests).

Reason for skip-gating Revit-iteration branches: per RESEARCH.md §Pitfall 6 and Don't-Hand-Roll table, Revit Document harness is not built; observable coverage via SSoT helpers + manual checklist.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --filter "FullyQualifiedName~BaseElementCollectionServiceTests"</automated>
  </verify>
  <done>Existing 4 tests GREEN. 12 new skip-gated tests Skipped. Zero Failed. File compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold 05-VERIFICATION.md matrix and flip 05-VALIDATION.md frontmatter</name>
  <files>.planning/phases/05-verification-and-polish/05-VERIFICATION.md, .planning/phases/05-verification-and-polish/05-VALIDATION.md</files>
  <action>
**Create `05-VERIFICATION.md`** with the following structure (frontmatter + sections):

```markdown
---
phase: 05-verification-and-polish
status: scaffold
created: 2026-05-10
---

# Phase 5 — Verification

## Service → Fixture Coverage Matrix

| # | Service | Fixture | Scenarios | Status |
|---|---------|---------|-----------|--------|
| 1 | FormulaUpdateService | FormulaUpdateServiceTests | delegation × 3; empty no-throw; ctor injection | ✅ |
| 2 | RenameRulePipelineService | RenameRulePipelineServiceTests | rule order; index propagation; null-throw guards; inactive-rules pass-through | ❌ → Wave 1 (05-01) |
| 3 | SearchReplaceService | SearchReplaceServiceTests | 4-method delegation; ctor null-guards | ❌ → Wave 2 (05-02) |
| 4 | SearchReplacePreviewService | SearchReplacePreviewServiceTests | scope/name filter; status; side-effect count; collision (8 tests) | ✅ |
| 5 | BaseElementCollectionService | BaseElementCollectionServiceTests | no-blanks invariant (4 GREEN); TryGetGroupLabel; DispatchScopeFlags; MergeParamScanResults; Revit branches skip-gated | ⚠ → Wave 1 (05-01) |
| 6 | BatchRenameExecutionService | BatchRenameExecutionServiceTests (new) + BatchRenameSafeRenameTests (existing) | 4-branch EvaluateFamilyParamSkipReason; 6-branch EvaluateStandardItemSkipReason; FormatSafeRenameLog 4-branch; GroupCheckedFamilyParameterItems; LegacyProgressReporter null-callback; ctor null-guards; + 3 polish helpers | ❌ → Wave 2 (05-02) + Wave 3 (05-03) |

Acceptance: every row ✅ AND full suite 100% GREEN.

## Manual Revit Verification Checklist (Wave 4 — 05-04)

Status legend: ⬜ pending · ✅ pass · ❌ fail · 🚫 N/A

### Polish #1 — count-on-rollback (`BatchRenameExecutionService.cs:222`)
- [ ] Force a family-edit rollback path (e.g., trigger concurrent edit / refuse-commit scenario)
- [ ] Confirm reported success count = 0 in completion message and LogView

### Polish #2 — Dimension.FamilyLabel null-clear
- [ ] Choose a family with a dimension whose `FamilyLabel.Definition.Name` already binds to a parameter
- [ ] Rename that parameter via Batch Rename → Family Parameters
- [ ] Confirm the dimension reassignment commits cleanly (no "cannot overwrite" warning from Revit)

### Polish #3 — Standard-item progress capping
- [ ] Run a mixed batch: 5+ Sheets or Materials + 3+ family parameters across ≥ 2 families
- [ ] Watch the progress bar — confirm it reaches 100% (monotonic 0→100, no mid-flight cap below 100)

## Sign-off (Wave 4)

- [ ] Full xUnit suite GREEN (target ≥ ~165 tests)
- [ ] Matrix above: all rows ✅
- [ ] Manual checklist: all 3 polish fixes PASS or trust-based sign-off recorded
- [ ] REQ-05 flipped Pending → Complete in `.planning/REQUIREMENTS.md`
```

**Edit `05-VALIDATION.md` frontmatter** (use Edit/Read pattern first — file already exists). Flip:
- `nyquist_compliant: false` → `nyquist_compliant: true`
- `wave_0_complete: false` → `wave_0_complete: true`
- `status: draft` → `status: ready`

Also update the bottom **Validation Sign-Off** checkboxes (all 6 → `[x]`) and `**Approval:** pending` → `**Approval:** wave-0-locked`.

Reason: Wave 0 fixtures + matrix scaffold satisfy the Nyquist contract. Per-task verification map already lists 5-00-XX through 5-04-01 — no further edits needed there (task IDs match this plan and subsequent plans 05-01..05-04).
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true</automated>
  </verify>
  <done>05-VERIFICATION.md exists with matrix + manual checklist + sign-off section. 05-VALIDATION.md frontmatter shows `nyquist_compliant: true, wave_0_complete: true, status: ready`. Full xUnit suite GREEN (anchors + existing tests, all new behavioural tests Skipped).</done>
</task>

</tasks>

<verification>
- All 4 fixture files compile and enumerate via `dotnet test --filter`.
- Full suite: zero Failed; expected delta = added Skipped count.
- `05-VERIFICATION.md` exists with all 6 matrix rows.
- `05-VALIDATION.md` Wave 0 frontmatter flipped.
</verification>

<success_criteria>
- [ ] 3 new fixture files exist with anchor + skip-gated RED rows
- [ ] Existing BaseElementCollectionServiceTests extended with 12 skip-gated RED rows (Wave 1) + 2 Revit-bound rows (manual)
- [ ] 05-VERIFICATION.md matrix scaffold complete
- [ ] 05-VALIDATION.md flipped to nyquist_compliant: true + wave_0_complete: true
- [ ] Full xUnit suite GREEN; 0 Failed
- [ ] Build clean via `dotnet build LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true`
</success_criteria>

<output>
After completion, create `.planning/phases/05-verification-and-polish/05-00-SUMMARY.md` recapping fixture counts, skip-gated row counts, and the matrix scaffold state.
</output>
