---
phase: 03-grid-collection-fixes
plan: 09
type: execute
wave: 6
depends_on: ["03-05", "03-06", "03-07", "03-08"]
files_modified: []
autonomous: false
requirements:
  - REQ-01
must_haves:
  truths:
    - "Manual Revit session confirms every grid in every migrated screen shows non-blank Name and Category"
    - "Default Category-ascending sort verified visually in Batch Rename"
    - "AND-combined filter verified in Batch Rename"
    - "FamilyParameter Category fallback to family name verified"
    - "LogView shows fallback warnings when null-Category elements are encountered (instead of silent skip)"
  artifacts:
    - path: ".planning/phases/03-grid-collection-fixes/03-09-SUMMARY.md"
      provides: "Per-screen verification results from the manual Revit session"
  key_links:
    - from: "User-driven Revit session"
      to: "REQ-01 acceptance"
      via: "Visual confirmation of no-blanks invariant across every migrated screen"
      pattern: "manual"
---

<objective>
Phase-end manual Revit verification consolidating all screens migrated in Plans 03-05 through 03-08. Closes REQ-01 acceptance.

Purpose: REQ-01 has manual-only acceptance criteria per VALIDATION.md (WPF + Revit Element objects not instantiable in xUnit). Single consolidated session avoids verification-fatigue across multiple plans.
Output: 03-09-SUMMARY.md documenting per-screen pass/fail; STATE.md updated to mark Phase 3 verification complete (or open gaps for `/gsd:plan-phase --gaps`).
</objective>

<execution_context>
@C:/Users/LECG Arquitectura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LECG Arquitectura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-grid-collection-fixes/03-CONTEXT.md
@.planning/phases/03-grid-collection-fixes/03-VALIDATION.md
@.planning/phases/03-grid-collection-fixes/03-05-SUMMARY.md
@.planning/phases/03-grid-collection-fixes/03-06-SUMMARY.md
@.planning/phases/03-grid-collection-fixes/03-07-SUMMARY.md
@.planning/phases/03-grid-collection-fixes/03-08-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual Revit acceptance — REQ-01 phase-end</name>
  <action>Pause execution and present the verification steps below to the user. Do NOT proceed past this task until user replies with the resume signal. No file changes — verification only.</action>
  <what-built>
    All grids across the plugin now use the shared row model + grid control. Every row has non-blank Name and Category. Batch Rename has Category column, default Category sort, AND-combined filter. Selection-backed screens show interactive grid with per-row checkbox.
  </what-built>
  <how-to-verify>
    Open Revit (latest dev build of the addin) on a project with diverse element types — include: walls, floors, doors, families, views, sheets, materials, line styles, fill patterns, AT LEAST one element type Revit returns with `Category == null` (e.g., certain system family types, GraphicsStyle entries), and at least one family parameter screen path.

    Per-screen checklist:

    **Batch Rename**
    1. Switch to Type-Name scope. Confirm column order ☑ | Type | Category | Original | New.
    2. Confirm default sort is Category ascending.
    3. Click Category header → flips to descending.
    4. Set Filter Category dropdown to "Walls" → only Walls visible.
    5. Apply per-column filter on Original column with text "X" → AND-combined with Walls.
    6. Confirm zero blank Name or Category cells.
    7. Switch to Family Parameter scope. Confirm Category cell shows Revit category for parented family parameters AND falls back to family name for category-less ones (no `<Uncategorized>` text, no blank cell).

    **Text-summary screens (Plans 03-06, 03-07)**: DivideToposolid, FixPoints, ConvertCad, SplitBoundaries, ConvertToposolidToFloor, ConvertFloorToToposolid
    - For each: pick mixed elements; confirm grid renders (no string-summary list); confirm Name + Category non-blank; confirm Status column carries the per-screen outcome text; confirm per-row checkbox toggles.

    **Selection-backed screens (Plan 03-08)**: AlignEdges, AlignElements, AssignMaterial, CategoryChanger, ChangeLevel, OffsetElevations, ResetSlabs, SimplifyPoints
    - For each: pick a few elements via the Selection control; confirm RowItems grid appears below the "N selected" summary; confirm Name + Category non-blank.

    **LogView**
    - Open LogView. Confirm at least one warning entry of the form `ElementLabelService: fallback for {ClrTypeName} {Id} → ({name}, {category})` exists if the model contained any null-Category elements. (If the model has none, this is N/A — note in the summary.)

    Document each screen's pass/fail in `03-09-SUMMARY.md`. For any failure, capture: screen name, exact misbehavior, repro steps. The `/gsd:plan-phase 3 --gaps` orchestrator will consume this summary if any gap remains.
  </how-to-verify>
  <resume-signal>Type "approved" if every screen passes, or paste the failure list to feed gap closure.</resume-signal>
</task>

</tasks>

<verification>
- Manual Revit session completed.
- 03-09-SUMMARY.md captures per-screen results.
- If any failure → orchestrator routes to `/gsd:plan-phase 3 --gaps`.
- If all pass → STATE.md updated; REQ-01 status flipped to Complete in REQUIREMENTS.md.
</verification>

<success_criteria>
REQ-01 user-visible acceptance verified end-to-end across every migrated screen.
</success_criteria>

<output>
After completion, create `.planning/phases/03-grid-collection-fixes/03-09-SUMMARY.md` and update `.planning/REQUIREMENTS.md` REQ-01 status.
</output>
