---
phase: 03-grid-collection-fixes
plan: 04
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - src/Services/Renaming/SearchReplacePreviewService.cs
  - src/Services/Renaming/ISearchReplacePreviewService.cs
  - src/ViewModels/SearchReplaceViewModel.cs
  - LECG.Tests/Services/SearchReplacePreviewServiceTests.cs
autonomous: true
requirements:
  - REQ-01
must_haves:
  truths:
    - "SearchReplacePreviewService.ProcessPreview returns List<ElementRowViewModel> (not List<ReplaceItem>)"
    - "Each returned row has non-blank Category propagated from ElementData.Category"
    - "ParamGroup, IsInstance, IsReadOnly, Family fields are propagated where ElementData provides them"
    - "Legacy ReplaceItem class is removed from SearchReplaceViewModel.cs (single row model only)"
  artifacts:
    - path: "src/Services/Renaming/SearchReplacePreviewService.cs"
      provides: "ProcessPreview returning List<ElementRowViewModel> with Category populated"
      contains: "ElementRowViewModel"
    - path: "src/ViewModels/SearchReplaceViewModel.cs"
      provides: "PreviewItems: ObservableCollection<ElementRowViewModel>; ReplaceItem class deleted"
      contains: "ObservableCollection<ElementRowViewModel>"
  key_links:
    - from: "src/Services/Renaming/SearchReplacePreviewService.cs"
      to: "src/ViewModels/Components/ElementRowViewModel.cs"
      via: "results.Add(new ElementRowViewModel { Category = el.Category, ... })"
      pattern: "new ElementRowViewModel"
    - from: "src/ViewModels/SearchReplaceViewModel.cs"
      to: "ElementRowViewModel"
      via: "PreviewItems collection type swap"
      pattern: "ObservableCollection<ElementRowViewModel>"
---

<objective>
Migrate the Batch Rename preview pipeline from `ReplaceItem` to `ElementRowViewModel`. Atomic swap: `SearchReplacePreviewService.ProcessPreview` return type, its interface, and `SearchReplaceViewModel.PreviewItems` all change in this plan to keep the project compiling. Delete the obsolete `ReplaceItem` class.

Purpose: closes RESEARCH §Pitfall 2 (atomic ReplaceItem migration) and adds Category propagation.
Output: SearchReplacePreviewServiceTests green; project compiles; `ReplaceItem` no longer exists.
</objective>

<execution_context>
@C:/Users/LECG Arquitectura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LECG Arquitectura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-grid-collection-fixes/03-CONTEXT.md
@.planning/phases/03-grid-collection-fixes/03-RESEARCH.md
@.planning/phases/03-grid-collection-fixes/03-02-SUMMARY.md
@src/Services/Renaming/SearchReplacePreviewService.cs
@src/Services/Renaming/ISearchReplacePreviewService.cs
@src/ViewModels/SearchReplaceViewModel.cs
@src/Views/SearchReplaceView.xaml
@LECG.Tests/Services/SearchReplacePreviewServiceTests.cs

<interfaces>
ElementRowViewModel (from Plan 03-02):
```csharp
namespace LECG.ViewModels.Components;
public partial class ElementRowViewModel : ObservableObject {
    public long Id { get; set; }
    public string Name, Category, Type, Status, Family, OriginalValue, NewValue, ParamGroup;
    public bool IsInstance, IsReadOnly;
    public bool IsChecked { get; set; }  // generated from [ObservableProperty] _isChecked
}
```

ElementData (existing): has Category, ParamGroup, IsInstance, IsReadOnly already.

ReplaceItem (DELETE in this plan): currently in `src/ViewModels/SearchReplaceViewModel.cs` lines 16-25. References to delete: `SearchReplacePreviewService.cs` line 110, any XAML `Binding ElementName` (likely in `SearchReplaceView.xaml` — confirm and rebind to `Name`).

ISearchReplacePreviewService (existing): single method `ProcessPreview(...)` returning `List<ReplaceItem>`. Change to `List<ElementRowViewModel>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate ProcessPreview return type and Category propagation</name>
  <files>
    src/Services/Renaming/SearchReplacePreviewService.cs,
    src/Services/Renaming/ISearchReplacePreviewService.cs,
    LECG.Tests/Services/SearchReplacePreviewServiceTests.cs
  </files>
  <behavior>
    - ProcessPreview returns List<ElementRowViewModel>.
    - Each row's Category equals the corresponding `ElementData.Category` (which the post-03-03 collection layer guarantees non-blank).
    - ParamGroup, IsInstance, IsReadOnly, Type, OriginalValue, NewValue, Id fields are propagated.
    - Tests authored in Plan 03-00 transition from Skip to GREEN.
  </behavior>
  <action>
    1. Edit `src/Services/Renaming/ISearchReplacePreviewService.cs`: change return type from `List<ReplaceItem>` to `List<ElementRowViewModel>`. Add `using LECG.ViewModels.Components;`.

    2. Edit `src/Services/Renaming/SearchReplacePreviewService.cs`: change return type at the method signature, change collection type at line ~70-ish (the local `results` variable), and update the construction at lines 110-118:

       ```csharp
       results.Add(new ElementRowViewModel
       {
           Id = el.Id,
           Name = el.Name,
           Category = el.Category,            // already guaranteed non-blank by 03-03
           OriginalValue = el.Name,
           NewValue = currentName,
           IsChecked = true,
           Type = el.Type,
           ParamGroup = el.ParamGroup,
           IsInstance = el.IsInstance,
           IsReadOnly = el.IsReadOnly
       });
       ```

       Add `using LECG.ViewModels.Components;` at the top.

    3. Update `LECG.Tests/Services/SearchReplacePreviewServiceTests.cs`: remove the "anchor — delete in 03-04" test marked in Plan 03-00. Remove `[Skip=...]` attributes from the three Category-propagation tests. Verify NSubstitute setup still compiles (the IRenameRulePipelineService mock has not changed).

    4. Anti-patterns to avoid:
       - Do NOT keep `ReplaceItem` and `ElementRowViewModel` coexisting (RESEARCH §Anti-Pattern). Deletion is part of Task 2.
       - Do NOT add a category-string filter inside ProcessPreview that compares English category names — that's reserved for ICollectionView in Plan 03-05.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj --filter "FullyQualifiedName~SearchReplacePreviewServiceTests" --no-restore</automated>
    Expected: SearchReplacePreviewServiceTests pass; previously-skipped Category tests green.
  </verify>
  <done>
    ProcessPreview returns `List<ElementRowViewModel>`; tests green; interface and implementation aligned. Project does not yet compile if SearchReplaceViewModel still uses `List<ReplaceItem>` — Task 2 closes that gap.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete ReplaceItem; migrate SearchReplaceViewModel + View bindings</name>
  <files>
    src/ViewModels/SearchReplaceViewModel.cs,
    src/Views/SearchReplaceView.xaml,
    src/Views/SearchReplaceView.xaml.cs
  </files>
  <action>
    1. In `src/ViewModels/SearchReplaceViewModel.cs`:
       - Delete the `ReplaceItem` class (lines ~16-25).
       - Add `using LECG.ViewModels.Components;`.
       - Change every `ObservableCollection<ReplaceItem>` and `List<ReplaceItem>` to the `ElementRowViewModel` equivalent. Most likely sites: `PreviewItems` property, the search command body, any helper methods that take `ReplaceItem`.
       - Where `ReplaceItem.ElementName` is used, rebind to `ElementRowViewModel.Name`. Where `ElementId` is used, rebind to `Id` (note type change long vs long — verify and update at usage sites).
       - Do NOT yet wire ICollectionView default sort or per-column filter — that is Plan 03-05's job.

    2. In `src/Views/SearchReplaceView.xaml`:
       - Find any DataGrid/ListView column bound to `{Binding ElementName}` and rebind to `{Binding Name}`.
       - Find any `{Binding ElementId}` and rebind to `{Binding Id}`.
       - Do NOT yet add the Category column — that is Plan 03-05.
       - Do NOT yet swap to `ElementGridControl` — Plan 03-05.

    3. In `src/Views/SearchReplaceView.xaml.cs`: any code-behind referencing `ReplaceItem` rebinds to `ElementRowViewModel`.

    4. Search the rest of the project for stragglers:
       ```
       grep -rn "ReplaceItem" src/ LECG.Tests/
       ```
       Every hit must either be (a) deleted or (b) renamed to `ElementRowViewModel`. Common stragglers: command handlers, DI registrations, executor services that consume the preview list.

    5. Confirm `BatchRenameExecutionService` (`src/Services/Renaming/BatchRenameExecutionService.cs`) still compiles after the type swap — it likely consumes the preview list. Update its parameter types to `List<ElementRowViewModel>` / `IEnumerable<ElementRowViewModel>` as needed.
  </action>
  <verify>
    <automated>dotnet build LECG.sln --no-restore</automated>
    Expected: clean build, zero "ReplaceItem" references remaining (`grep -r "ReplaceItem" src/ LECG.Tests/` returns empty).

    Followed by:
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj --no-restore</automated>
    Expected: full suite green (excluding any tests still Skip-gated for Plan 03-05).
  </verify>
  <done>
    `grep "ReplaceItem" src/ LECG.Tests/` returns no hits; build clean; full unit suite green; `SearchReplaceViewModel.PreviewItems` is `ObservableCollection<ElementRowViewModel>`.
  </done>
</task>

</tasks>

<verification>
- ReplaceItem class removed from codebase.
- ProcessPreview returns ElementRowViewModel rows with non-blank Category.
- SearchReplacePreviewServiceTests green.
- Build clean.
</verification>

<success_criteria>
Single shared row model in use across the Batch Rename pipeline. Category propagation invariant holds end-to-end from `BaseElementCollectionService` through `ElementData` through `SearchReplacePreviewService` into `SearchReplaceViewModel.PreviewItems`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-grid-collection-fixes/03-04-SUMMARY.md` listing files touched, ReplaceItem-reference removal count, and any consumer services that needed signature updates.
</output>
