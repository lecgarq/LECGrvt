---
phase: 04-advanced-renaming-logic
plan: 04
type: execute
wave: 3
depends_on: [00, 03]
files_modified:
  - src/Services/Renaming/BatchRenameExecutionService.cs
  - LECG.Tests/Services/BatchRenameSafeRenameTests.cs
requirements: [REQ-03]
autonomous: true
must_haves:
  truths:
    - "After a successful RenameParameter call inside the per-param SubTransaction, every Dimension in the family whose `FamilyLabel.Definition.Name == oldName` has its `FamilyLabel` reassigned to the renamed FamilyParameter reference"
    - "The reassignment refetches the renamed param via `FindFamilyParameterByName(manager, newName)` BEFORE setting `dim.FamilyLabel` (Pitfall 2 — stale reference)"
    - "Success log line reads `Renamed '{old}' to '{new}' (updated {F} formulas, {D} dimension labels)` when at least one of F/D > 0; falls back to `Renamed '{old}' to '{new}' (updated {F} formulas)` or `Renamed '{old}' to '{new}' (updated {D} dimension labels)` or `Renamed '{old}' to '{new}'` accordingly"
    - "If `Dimension.FamilyLabel` setter throws, the SubTransaction rolls back; renamedCount is NOT incremented for that param"
    - "BuildDimensionLabelNames is extended (or a sibling collector is added) to return the actual Dimension objects per oldName, not just the HashSet of names — Phase 4 needs the objects to reassign"
    - "All REQ-03 RED tests in BatchRenameSafeRenameTests GREEN"
  artifacts:
    - path: "src/Services/Renaming/BatchRenameExecutionService.cs"
      provides: "Dimension reassignment loop inside per-param SubTransaction; dimension-by-name collector"
      contains: "FamilyLabel ="
  key_links:
    - from: "Per-param SubTransaction body (Plan 04-03)"
      to: "Dimension.FamilyLabel = renamedFamilyParameter"
      via: "for-each over dimensionsByName[item.OriginalValue]"
      pattern: "FamilyLabel\\s*="
    - from: "Renamed param resolution"
      to: "FindFamilyParameterByName(manager, item.NewValue)"
      via: "post-rename refetch"
      pattern: "FindFamilyParameterByName"
---

<objective>
Extend the per-param SubTransaction (built in Plan 04-03) to also reassign every `Dimension.FamilyLabel` that pointed at the old parameter name. Closes REQ-03.

Purpose: Complete the safe-rename trio (formula + dimension + element-associated). After this plan, "skip with reason" is reserved for the three immutable categories (built-in, reporting, name-conflict).

Output: Modified `BatchRenameExecutionService` with dimension reassignment loop; REQ-03 RED tests GREEN; live-Revit verification deferred to Plan 04-05.
</objective>

<execution_context>
@C:/Users/LECG Arquitectura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LECG Arquitectura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-advanced-renaming-logic/04-CONTEXT.md
@.planning/phases/04-advanced-renaming-logic/04-RESEARCH.md
@.planning/phases/04-advanced-renaming-logic/04-03-PLAN.md
@src/Services/Renaming/BatchRenameExecutionService.cs

<interfaces>
<!-- Existing collector (returns names only, lines 381-404) -->
private static HashSet<string> BuildDimensionLabelNames(Document famDoc)

<!-- New companion collector — returns objects grouped by label name -->
private static Dictionary<string, List<Dimension>> BuildDimensionsByLabelName(Document famDoc)
{
    var result = new Dictionary<string, List<Dimension>>(StringComparer.Ordinal);
    foreach (Dimension dim in new FilteredElementCollector(famDoc).OfClass(typeof(Dimension)).Cast<Dimension>())
    {
        FamilyParameter? label = dim.FamilyLabel;
        if (label == null) continue;
        string name = label.Definition.Name;
        if (!result.TryGetValue(name, out var list)) { list = new(); result[name] = list; }
        list.Add(dim);
    }
    return result;
}

<!-- Post-rename param refetch helper (existing) -->
private static FamilyParameter? FindFamilyParameterByName(FamilyManager manager, string name)
    => manager.Parameters.Cast<FamilyParameter>().FirstOrDefault(p => string.Equals(p.Definition.Name, name, StringComparison.Ordinal));

<!-- Dimension reassignment inside SubTransaction -->
if (dimensionsByName.TryGetValue(item.OriginalValue, out var dims))
{
    var renamedRef = FindFamilyParameterByName(manager, item.NewValue);
    if (renamedRef != null)
    {
        foreach (Dimension dim in dims)
        {
            try { dim.FamilyLabel = renamedRef; dimCount++; }
            catch (Exception ex)
            {
                _logger.LogWarning($"Dimension label reassign failed for '{item.OriginalValue}': {ex.Message}");
                throw; // propagate to rollback the SubTransaction
            }
        }
    }
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BuildDimensionsByLabelName collector + dimension reassignment loop in SubTransaction</name>
  <files>src/Services/Renaming/BatchRenameExecutionService.cs, LECG.Tests/Services/BatchRenameSafeRenameTests.cs</files>
  <behavior>
    - New static method `BuildDimensionsByLabelName(Document famDoc) → Dictionary<string, List<Dimension>>` returns dimensions grouped by their current label parameter name; `null` `FamilyLabel` dimensions are skipped (no key added)
    - Inside the per-param SubTransaction (built in Plan 04-03), AFTER the formula-update loop and BEFORE `subTx.Commit()`:
      1. Look up `dimensionsByName[item.OriginalValue]` (StringComparer.Ordinal)
      2. If non-empty, call `FindFamilyParameterByName(manager, item.NewValue)` to get the freshly renamed param reference (Pitfall 2)
      3. For each dimension in the list, set `dim.FamilyLabel = renamedRef`; increment `dimCount` per success
      4. If the setter throws, propagate the exception — the outer try/catch rolls back the SubTransaction, renamedCount stays unincremented, LogWarning emitted
    - Success log line composes formula + dimension counts:
      - Both > 0: `Renamed '{old}' to '{new}' (updated {F} formulas, {D} dimension labels)`
      - Only formulas: `Renamed '{old}' to '{new}' (updated {F} formulas)`
      - Only dims: `Renamed '{old}' to '{new}' (updated {D} dimension labels)`
      - Neither: `Renamed '{old}' to '{new}'`
    - `BuildDimensionLabelNames` STAYS intact — it's still used to feed the (now narrowed) `GetRenameSkipReason` for any call-site that hasn't been refactored away. Plan 04-04 only ADDS the new collector; it does not remove the old one.
  </behavior>
  <action>
    Step A — Un-Skip and implement the 3 REQ-03 RED tests in `BatchRenameSafeRenameTests.cs`:
    - `RenameFamilyParameters_DimensionLabel_ReassignsAllMatchingDimensions` — arrange 3 mock Dimensions with `FamilyLabel.Definition.Name == "old"`, assert `dim.FamilyLabel = renamedRef` was called on each
    - `RenameFamilyParameters_DimensionLabel_LogsSuccessWithDimensionCount` — assert `LogSuccess` invoked with `"(updated 0 formulas, 3 dimension labels)"` (or the dim-only variant — whichever the format-tree dictates)
    - `RenameFamilyParameters_DimensionLabel_StaleParamReference_ReFetchedByNewName` — assert `FindFamilyParameterByName` was called between `RenameParameter` and the first `dim.FamilyLabel` assignment

    NSubstitute for `Dimension` returning a mock `FamilyParameter` from `FamilyLabel.get` with a `Definition.Name` set via NSubstitute. For the assignment side, NSubstitute records the property setter call (`dim.Received(1).FamilyLabel = Arg.Is<FamilyParameter>(...)`).

    Step B — Add `BuildDimensionsByLabelName` static method to `BatchRenameExecutionService.cs` adjacent to `BuildDimensionLabelNames`. Implementation matches the `<interfaces>` block exactly.

    Step C — Modify the EditFamily/LoadFamily cycle (~lines 142-216) to call `BuildDimensionsByLabelName(famDoc)` once per family alongside the existing `BuildDimensionLabelNames` call, and pass the dictionary into `RenameFamilyParameters` (add a parameter to that private method).

    Step D — Modify the per-param SubTransaction body in `RenameFamilyParameters` (built in Plan 04-03):
    - Add `int dimCount = 0;` adjacent to `int formulaCount = 0;`
    - After the formula-update loop, BEFORE `subTx.Commit()`, insert the dimension reassignment block from `<interfaces>`.
    - Replace the Plan 04-03 success log call with the new composite format. Centralize the format in a tiny helper for testability:
      ```csharp
      private static string FormatSafeRenameLog(string oldName, string newName, int formulaCount, int dimCount)
      {
          if (formulaCount == 0 && dimCount == 0) return $"Renamed '{oldName}' to '{newName}'";
          if (formulaCount > 0 && dimCount > 0) return $"Renamed '{oldName}' to '{newName}' (updated {formulaCount} formulas, {dimCount} dimension labels)";
          if (formulaCount > 0) return $"Renamed '{oldName}' to '{newName}' (updated {formulaCount} formulas)";
          return $"Renamed '{oldName}' to '{newName}' (updated {dimCount} dimension labels)";
      }
      ```

    Step E — Re-confirm Pitfall 1: every catch path in the SubTransaction body calls `RollbackIfNotClosed` (already done in Plan 04-03; this plan does not alter it).

    Step F — Side-effect count surfaced in Status (Plan 04-02) — verify the ProcessPreview side-effect count loop is consistent with this dim-count computation. If Plan 04-02 used a different formula for `dimensionCount`, normalize both to: `count of Dimension objects in the family whose FamilyLabel.Definition.Name == row.Original`. If a fix is needed in `SearchReplacePreviewService`, include it in this task and ensure the affected ProcessPreview tests still GREEN.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --filter "FullyQualifiedName~BatchRenameSafeRenameTests|FullyQualifiedName~SearchReplacePreviewServiceTests" --nologo -v minimal</automated>
  </verify>
  <done>
    - 3 REQ-03 RED tests flipped GREEN
    - All REQ-02 + REQ-04 tests still GREEN
    - `FormatSafeRenameLog` helper used at the success-log call site
    - Stale-reference safeguard via `FindFamilyParameterByName` confirmed by test
    - Full suite GREEN
    - No new probe SubTransactions introduced
  </done>
</task>

</tasks>

<verification>
- `dotnet test LECG.Tests/LECG.Tests.csproj -p:SkipRevitDeploy=true --nologo -v minimal` GREEN (100+ tests, all REQ-02/03/04 stubs flipped GREEN)
- Build clean
- All REQ-02/03/04 RED tests are now GREEN
- Live-Revit verification still pending (Plan 04-05)
</verification>

<success_criteria>
- `BuildDimensionsByLabelName` exists and is used per-family
- Dimension reassignment runs inside the per-param SubTransaction, after formula updates, before commit
- Log line format is composite and reflects actual counts
- No regression in REQ-02 or REQ-04 tests
- Open Question 1 (RESEARCH.md) — "Dimension FamilyLabel setter may require null-clear first" — handled by the try/catch + rollback path; if the setter does throw on assignment, the rollback contains the damage; manual Revit (Plan 04-05) verifies whether null-clear-first is needed in practice
</success_criteria>

<output>
Create `.planning/phases/04-advanced-renaming-logic/04-04-SUMMARY.md` covering:
- New `BuildDimensionsByLabelName` collector signature and usage
- Dimension reassignment block location in SubTransaction
- `FormatSafeRenameLog` helper and the 4 branches
- Cross-check normalization done with `SearchReplacePreviewService` dim-count (if any divergence found)
- Test count flipped RED → GREEN (3)
- Open Question 1 disposition (waiting on live Revit in Plan 04-05)
</output>
