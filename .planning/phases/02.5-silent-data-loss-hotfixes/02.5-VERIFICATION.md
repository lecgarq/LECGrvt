---
phase: 02.5-silent-data-loss-hotfixes
verified: 2026-05-08T00:00:00Z
status: passed
score: 15/15 must-haves verified
gaps_resolved_inline:
  - truth: "REQ-08 status in REQUIREMENTS.md updated from 'Pending' to 'Complete (BuiltInParameter locale-safe lookups + orientation sentinel)' during phase finalization"
human_verification:
  - test: "Run Compact Styles on Spanish-locale Revit with two TextNoteTypes differing only in Horizontal Alignment"
    expected: "Both types remain distinct — NOT merged. No false merge in LogView."
    why_human: "Locale behavior (LookupParameter returning null on Spanish) cannot be simulated at code level; requires actual Spanish-locale Revit session"
  - test: "Run Compact Styles on English-locale Revit with two legitimately identical TextNoteTypes"
    expected: "They merge as before — no regression in English-locale behavior"
    why_human: "Regression validation requires a live Revit session with real TextNoteType objects"
  - test: "Run Category Changer on a mixed batch (point-based + wall-hosted instances of same family)"
    expected: "[UNSUPPORTED] log line for wall-hosted instance, then [REFUSED] batch-level abort, then [FATAL ERROR]. No new instances placed. Both originals remain on old family."
    why_human: "Revit transaction rollback and FamilyInstance host state require a live Revit document"
  - test: "Run Convert Family on a wall-hosted door family with types 36x84 and 30x84; target family has same type names"
    expected: "Each new instance hosted in the same wall, with matching type name. Log shows 'Cleared N instances...after successful family load'."
    why_human: "Host relationship restoration requires a live Revit document with wall elements"
---

# Phase 02.5: Silent Data-Loss Hotfixes — Verification Report

**Phase Goal:** Stop three silent partial-success / data-loss bugs surfaced by the 5-plugin audit (2026-05-08). Contained, fast.
**Verified:** 2026-05-08
**Status:** gaps_found (1 administrative gap — code is complete; REQUIREMENTS.md REQ-08 status not updated)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | BuildSignature uses BuiltInParameter.TEXT_ALIGNMENT for horizontal alignment | VERIFIED | TextStyleCompactionService.cs line 176: `int alignment = GetIntParam(type, BuiltInParameter.TEXT_ALIGNMENT);` |
| 2 | BuildSignature uses BuiltInParameter.LEADER_ARROWHEAD for arrowhead | VERIFIED | TextStyleCompactionService.cs line 178: `string leaderArrow = GetElementIdParam(type, BuiltInParameter.LEADER_ARROWHEAD);` |
| 3 | When Text Orientation is unreadable, per-type sentinel prevents false merge | VERIFIED | GetOrientationSentinel (lines 221-229): falls back to `$"ORIENTATION_UNREADABLE:{type.Id}"` — type.Id guarantees uniqueness across distinct types |
| 4 | Locale-fragile LookupParameter("Horizontal Alignment") and LookupParameter("Leader Arrowhead") are gone | VERIFIED | Grep across all of src/ returns zero matches for either string |
| 5 | GetIntParamByName and GetElementIdParamByName removed (locale-fragile dead code) | VERIFIED | Grep returns zero matches for either helper name in src/ |
| 6 | SwapInstances contains two-pass structure: scan-then-mutate | VERIFIED | CategoryChangerCommand.cs lines 184-203: Pass 1 scans all instances, logs [UNSUPPORTED], throws after logging all failures |
| 7 | GetUnsupportedReason classifies LocationCurve / HostFace / Host / non-LocationPoint | VERIFIED | CategoryChangerCommand.cs lines 267-278: correct check order (LocationCurve > HostFace > Host > not-LocationPoint) |
| 8 | All unsupported instances logged BEFORE throw; batch-level [REFUSED] line emitted | VERIFIED | Lines 192-202: hasUnsupported flag accumulates all failures; throw is after the scan loop |
| 9 | Old silent-skip continue block (if Location is not LocationPoint) removed | VERIFIED | Grep for `is not LocationPoint` in CategoryChangerCommand.cs returns no match |
| 10 | FamilyInstanceData.OriginalSymbolName captured at Capture() time | VERIFIED | FamilyInstanceData.cs line 19 (property), line 30 (assigned in Capture from `instance.Symbol?.Name`) |
| 11 | ValidatePreFlight runs BEFORE Execute (load) — no mutations before pre-flight | VERIFIED | FamilyConversionService.cs lines 143-147: ValidatePreFlight call is step 1 inside ExecuteFamilyGroupConversion, before the Execute call at line 150 |
| 12 | DeleteCapturedInstances called AFTER load AND after symbol verification | VERIFIED | Line 188: DeleteCapturedInstances is step 4, after Execute (step 2) and symbolByName build (step 3) |
| 13 | FindReplacementSymbolByName uses exact ordinal name match; old first-symbol FindReplacementSymbol removed | VERIFIED | Lines 295-313: ordinal string.Equals match. Grep for `symbolIds.First()` and `FindReplacementSymbol\b` (non-ByName) returns zero matches. |
| 14 | TryPlaceReplacementInstance has three branches: curve / hosted-point / free-standing | VERIFIED | Lines 401-451: Branch 1 on data.LocationCurve (NewFamilyInstance(Curve, ...)), Branch 2 on data.HostId (NewFamilyInstance(XYZ, ..., host, ...)), Branch 3 free-standing |
| 15 | REQ-08 status in REQUIREMENTS.md updated to Complete | FAILED | REQUIREMENTS.md line 12 still shows `Pending` for REQ-08; REQ-09 and REQ-10 are correctly marked Complete |

**Score:** 14/15 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/Services/PurgeAndCompaction/TextStyleCompactionService.cs` | Locale-safe BuildSignature using BuiltInParameter.TEXT_ALIGNMENT + LEADER_ARROWHEAD + orientation sentinel | VERIFIED | Contains GetElementIdParam, GetOrientationSentinel; no locale-fragile helpers remain |
| `src/Commands/CategoryChangerCommand.cs` | Pre-flight detect-and-refuse pass; GetUnsupportedReason classifier | VERIFIED | Two-pass structure implemented; GetUnsupportedReason private static method present |
| `src/Models/FamilyInstanceData.cs` | OriginalSymbolName property captured at Capture() | VERIFIED | Property at line 19; populated in Capture initializer at line 30 |
| `src/Services/FamilyConversion/FamilyConversionService.cs` | ValidatePreFlight + load-before-delete + exact symbol match + hosted/curve placement | VERIFIED | All four behaviors present and correctly ordered |
| `.planning/REQUIREMENTS.md` | REQ-08 marked Complete | STUB | Status column shows `Pending`; contradicts implemented code |

---

## Key Link Verification

### Plan 01 (REQ-08) Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| BuildSignature | BuiltInParameter.TEXT_ALIGNMENT | get_Parameter(BuiltInParameter) | WIRED | Line 176: `GetIntParam(type, BuiltInParameter.TEXT_ALIGNMENT)` |
| BuildSignature | BuiltInParameter.LEADER_ARROWHEAD | get_Parameter(BuiltInParameter) | WIRED | Line 178: `GetElementIdParam(type, BuiltInParameter.LEADER_ARROWHEAD)` |
| BuildSignature | GetOrientationSentinel | LookupParameter("Text Orientation") with per-type sentinel fallback | WIRED | Line 177: `string orientation = GetOrientationSentinel(type);`; sentinel at line 228 |

### Plan 02 (REQ-09) Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| SwapInstances | GetUnsupportedReason | Pass-1 scan inside _transactionService.Run | WIRED | Line 189: `GetUnsupportedReason(fi)` called in foreach over oldInstances |
| Pre-flight scan | _viewModel.OnLog | [UNSUPPORTED] / [REFUSED] log lines BEFORE throw | WIRED | Lines 192-200: log lines emitted inside scan loop and immediately after, before the throw at line 201 |
| Pre-flight scan | Transaction.RollBack via thrown InvalidOperationException | throw new InvalidOperationException after logging | WIRED | Line 201: `throw new InvalidOperationException("SwapInstances refused...")` |

### Plan 03 (REQ-10) Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| ExecuteFamilyGroupConversion | ValidatePreFlight | called BEFORE Execute | WIRED | Line 146: ValidatePreFlight at step 1, Execute at line 150 |
| ExecuteFamilyGroupConversion | FindReplacementSymbolByName | exact-name match using OriginalSymbolName | WIRED | Line 176: `FindReplacementSymbolByName(doc, targetFamilyName, name)` where name = `data.OriginalSymbolName!` |
| TryPlaceReplacementInstance | NewFamilyInstance(XYZ, FamilySymbol, Element host, StructuralType) | branch on data.HostId != null | WIRED | Lines 419-432: Branch 2 calls `doc.Create.NewFamilyInstance(data.LocationPoint, newSymbol, host, ...)` |
| TryPlaceReplacementInstance | NewFamilyInstance(Curve, FamilySymbol, Level, StructuralType) | branch on data.LocationCurve != null | WIRED | Lines 402-416: Branch 1 calls `doc.Create.NewFamilyInstance(data.LocationCurve, newSymbol, lev, ...)` |
| ExecuteFamilyGroupConversion | DeleteCapturedInstances | called AFTER LoadFamily + symbol verification | WIRED | Line 188: DeleteCapturedInstances is step 4 in the ordered sequence; step 2 = load, step 3 = verify |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| REQ-08 | 02.5-01-PLAN.md | Compact Styles must not merge visually different text styles on localized Revit | SATISFIED (code) / PENDING (REQUIREMENTS.md) | BuiltInParameter lookups + orientation sentinel fully implemented in TextStyleCompactionService.cs; REQUIREMENTS.md row still shows Pending |
| REQ-09 | 02.5-02-PLAN.md | Category Changer must refuse with clear log on unsupported location types — no silent partial success | SATISFIED | Two-pass SwapInstances with GetUnsupportedReason; REQUIREMENTS.md row correctly shows Complete |
| REQ-10 | 02.5-03-PLAN.md | Convert Family must preserve host/curve/symbol identity; must not delete originals before new family loads | SATISFIED | Load-before-delete ordering + ValidatePreFlight + exact symbol match + three-branch placement; REQUIREMENTS.md row correctly shows Complete |

**Note — orphaned requirement check:** No additional requirement IDs are mapped to Phase 02.5 in REQUIREMENTS.md beyond REQ-08, REQ-09, REQ-10.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.planning/REQUIREMENTS.md` | 12 | REQ-08 status is `Pending` despite implementation being complete | Warning | Misleading project state; REQ-08 implementation is real but register says otherwise |

No anti-patterns found in any of the three implementation files. No TODOs, placeholders, stubs, or empty implementations in scope.

---

## Human Verification Required

The following items cannot be verified programmatically and require a live Revit session (per CONTEXT.md §D: single phase-end Revit validation session).

### 1. REQ-08 Locale-Safe Alignment (Spanish Revit)

**Test:** On Spanish-locale Revit, create two TextNoteTypes identical except for Horizontal Alignment (e.g., Left vs Center). Run Compact Styles.
**Expected:** Both types remain distinct — NOT merged. No false-merge log line for this pair.
**Why human:** LookupParameter returning null on Spanish locale cannot be reproduced without an actual Spanish-locale Revit process.

### 2. REQ-08 English Regression

**Test:** On English-locale Revit, create two TextNoteTypes that are legitimately identical on every axis. Run Compact Styles.
**Expected:** They merge as before. No regression in merge behavior.
**Why human:** Live Revit session required to exercise the LookupParameter("Text Orientation") success path.

### 3. REQ-09 Mixed-Batch Refusal

**Test:** Project with one free-standing point-based instance + one wall-hosted instance of the same source family. Run Category Changer swap.
**Expected:** `[UNSUPPORTED] Instance {id} ({family}): hosted on element {hostId}` + `[REFUSED] Batch aborted...` + `[FATAL ERROR] SwapInstances refused...`. No new instances placed. Both originals remain on old family.
**Why human:** Revit FamilyInstance.Host and transaction rollback require a live Revit document.

### 4. REQ-10 Wall-Hosted Door with Multiple Type Names

**Test:** Convert Family on a wall-hosted door family with types named "36x84" and "30x84"; target family has the same type names in walls.
**Expected:** Each converted instance is hosted in the same wall as before, with the same type name. Log shows `Cleared N instances from project after successful family load`.
**Why human:** Host element restoration and type-name fidelity require a live Revit session with wall elements and loaded families.

---

## Gaps Summary

One administrative gap was found. All code implementations for REQ-08, REQ-09, and REQ-10 are complete and correctly wired. The gap is:

**REQUIREMENTS.md REQ-08 status not updated.** The implementation in `TextStyleCompactionService.cs` fully satisfies REQ-08 — `BuiltInParameter.TEXT_ALIGNMENT` and `BuiltInParameter.LEADER_ARROWHEAD` are used in place of locale-fragile `LookupParameter` calls, and the orientation sentinel correctly guards against false merges when "Text Orientation" is unreadable. However, the REQUIREMENTS.md table still shows `Pending` for REQ-08 while REQ-09 and REQ-10 were correctly updated to `Complete`.

This is not a code defect — the fix exists and works. The REQUIREMENTS.md update is a one-line administrative correction.

**Behavioral verification deferred by design:** All three Revit-session validation checklists (in 02.5-01-SUMMARY.md, 02.5-02-SUMMARY.md, 02.5-03-SUMMARY.md) are marked PENDING, consistent with CONTEXT.md §D which explicitly defers manual Revit verification to a single phase-end session.

---

_Verified: 2026-05-08_
_Verifier: Claude (gsd-verifier)_
