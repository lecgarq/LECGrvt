---
phase: 06-cross-cutting-foundation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/Core/Bootstrapper.cs
  - src/Core/RevitCommand.cs
  - src/App.cs
  - src/Views/LogView.xaml.cs
  - src/ViewModels/LogViewModel.cs
  - src/ViewModels/CategoryChangerViewModel.cs
  - src/ViewModels/FilterCopyViewModel.cs
  - src/Commands/ConvertFamilyCommand.cs
  - src/Commands/FormulaAutoGroupingCommand.cs
  - src/Commands/PurgeCommand.cs
  - src/Commands/SearchReplaceCommand.cs
  - src/Utilities/ExecutionTimer.cs
  - src/Services/ElementLabelService.cs
  - src/Services/CadConversion/CadCurveTessellationService.cs
  - src/Services/CadConversion/CadPolylineExtractionService.cs
  - src/Services/FamilyConversion/FamilyGeometryCopyService.cs
  - src/Services/FamilyConversion/FamilyConversionLoggingService.cs
  - src/Services/FamilyConversion/FamilyConversionExecutionService.cs
  - src/Services/FamilyConversion/FamilyEditorService.cs
  - src/Services/FamilyConversion/FamilyConversionService.cs
  - src/Services/FamilyConversion/FamilyProjectLoadService.cs
  - src/Services/FamilyConversion/FamilyTempFileCleanupService.cs
  - src/Services/FamilyConversion/FamilyParameterSetupService.cs
  - src/Services/FamilyConversion/FamilySourceDocumentService.cs
  - src/Services/FamilyConversion/FamilyTargetDocumentService.cs
  - src/Services/FamilyConversion/FamilySaveService.cs
  - src/Services/FamilyConversion/FamilyTemplatePathService.cs
  - src/Services/Renaming/BaseElementCollectionService.cs
  - src/Services/Renaming/BatchRenameExecutionService.cs
  - src/Services/RenderAppearance/RenderAppearanceSingleSyncService.cs
  - src/Services/PurgeAndCompaction/CompactionSharedHelper.cs
  - src/Services/PurgeAndCompaction/LinePatternCompactionService.cs
  - src/Services/PurgeAndCompaction/PurgeContext.cs
  - src/Services/PurgeAndCompaction/PurgeLinePatternService.cs
  - src/Services/PurgeAndCompaction/PurgeReferenceScannerService.cs
  - src/Services/Materials/MaterialBitmapPropertyService.cs
  - src/Services/Infrastructure/SettingsManager.cs
  - src/Services/Infrastructure/Logging/Logger.cs
autonomous: true
requirements: [CROSS-01]
must_haves:
  truths:
    - "Zero references to `Logger.Instance` remain in src/"
    - "Every service that previously called Logger.Instance now receives ILogger via constructor injection"
    - "RevitCommand exposes a protected ILogger resolved via ServiceLocator in PrepareCommandExecution"
    - "Bootstrapper.cs registers Logger as ILogger via AddSingleton<ILogger, Logger>() and configures the structured-logger post-build"
    - "Full xUnit suite still GREEN (175 baseline + CrossCutting Wave-0 tests still passing)"
  artifacts:
    - path: "src/Services/Infrastructure/Logging/Logger.cs"
      provides: "Singleton accessor `Logger.Instance` deleted; [Obsolete] no-scope overloads deleted"
      contains: "public sealed class Logger"
    - path: "src/Core/Bootstrapper.cs"
      provides: "Logger registered as ILogger via DI; ConfigureStructuredLogger called post-build"
      contains: "AddSingleton<ILogger"
    - path: "src/Core/RevitCommand.cs"
      provides: "Protected `_logger` field resolved from ServiceLocator in PrepareCommandExecution; `Log` helper delegates to `_logger.Log(text, scope: CommandName)`"
      contains: "ServiceLocator.GetRequiredService<ILogger>"
  key_links:
    - from: "RevitCommand.PrepareCommandExecution"
      to: "ServiceLocator.GetRequiredService<ILogger>()"
      via: "field assignment"
      pattern: "_logger = ServiceLocator"
    - from: "Bootstrapper.Initialize (post-BuildServiceProvider)"
      to: "ILogger.ConfigureStructuredLogger(loggerFactory)"
      via: "resolved service call"
      pattern: "ConfigureStructuredLogger"
    - from: "Every migrated service constructor"
      to: "ILogger"
      via: "constructor injection"
      pattern: "ILogger _logger"
---

<objective>
Migrate every `Logger.Instance` call site to constructor-injected `ILogger`, then physically delete the `Logger.Instance` singleton and the `[Obsolete]` transitional overloads.

Purpose: This is the bulk of CROSS-01 — converting the inventory (RESEARCH "Migration Inventory" table, ~65 call sites across 18 files) into DI-pure code so Phases 7–11 can rely on a single logger pipeline with explicit scope tags.
Output: Codebase with zero `Logger.Instance` references in `src/`. Full test suite still GREEN. Bootstrapper / RevitCommand initialization order preserved.
</objective>

<execution_context>
@C:/Users/LECG Arquitectura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LECG Arquitectura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cross-cutting-foundation/06-CONTEXT.md
@.planning/phases/06-cross-cutting-foundation/06-RESEARCH.md
@.planning/phases/06-cross-cutting-foundation/06-01-PLAN.md

<!-- The complete Migration Inventory lives in 06-RESEARCH.md "Migration Inventory — Logger.Instance Call Sites" (rows 1–41). Treat that table as the source of truth for scope tags and call-site coordinates. The frontmatter `files_modified` list mirrors that inventory; if the executor uncovers a new call site during the sweep, add it to the list and the Migration Inventory before committing. -->

@src/Services/Infrastructure/Logging/Logger.cs
@src/Core/Bootstrapper.cs
@src/Core/RevitCommand.cs
@src/Core/ServiceLocator.cs

<interfaces>
ILogger (post-Wave 1, current shape):
```csharp
void Log(string message, string scope);
void LogSuccess(string message, string scope);
void LogWarning(string message, string scope, Exception? exception = null);
void LogError(string message, string scope, Exception? exception = null);
void UpdateProgress(string message, double percentage);   // confirm exact signature in Logger.cs
IReadOnlyList<LogEntry> Entries { get; }
event Action<LogEntry>? EntryAdded;
void Clear();
void SetDispatcher(Dispatcher dispatcher);
void ConfigureStructuredLogger(ILoggerFactory factory);
```

ServiceLocator (already in use per Bootstrapper.cs):
```csharp
public static T GetRequiredService<T>() where T : notnull;
public static T? GetService<T>() where T : class;
public static void Initialize(IServiceProvider provider);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate non-RevitCommand services + delete singleton</name>
  <files>
    src/App.cs, src/ViewModels/CategoryChangerViewModel.cs, src/ViewModels/LogViewModel.cs,
    src/ViewModels/FilterCopyViewModel.cs, src/Views/LogView.xaml.cs,
    src/Utilities/ExecutionTimer.cs, src/Services/ElementLabelService.cs,
    src/Services/CadConversion/CadCurveTessellationService.cs,
    src/Services/CadConversion/CadPolylineExtractionService.cs,
    src/Services/FamilyConversion/FamilyGeometryCopyService.cs,
    src/Services/FamilyConversion/FamilyConversionLoggingService.cs,
    src/Services/FamilyConversion/FamilyConversionExecutionService.cs,
    src/Services/FamilyConversion/FamilyEditorService.cs,
    src/Services/FamilyConversion/FamilyConversionService.cs,
    src/Services/FamilyConversion/FamilyProjectLoadService.cs,
    src/Services/FamilyConversion/FamilyTempFileCleanupService.cs,
    src/Services/FamilyConversion/FamilyParameterSetupService.cs,
    src/Services/FamilyConversion/FamilySourceDocumentService.cs,
    src/Services/FamilyConversion/FamilyTargetDocumentService.cs,
    src/Services/FamilyConversion/FamilySaveService.cs,
    src/Services/FamilyConversion/FamilyTemplatePathService.cs,
    src/Services/Renaming/BaseElementCollectionService.cs,
    src/Services/Renaming/BatchRenameExecutionService.cs,
    src/Services/RenderAppearance/RenderAppearanceSingleSyncService.cs,
    src/Services/PurgeAndCompaction/CompactionSharedHelper.cs,
    src/Services/PurgeAndCompaction/LinePatternCompactionService.cs,
    src/Services/PurgeAndCompaction/PurgeContext.cs,
    src/Services/PurgeAndCompaction/PurgeLinePatternService.cs,
    src/Services/PurgeAndCompaction/PurgeReferenceScannerService.cs,
    src/Services/Materials/MaterialBitmapPropertyService.cs,
    src/Services/Infrastructure/SettingsManager.cs
  </files>
  <behavior>
    - Every service file in the Migration Inventory (rows 2–7, 12, 15–40) accepts `ILogger` via its constructor and stores it as `private readonly ILogger _logger`.
    - Every `Logger.Instance.X(...)` call becomes `_logger.X(..., scope: "<ScopeTag>")` with the scope tag taken from the Migration Inventory column.
    - The two `(message, categoryName, Exception?)` overloads previously called by `CadCurveTessellationService` and `SettingsManager` become normal `LogWarning(message, scope, exception)` calls.
    - `LogViewModel.cs` line 30: `_logger = logger ?? Logger.Instance;` → `_logger = logger ?? throw new ArgumentNullException(nameof(logger));`.
    - `LogView.xaml.cs` line 25: `ServiceLocator.GetService<LogViewModel>() ?? new LogViewModel(Logger.Instance)` → `ServiceLocator.GetRequiredService<LogViewModel>()`.
    - DI registration in `Bootstrapper.cs` (Wave 1 left registration line in place) keeps working — services that need `ILogger` already get it because `AddSingleton<ILogger>(_ => Logger.Instance)` is still live. We swap the registration in Task 3 of this plan.
    - After this task, `dotnet build` succeeds with the CS0618 warnings DOWN from ~50 to ONLY the `Logger.Instance` references in RevitCommand / Commands / Bootstrapper (tackled in Task 2 + 3).
  </behavior>
  <action>
    Work file by file. For each entry in the Migration Inventory (RESEARCH "Migration Inventory" rows 2–7, 12, 15–40):

    1. Add `private readonly ILogger _logger;` to the class (or reuse an existing one if present).
    2. Add `ILogger logger` to the constructor (last parameter, no default). Assign `_logger = logger ?? throw new ArgumentNullException(nameof(logger));`.
    3. If the class has multiple constructors, update them all. If a parameterless constructor exists ONLY for designer/serialization, add a `[Obsolete("Use ILogger overload")]` attribute and route it to a chained construction with `null!` would defeat the purpose — instead, audit whether designer support is required; for service classes (not VMs), delete the parameterless ctor.
    4. Replace each `Logger.Instance.X(message)` with `_logger.X(message, scope: "<TAG>")` where `<TAG>` is the column-4 value from the Migration Inventory.
    5. For the special two callers using `(message, categoryName, Exception?)` overloads:
       - `CadCurveTessellationService` line 40: `_logger.LogWarning(message, scope: "CadCurveTessellation", exception: ex)`.
       - `SettingsManager` line 89: `_logger.LogWarning(message, scope: "SettingsManager")` (no exception arg) — confirm the source line to see if an exception is actually passed; pass through if so.

    Pay special attention to the densest files (RESEARCH Risk 3):
    - **`PurgeContext.cs` (~30 call sites)**: many are inside catch blocks with the pattern `Logger.Instance.LogWarning($"[PurgeContext] ...")`. Strip the `[PurgeContext]` prefix from the message AND set scope to `"PurgeContext"` — the scope replaces the embedded prefix.
    - **`FamilyConversionService.cs` (~15 call sites)**: same pattern; strip `[FamilyConversionService]`-style prefixes and use scope `"FamilyConversion"`.
    - **`BatchRenameExecutionService.cs` lines 931–932** (RESEARCH Open Question 3): read these lines BEFORE rewriting to confirm the shape — the grep output reported them as long lines.

    For ViewModels (CategoryChangerViewModel, FilterCopyViewModel): they are resolved via DI per existing ServiceLocator patterns; add `ILogger` to ctor, same pattern.

    For `App.cs` lines 90/107/129: `App` is the WPF Application class and runs before DI is built (line 90 is WPF resource load failure). For these three sites specifically, use `ServiceLocator.GetService<ILogger>()` (null-safe), and if it's null at that point (very early startup), fall back to `System.Diagnostics.Debug.WriteLine`. Document with a comment: `// App-lifecycle exception path; ServiceLocator may not be initialized yet`.

    For `ExecutionTimer.cs` (lines 20, 36): this is a `using`-block timer used inline. Accept `ILogger` as a ctor parameter; every caller must pass their `_logger`.

    The grep pattern to verify per-file completion:
    ```
    grep -n "Logger\.Instance" <file>
    ```
    Each file should return zero matches after its rewrite (except the ones left intentionally for Task 2: RevitCommand, Bootstrapper, and the four Command classes whose `OnDialogShowing` handlers Task 2 / Plan 03 own).

    Commit frequency: commit per logical group to keep diffs reviewable —
    - Commit 1: App.cs + ViewModels + LogView.xaml.cs (UI layer).
    - Commit 2: Utilities + simple services (ExecutionTimer, ElementLabelService, both CadConversion services, the smaller FamilyConversion services).
    - Commit 3: Heavy FamilyConversion files (FamilyConversionService, FamilyEditorService, FamilyConversionLoggingService).
    - Commit 4: PurgeContext + PurgeLinePatternService + CompactionSharedHelper + LinePatternCompactionService + PurgeReferenceScannerService.
    - Commit 5: Renaming + RenderAppearance + Materials + SettingsManager.

    After each commit, run `dotnet test LECG.Tests --filter "Category!=CrossCutting"` to confirm baseline holds.
  </action>
  <verify>
    <automated>dotnet build LECG.sln -c Debug 2>&amp;1 | grep -cE "error|warning CS0618" | head -5</automated>
    Expected: 0 errors. CS0618 warning count drops to ≤ 10 (only RevitCommand, Bootstrapper, and the four Commands left, which Task 2 + Plan 03 handle).
    <automated>grep -rn "Logger\.Instance" src/ --include="*.cs" 2>&amp;1 | grep -vE "(RevitCommand|Bootstrapper|PurgeCommand|ConvertFamilyCommand|FormulaAutoGroupingCommand|SearchReplaceCommand|Logger\.cs)" | head -20</automated>
    Expected: empty output (every non-command/non-bootstrapper reference is gone).
    <automated>dotnet test LECG.Tests 2>&amp;1 | tail -5</automated>
    Expected: 175 baseline + 7 CrossCutting (6 reporter-severity + 1 compile) = ≥182 passed, 0 failed (DialogWhitelist tests still fail to build at this point — they need Plan 03; if the test project fails to build because of them, mark them `[Fact(Skip = ...)]` until Plan 03, but document with a TODO).
  </verify>
  <done>
    All ~30 non-command files migrated. `grep` confirms `Logger.Instance` only remains in RevitCommand.cs, Bootstrapper.cs, and the four Command classes. Build succeeds with CS0618 warning count ≤ 10. Baseline tests + Wave-0 CrossCutting tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate RevitCommand base class + four Command classes via ServiceLocator</name>
  <files>
    src/Core/RevitCommand.cs,
    src/Commands/PurgeCommand.cs,
    src/Commands/ConvertFamilyCommand.cs,
    src/Commands/FormulaAutoGroupingCommand.cs,
    src/Commands/SearchReplaceCommand.cs
  </files>
  <behavior>
    - `RevitCommand` exposes `protected ILogger _logger` populated by `_logger = ServiceLocator.GetRequiredService<ILogger>();` inside `PrepareCommandExecution()` (per RESEARCH Risk 1, recommended Option 1).
    - `RevitCommand.Log(string text)` protected helper becomes `protected void Log(string text) => _logger.Log(text, scope: CommandName)` where `CommandName` is an existing property or `GetType().Name` if no such property exists.
    - `RevitCommand.PrepareCommandExecution()` line 120/121: `Logger.Instance.Clear()` and `Logger.Instance.SetDispatcher(...)` become `_logger.Clear()` and `_logger.SetDispatcher(...)`.
    - `PurgeCommand.OnDialogShowing` (line 41) currently logs via `Logger.Instance.Log(...)`. This whole method is going away in Plan 03 (DialogWhitelist). For THIS task: change the log to `_logger?.Log(...)` if accessible from a static handler; if not (it's a static method), use `ServiceLocator.GetRequiredService<ILogger>()` at the top of the method. Either path is acceptable — Plan 03 replaces it.
    - Same for `ConvertFamilyCommand.OnDialogShowing` (lines 41/53/59/65) and `FormulaAutoGroupingCommand.cs` line 35.
    - `SearchReplaceCommand.cs` line 40 currently passes `Logger.Instance` as an argument to `ExecuteBatchRename(doc, items, Logger.Instance, reporter)`. Replace with `_logger`.
    - `RevitCommandProgressReporter` construction sites left by Plan 01 (PurgeCommand line ~55, ConvertFamilyCommand line ~129) currently pass `Logger.Instance` — change to `_logger`.
  </behavior>
  <action>
    Edit `src/Core/RevitCommand.cs`:
    1. Add `protected ILogger _logger = null!;` field (null-forgiving because it's populated in PrepareCommandExecution, which runs before any command logic).
    2. In `PrepareCommandExecution()` (around line 120), insert as the FIRST line: `_logger = ServiceLocator.GetRequiredService<ILogger>();`.
    3. Change the rest of `PrepareCommandExecution`: `Logger.Instance.Clear()` → `_logger.Clear()`; `Logger.Instance.SetDispatcher(...)` → `_logger.SetDispatcher(...)`.
    4. Change the protected `Log` helper (line 62): `protected void Log(string text) => _logger.Log(text, scope: GetType().Name);`. If `RevitCommand` already exposes a `CommandName` property, use that instead of `GetType().Name`.

    For static dialog handlers (Purge/ConvertFamily/FormulaAutoGrouping): static methods can't access `_logger`. Two patterns are acceptable; pick per command:
    - **Pattern A (preferred for commands surviving to Plan 03):** keep the static method but resolve via ServiceLocator on entry: `var logger = ServiceLocator.GetRequiredService<ILogger>();`. Use it for the existing `Log(...)` calls with explicit scope tag from the Migration Inventory.
    - **Pattern B:** make the handler a non-static lambda captured in an instance field set during `Execute`. More invasive — only use if Pattern A causes test surprise.

    Apply Pattern A to all four commands.

    Edit `src/Commands/SearchReplaceCommand.cs` line 40: replace `Logger.Instance` argument with `_logger`.

    Edit `src/Commands/PurgeCommand.cs` and `src/Commands/ConvertFamilyCommand.cs` `new RevitCommandProgressReporter(Logger.Instance, UpdateProgress)` → `new RevitCommandProgressReporter(_logger, UpdateProgress)`.

    After this task, the only remaining `Logger.Instance` references in `src/` should be in:
    - `src/Services/Infrastructure/Logging/Logger.cs` (the class itself — the static accessor will be deleted in Task 3).
    - `src/Core/Bootstrapper.cs` (registration + ConfigureStructuredLogger — Task 3 deletes).
  </action>
  <verify>
    <automated>grep -rn "Logger\.Instance" src/ --include="*.cs" 2>&amp;1 | grep -vE "(Logger\.cs|Bootstrapper\.cs)" | head -20</automated>
    Expected: empty (every reference outside Logger.cs and Bootstrapper.cs gone).
    <automated>dotnet build LECG.sln -c Debug 2>&amp;1 | grep -E "error" | head -10</automated>
    Expected: 0 errors.
    <automated>dotnet test LECG.Tests 2>&amp;1 | tail -5</automated>
    Expected: full suite green.
  </verify>
  <done>
    `RevitCommand` resolves `ILogger` from `ServiceLocator` in `PrepareCommandExecution`. Four command classes use `_logger` (via base class) or ServiceLocator-in-static-handler. `grep "Logger\.Instance" src/` returns matches ONLY in `Logger.cs` and `Bootstrapper.cs`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delete Logger.Instance + Obsolete overloads + rewire Bootstrapper</name>
  <files>
    src/Services/Infrastructure/Logging/Logger.cs,
    src/Core/Bootstrapper.cs
  </files>
  <behavior>
    - `Logger.Instance` static property DELETED.
    - The two `[Obsolete]` no-scope overloads on the concrete `Logger` class (added in Plan 01 Task 1) DELETED.
    - `Bootstrapper.cs` line 54: `services.AddSingleton<LECG.Services.Logging.ILogger>(_ => Logger.Instance)` → `services.AddSingleton<ILogger, Logger>()`.
    - `Bootstrapper.cs` line 51: `Logger.Instance.ConfigureStructuredLogger(loggerFactory)` — moves to AFTER `BuildServiceProvider()` so it operates on the registered singleton instance. Resolve via `_provider.GetRequiredService<ILogger>().ConfigureStructuredLogger(loggerFactory)`.
    - `Bootstrapper.cs` line 34: `Logger.Instance.LogWarning(...)` startup warning — this runs BEFORE the DI container exists. Options:
      a. Defer the log: enqueue the message into a local `List<string>` and replay it after `_provider` exists.
      b. Construct a transient `Logger` instance just for the warning, then replay/transfer entries to the DI-registered instance.
      c. Inline `System.Diagnostics.Debug.WriteLine(...)` for this single pre-DI message.
      Pick (a) — list-then-replay. It preserves the entry in LogView post-startup.
    - Phase exit grep gate: `grep -rn "Logger\.Instance" src/` returns ZERO matches.
  </behavior>
  <action>
    Edit `src/Services/Infrastructure/Logging/Logger.cs`:
    1. Delete the `private static Logger? _instance;` field and the `public static Logger Instance => _instance ??= new Logger();` property.
    2. Delete the temporary `[Obsolete]` no-scope overloads added in Plan 01 Task 1.
    3. Confirm the constructor remains parameterless (RESEARCH Risk 2 — Logger must be constructable without args because `ConfigureStructuredLogger` runs post-build).

    Edit `src/Core/Bootstrapper.cs`:
    1. Line 54: `services.AddSingleton<ILogger, Logger>();`.
    2. Restructure `Initialize()`:
       ```csharp
       // Pre-DI: capture startup warnings into a buffer
       var startupBuffer = new List<(string msg, LogLevel level)>();
       if (someStartupCondition)
           startupBuffer.Add(("Startup warning text", LogLevel.Warning));

       // ... existing service registration ...
       services.AddSingleton<ILogger, Logger>();
       // ... rest of registrations ...

       _provider = services.BuildServiceProvider();
       ServiceLocator.Initialize(_provider);

       // Post-DI: configure structured logger and flush buffered entries
       var logger = _provider.GetRequiredService<ILogger>();
       logger.ConfigureStructuredLogger(loggerFactory);
       foreach (var (msg, level) in startupBuffer)
       {
           if (level == LogLevel.Warning) logger.LogWarning(msg, scope: "Bootstrapper");
           else logger.Log(msg, scope: "Bootstrapper");
       }
       ```
       Match the existing formatting / naming conventions in Bootstrapper.cs; the above is shape-only.

    After these edits, run the phase exit gate.
  </action>
  <verify>
    <automated>grep -rn "Logger\.Instance" src/ --include="*.cs" 2>&amp;1 | head -5</automated>
    Expected: empty output. This is the CROSS-01 phase-exit gate.
    <automated>dotnet build LECG.sln -c Debug 2>&amp;1 | grep -cE "(error|warning CS0618)" | head -5</automated>
    Expected: 0 errors, 0 CS0618 warnings (the Obsolete overloads they referenced are deleted along with their last callers).
    <automated>dotnet test LECG.Tests 2>&amp;1 | tail -5</automated>
    Expected: 175 baseline + Wave-0 CrossCutting tests still GREEN.
  </verify>
  <done>
    `Logger.Instance` deleted. `[Obsolete]` overloads deleted. Bootstrapper registers `Logger` as `ILogger` and calls `ConfigureStructuredLogger` post-build. Startup warning buffer drains into the registered logger. `grep "Logger\.Instance" src/` returns empty. Full test suite green.
  </done>
</task>

</tasks>

<verification>
- [ ] `grep -rn "Logger\.Instance" src/` returns ZERO matches (phase-exit gate per CONTEXT §1.4).
- [ ] `dotnet build` returns 0 errors, 0 CS0618 warnings.
- [ ] `dotnet test LECG.Tests` returns 175 baseline + Wave-0 CrossCutting tests passing.
- [ ] Every file in `files_modified` no longer references `Logger.Instance`.
- [ ] `RevitCommand._logger` is populated via `ServiceLocator.GetRequiredService<ILogger>()` in `PrepareCommandExecution`.
- [ ] Bootstrapper's startup warning (line 34) is preserved and surfaces in LogView post-DI.
</verification>

<success_criteria>
Phase Goal SC #1 fully met after this plan ships: every plugin log entry routes through the single `ILogger` interface (severity + scope) and lands in `LogView` with severity preserved (combined with Plan 01's reporter fix). Singleton hard-deletion complete.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cross-cutting-foundation/06-02-SUMMARY.md`
</output>
