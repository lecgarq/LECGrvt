---
phase: 06-cross-cutting-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - LECG.Tests/Services/Logging/LoggerSeverityTests.cs
  - LECG.Tests/Core/DialogWhitelistTests.cs
  - .planning/phases/06-cross-cutting-foundation/06-DIALOG-DISCOVERY.md
autonomous: false
requirements: [CROSS-01, CROSS-02, CROSS-03]
must_haves:
  truths:
    - "LoggerSeverityTests.cs exists and contains failing tests for ILogger scope parameter + per-impl severity preservation"
    - "DialogWhitelistTests.cs exists and contains failing tests for hit/miss behavior + log policy"
    - "06-DIALOG-DISCOVERY.md captures every DialogId emitted by Purge and Convert Family against a representative dirty model"
  artifacts:
    - path: "LECG.Tests/Services/Logging/LoggerSeverityTests.cs"
      provides: "Wave-0 RED tests for CROSS-01 + CROSS-02"
      contains: "[Category(\"CrossCutting\")]"
      min_lines: 60
    - path: "LECG.Tests/Core/DialogWhitelistTests.cs"
      provides: "Wave-0 RED tests for CROSS-03"
      contains: "[Category(\"CrossCutting\")]"
      min_lines: 40
    - path: ".planning/phases/06-cross-cutting-foundation/06-DIALOG-DISCOVERY.md"
      provides: "Captured DialogIds from runtime observation; input to Wave 3 whitelist"
  key_links:
    - from: "LoggerSeverityTests.cs"
      to: "ILogger (new scope param contract)"
      via: "compile reference"
      pattern: "ILogger.*scope:"
    - from: "06-DIALOG-DISCOVERY.md"
      to: "06-03 DialogWhitelist task"
      via: "captured DialogIds drive whitelist entries"
      pattern: "DialogId="
---

<objective>
Lay the validation foundation for Phase 6: write the RED tests that every later wave must turn GREEN, and capture the live `DialogId` enumeration the dialog-whitelist plan needs.

Purpose: Without these tests in place first, Waves 1–3 cannot verify they advance CROSS-01/02/03. Without the DialogId capture, Wave 3 has only LOW-confidence guesses for whitelist contents.
Output: Two failing xUnit test files under `[Category("CrossCutting")]`, plus a markdown discovery report enumerating real DialogIds.
</objective>

<execution_context>
@C:/Users/LECG Arquitectura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LECG Arquitectura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-cross-cutting-foundation/06-CONTEXT.md
@.planning/phases/06-cross-cutting-foundation/06-RESEARCH.md
@.planning/phases/06-cross-cutting-foundation/06-VALIDATION.md

@src/Services/Infrastructure/Logging/Logger.cs
@src/Services/Infrastructure/IProgressReporter.cs
@src/Services/Infrastructure/LegacyProgressReporter.cs
@src/Services/Infrastructure/RevitCommandProgressReporter.cs
@src/Commands/PurgeCommand.cs
@src/Commands/ConvertFamilyCommand.cs

<interfaces>
<!-- Current ILogger contract (will be EXTENDED in Wave 1; tests reference the future shape). -->
From src/Services/Infrastructure/Logging/Logger.cs:
```csharp
public interface ILogger
{
    void Log(string message);
    void LogSuccess(string message);
    void LogWarning(string message);
    void LogError(string message);
    // Wave 1 will add `string scope` as a required parameter on each method above.
    IReadOnlyList<LogEntry> Entries { get; }      // observable list
    event Action<LogEntry>? EntryAdded;
}

public sealed record LogEntry(string Message, LogLevel Level, DateTime Timestamp, string? Scope = null);
public enum LogLevel { Info, Success, Warning, Error }
```

From src/Services/Infrastructure/IProgressReporter.cs:
```csharp
public interface IProgressReporter
{
    void Report(string message, double percentage);
    void Log(string message);
    void LogWarning(string message);
    void LogError(string message);
}
```

From Autodesk.Revit.UI.Events:
```csharp
public class DialogBoxShowingEventArgs : EventArgs
{
    public string? DialogId { get; }
    public void OverrideResult(int result);
}
public class TaskDialogShowingEventArgs : DialogBoxShowingEventArgs
{
    public string Message { get; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LoggerSeverityTests.cs (RED tests for CROSS-01 + CROSS-02)</name>
  <files>LECG.Tests/Services/Logging/LoggerSeverityTests.cs</files>
  <behavior>
    - Compile-only test: `ILogger.Log(string, string scope)` accepts the new scope parameter (will fail to compile until Wave 1 lands the contract change — that is the intended RED state).
    - Compile-only test: `ILogger.LogWarning(string, string scope)` accepts scope; same for LogError, LogSuccess.
    - Behavior test (RevitCommandProgressReporter): given `var logger = new Logger(); var r = new RevitCommandProgressReporter(logger, (_,_)=>{});` then `r.LogWarning("x")` causes `logger.Entries` to contain exactly one entry with `Level == LogLevel.Warning`.
    - Behavior test (RevitCommandProgressReporter): same flow for `LogError` → `LogLevel.Error`.
    - Behavior test (LegacyProgressReporter): `new LegacyProgressReporter(logger).LogWarning("x")` → entry with `LogLevel.Warning`.
    - Behavior test (LegacyProgressReporter): `LogError` → `LogLevel.Error`.
    - Behavior test (SimpleProgressReporter): `new SimpleProgressReporter(logger).LogWarning("x")` → entry with `LogLevel.Warning`.
    - Behavior test (SimpleProgressReporter): `LogError` → `LogLevel.Error`.
    - All test classes/methods carry `[Trait("Category","CrossCutting")]` (xUnit equivalent of `[Category]`).
  </behavior>
  <action>
    Create `LECG.Tests/Services/Logging/LoggerSeverityTests.cs`. Use xUnit `[Fact]` + `[Trait("Category","CrossCutting")]` to match the existing `LECG.Tests` convention (mirror `LECG.Tests/Commands/FormulaAutoGroupingCommandTests.cs` for trait style — read it once if unsure).

    Structure:
    - One class per impl: `RevitCommandProgressReporter_SeverityTests`, `LegacyProgressReporter_SeverityTests`, `SimpleProgressReporter_SeverityTests`.
    - Plus a class `Logger_ScopeParameterTests` whose tests assert the new scope parameter is accepted (a single test that compiles `_logger.LogWarning("x", scope: "Test")` is enough — it will produce a build error until Wave 1, which is the intended RED).
    - Each behavior test follows arrange-act-assert: construct concrete `Logger` (no DI container needed), construct reporter against it, call `LogWarning`/`LogError`, assert `logger.Entries.Single().Level` matches.

    Tests are EXPECTED TO BE RED at this wave (either failing to compile because `scope:` parameter doesn't exist yet, or failing assertions because impls still collapse severity). Document this in a class-level `// EXPECTED RED until Wave 1` comment.

    Do NOT modify any production code in this task. Pure test scaffolding.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests --filter "Category=CrossCutting" --no-restore 2>&amp;1 | grep -E "(Failed|Passed|error)" | head -20</automated>
    Expected outcome: build errors mentioning missing `scope` parameter on `ILogger.LogWarning/Error/Log`, OR if the build succeeds, test failures asserting severity preservation. EITHER state confirms the tests are wired correctly. A clean PASS at this stage means the tests are not actually exercising the contract — investigate.
  </verify>
  <done>
    File exists at `LECG.Tests/Services/Logging/LoggerSeverityTests.cs`, contains all 8 behavior tests + 1 scope-parameter compile test, all carry `[Trait("Category","CrossCutting")]`, and `dotnet test --filter "Category=CrossCutting"` reports the new tests as failing (build error or red assert). Existing 175 baseline tests still pass: `dotnet test LECG.Tests --filter "Category!=CrossCutting"` returns 175 passed / 5 skipped.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DialogWhitelistTests.cs (RED tests for CROSS-03)</name>
  <files>LECG.Tests/Core/DialogWhitelistTests.cs</files>
  <behavior>
    - Compile test: `DialogWhitelist.Apply(eventArgs, logger)` exists with that signature (RED until Wave 3 creates the class).
    - Behavior test (whitelist hit): given a whitelist entry `("TaskDialog_Overwrite", 1)`, when `Apply` is called with an args whose `DialogId == "TaskDialog_Overwrite"`, then `OverrideResult` is invoked with `1` AND `logger.Entries` contains one `Info`-level entry whose message references the DialogId.
    - Behavior test (whitelist miss / reach-user): given a `DialogId` not in the whitelist (e.g. `"TaskDialog_Unknown"`), when `Apply` is called, then `OverrideResult` is NOT invoked AND `logger.Entries` contains one `Warning`-level entry whose message references the unhandled DialogId.
    - Behavior test (null DialogId): args with `DialogId == null` → treated as miss → reach-user, Warning logged.
    - Behavior test (case sensitivity): "TaskDialog_Overwrite" in whitelist, args.DialogId = "taskdialog_overwrite" → miss (exact string match per CONTEXT §3.2).
  </behavior>
  <action>
    Create `LECG.Tests/Core/DialogWhitelistTests.cs` with `[Trait("Category","CrossCutting")]`.

    Because `DialogBoxShowingEventArgs.OverrideResult` is a method on a sealed Revit type that cannot be subclassed in unit tests, define a small test seam: introduce an `IDialogOverride` adapter abstracting `OverrideResult(int)` and have `DialogWhitelist.Apply` take `(string? dialogId, IDialogOverride sink, ILogger logger)` as its primary overload. Wave 3 will add the convenience overload that takes `DialogBoxShowingEventArgs`. Use a `RecordingDialogOverride` fake (single int property capturing the last result, plus a `wasCalled` flag) in tests.

    All tests RED expected:
    - Missing `DialogWhitelist` class → build error.
    - Missing `IDialogOverride` → build error.

    Do NOT create the production class in this task. Pure test scaffolding.
  </action>
  <verify>
    <automated>dotnet test LECG.Tests --filter "Category=CrossCutting" --no-restore 2>&amp;1 | grep -E "(DialogWhitelist|error|Failed)" | head -20</automated>
    Expected: build errors referencing missing `DialogWhitelist` / `IDialogOverride` types. Same RED-is-correct logic as Task 1.
  </verify>
  <done>
    File exists at `LECG.Tests/Core/DialogWhitelistTests.cs`, contains 5 behavior tests (hit, miss, null-id, case-sensitivity, and the compile-only scope check), all carry `[Trait("Category","CrossCutting")]`, and `dotnet test --filter "Category=CrossCutting"` reports them as failing (build error). Baseline tests unaffected.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: DialogId runtime discovery (Purge + Convert Family on dirty model)</name>
  <files>.planning/phases/06-cross-cutting-foundation/06-DIALOG-DISCOVERY.md</files>
  <action>See <how-to-verify> below — this is a checkpoint task requiring human-driven Revit interaction. Steps 1–8 in <how-to-verify> are the action: patch both dialog handlers in logging-only mode, build, run Purge + Convert Family against a dirty model, transcribe captured DialogIds into 06-DIALOG-DISCOVERY.md, revert the patch, commit the markdown.</action>
  <verify><automated>test -f .planning/phases/06-cross-cutting-foundation/06-DIALOG-DISCOVERY.md &amp;&amp; grep -cE "^|" .planning/phases/06-cross-cutting-foundation/06-DIALOG-DISCOVERY.md</automated> Expected: file exists with at least the table header rows (count &gt;= 4: two table headers + at least 2 data rows). If the operator was blocked (no Revit / no representative model), the file may exist with a blocker note and zero data rows — that is acceptable but must be documented in the resume signal.</verify>
  <done>06-DIALOG-DISCOVERY.md exists at the canonical path with either (a) at least one captured DialogId per command (Purge + ConvertFamily) OR (b) an explicit blocker note explaining why discovery could not be performed. Temporary diagnostic patch reverted: grep -rn "DISCOVERY-PURGE|DISCOVERY-CONVERT" src/ returns empty.</done>
  <what-built>
    A logging-only branch of `PurgeCommand.OnDialogShowing` and `ConvertFamilyCommand.OnDialogShowing` that records `DialogId` + message for every `DialogBoxShowing` event WITHOUT calling `OverrideResult`. Run both commands against a representative dirty model, capture every emitted `DialogId`.
  </what-built>
  <how-to-verify>
    Step 1 — temporarily patch both handlers (do NOT commit this patch):

    In `src/Commands/PurgeCommand.cs::OnDialogShowing` replace the body with:
    ```csharp
    string dialogId = e.DialogId ?? "(null)";
    string message = e is TaskDialogShowingEventArgs td ? td.Message : "(non-task)";
    Logger.Instance.LogWarning($"[DISCOVERY-PURGE] DialogId='{dialogId}' Message='{message.Replace("\n"," | ")}'");
    // intentionally NOT calling e.OverrideResult — dialog reaches user
    ```

    In `src/Commands/ConvertFamilyCommand.cs::OnDialogShowing` apply the same pattern with tag `[DISCOVERY-CONVERT]`.

    Step 2 — build and load the plugin into Revit 2026:
    ```
    dotnet build -c Debug
    ```
    Then start Revit, load the addin from the build output.

    Step 3 — open a representative dirty model (any LECG test family with many unused styles / line patterns / unloaded family references). If no canned test model exists, use any production project with > 50 elements.

    Step 4 — run Purge Unused in Deep mode. Whenever a dialog appears, READ the dialog body, then dismiss it manually. Take a screenshot per dialog. Repeat until purge completes or you've gathered ≥ 5 distinct DialogIds.

    Step 5 — run Convert Family on a family that already exists in the project (to trigger the "already exists / overwrite" path). Capture dialogs.

    Step 6 — copy every `[DISCOVERY-PURGE]` and `[DISCOVERY-CONVERT]` line from LogView (or the LECG log file) into a new markdown file:

    Create `.planning/phases/06-cross-cutting-foundation/06-DIALOG-DISCOVERY.md` with this shape:
    ```
    ---
    phase: 06-cross-cutting-foundation
    captured: <ISO date>
    model: <model name / "synthetic test project">
    revit_version: 2026
    ---

    # Phase 6 — DialogId Discovery

    ## Purge (Deep mode)

    | DialogId | Message (truncated) | Proposed OverrideResult | Rationale |
    |----------|---------------------|-------------------------|-----------|
    | (fill from discovery output) | | | |

    ## Convert Family

    | DialogId | Message (truncated) | Proposed OverrideResult | Rationale |
    |----------|---------------------|-------------------------|-----------|
    | | | | |

    ## Unmapped / let-reach-user

    DialogIds observed but deliberately NOT whitelisted, with a one-line rationale each.
    ```

    Step 7 — `git checkout -- src/Commands/PurgeCommand.cs src/Commands/ConvertFamilyCommand.cs` to revert the temporary patch. Verify with `git status` that only the new markdown file is left.

    Step 8 — commit the markdown file only: `git add .planning/phases/06-cross-cutting-foundation/06-DIALOG-DISCOVERY.md && git commit -m "docs(phase-06): capture DialogId enumeration from runtime discovery"`.
  </how-to-verify>
  <resume-signal>
    Type "captured" with the count of distinct DialogIds discovered (e.g., "captured: 7 Purge, 3 Convert"), or describe what blocked the capture (no Revit access, no representative model, etc.). If blocked, the planner will need to defer Wave 3 OR Wave 3's whitelist will ship with the LOW-confidence guesses from RESEARCH.md.
  </resume-signal>
</task>

</tasks>

<verification>
- [ ] `LoggerSeverityTests.cs` and `DialogWhitelistTests.cs` exist at the paths listed in `files_modified` and carry `[Trait("Category","CrossCutting")]`.
- [ ] `dotnet test LECG.Tests --filter "Category=CrossCutting"` returns RED (build error or failed asserts) — this is the intended Wave 0 state.
- [ ] `dotnet test LECG.Tests --filter "Category!=CrossCutting"` returns 175 passed / 5 skipped (no regression).
- [ ] `06-DIALOG-DISCOVERY.md` exists with at least the Purge table populated, OR a documented blocker explains why discovery was skipped.
- [ ] Temporary discovery patch reverted: `grep -rn "DISCOVERY-PURGE\|DISCOVERY-CONVERT" src/` returns empty.
</verification>

<success_criteria>
1. RED tests for CROSS-01 / CROSS-02 / CROSS-03 exist and run under the `CrossCutting` category.
2. Real-world DialogIds from Purge + Convert Family captured (or blocker documented).
3. No regression in the 175-test baseline.
4. No production-code changes committed in this wave.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cross-cutting-foundation/06-00-SUMMARY.md`
</output>
