<base:LecgWindow x:Class="LECG.Batch.Views.BatchProcessView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:LECG.Views.Base"
    xmlns:icons="clr-namespace:LECG.Utils"
    Title="Batch Cloud Process"
    Width="700" Height="600" MinWidth="550" MinHeight="450"
    UseLayoutRounding="True" SnapsToDevicePixels="True"
    WindowIcon="{x:Static icons:Icons.Home}">

    <base:LecgWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LECG;component/src/Resources/Themes/LecgTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        </ResourceDictionary>
    </base:LecgWindow.Resources>

    <Grid Margin="{DynamicResource ViewContentMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="{DynamicResource HeaderMarginSmall}">
            <TextBlock Text="Batch Cloud Process" FontWeight="{DynamicResource WeightBold}"
                       FontSize="{DynamicResource FontSizeTitle}" Foreground="{DynamicResource LecgTextPrimary}"
                       Margin="{DynamicResource TitleMarginSmall}"/>
            <TextBlock Text="Open, process, sync, and optionally publish multiple cloud Revit models in sequence."
                       FontSize="{DynamicResource FontSizeSmall}" Foreground="{DynamicResource LecgTextSecondary}" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Auth + Options Bar -->
        <Grid Grid.Row="1" Margin="0,8,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Sign In"
                    Style="{DynamicResource ModernButtonStyle}"
                    Command="{Binding SignInCommand}"
                    Visibility="{Binding IsSignedIn, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}"/>
            <Button Grid.Column="0" Content="Sign Out"
                    Style="{DynamicResource ModernButtonSecondaryStyle}"
                    Command="{Binding SignOutCommand}"
                    Visibility="{Binding IsSignedIn, Converter={StaticResource BoolToVis}}"/>

            <Button Grid.Column="2" Content="Browse Models"
                    Style="{DynamicResource ModernButtonStyle}"
                    Click="OnBrowseModelsClick"
                    IsEnabled="{Binding IsSignedIn}"/>

            <CheckBox Grid.Column="4" Content="Publish after sync"
                      IsChecked="{Binding PublishAfterSync}"
                      Style="{DynamicResource ModernCheckboxStyle}"
                      VerticalAlignment="Center"/>
        </Grid>

        <!-- Job Queue -->
        <Border Grid.Row="2" BorderBrush="{DynamicResource LecgBorderLight}" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Column headers -->
                <Grid Grid.Row="0" Background="{DynamicResource LecgSurface}" Margin="8,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Model" Style="{DynamicResource SectionHeaderStyle}"/>
                    <TextBlock Grid.Column="1" Text="Status" Style="{DynamicResource SectionHeaderStyle}"/>
                    <TextBlock Grid.Column="2" Text="Type" Style="{DynamicResource SectionHeaderStyle}"/>
                    <TextBlock Grid.Column="3" Text="Retries" Style="{DynamicResource SectionHeaderStyle}"/>
                </Grid>

                <!-- Job rows -->
                <ListView Grid.Row="1" ItemsSource="{Binding Jobs}" BorderThickness="0">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="8,4"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding DisplayName}"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding ErrorMessage}"/>
                                <TextBlock Grid.Column="1" Text="{Binding Status}"
                                           Foreground="{DynamicResource LecgAccent}"/>
                                <TextBlock Grid.Column="2" Text="{Binding ModelType}"
                                           FontSize="{DynamicResource FontSizeSmall}"/>
                                <TextBlock Grid.Column="3" Text="{Binding RetryCount}"
                                           FontSize="{DynamicResource FontSizeSmall}"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- Status -->
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}"
                   FontSize="{DynamicResource FontSizeSmall}"
                   Foreground="{DynamicResource LecgTextSecondary}"
                   Margin="0,6,0,4" TextWrapping="Wrap"/>

        <!-- Control Buttons -->
        <Grid Grid.Row="4" Margin="{DynamicResource ButtonRowMargin}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Start"
                    Style="{DynamicResource ModernButtonStyle}"
                    Command="{Binding StartBatchCommand}"/>
            <Button Grid.Column="2" Content="Pause"
                    Style="{DynamicResource ModernButtonSecondaryStyle}"
                    Command="{Binding PauseBatchCommand}"/>
            <Button Grid.Column="4" Content="Resume"
                    Style="{DynamicResource ModernButtonStyle}"
                    Command="{Binding ResumeBatchCommand}"/>
            <Button Grid.Column="6" Content="Cancel"
                    Style="{DynamicResource ModernButtonSecondaryStyle}"
                    Command="{Binding CancelBatchCommand}"/>
        </Grid>
    </Grid>
</base:LecgWindow>
