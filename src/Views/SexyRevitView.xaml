<base:LecgWindow x:Class="LECG.Views.SexyRevitView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:LECG.Views.Base"
    xmlns:utils="clr-namespace:LECG.Utils"
    xmlns:icons="clr-namespace:LECG.Utils" Title="SEXY REVIT" Width="400" WindowIcon="{x:Static icons:Icons.Sparkles}" WindowStartupLocation="CenterScreen" SizeToContent="Height">

    <base:LecgWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LECG;component/src/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </base:LecgWindow.Resources>

    <Grid Margin="{StaticResource ViewContentMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="{DynamicResource HeaderMarginSmall}">
            <TextBlock Text="View Beautification" FontWeight="{StaticResource WeightBold}" FontSize="{StaticResource FontSizeTitle}" Foreground="{StaticResource TextPrimaryBrush}" Margin="{StaticResource TitleMarginSmall}"/>
            <TextBlock Text="Configure visual settings for a stunning view." FontSize="{StaticResource FontSizeSmall}" Foreground="{StaticResource TextSecondaryBrush}"/>
        </StackPanel>

        <!-- Options -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="{StaticResource HeaderMarginSmall}" Padding="0,0,4,0">
            <StackPanel>
                <!-- Graphics Section -->
                <Border Style="{DynamicResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="GRAPHICS" Style="{DynamicResource CardHeaderStyle}"/>
                        <CheckBox Content="Consistent Colors (Flat shading)" IsChecked="{Binding UseConsistentColors, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="{StaticResource CheckboxSpacingMedium}"/>
                        <CheckBox Content="Smooth Lines (Anti-aliasing)" IsChecked="{Binding UseSmoothLines, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="{StaticResource CheckboxSpacingMedium}"/>
                        <CheckBox Content="Detail Level: Fine" IsChecked="{Binding UseDetailFine, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Hide Elements Section -->
                <Border Style="{DynamicResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="HIDE ELEMENTS" Style="{DynamicResource CardHeaderStyle}"/>
                        <CheckBox Content="Levels" IsChecked="{Binding HideLevels, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="{StaticResource CheckboxSpacingSmall}"/>
                        <CheckBox Content="Grids" IsChecked="{Binding HideGrids, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="0,0,0,8"/>
                        <CheckBox Content="Reference Points" IsChecked="{Binding HideRefPoints, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="0,0,0,8"/>
                        <CheckBox Content="Scope Box" IsChecked="{Binding HideScopeBox, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="0,0,0,8"/>
                        <CheckBox Content="Section Box" IsChecked="{Binding HideSectionBox, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Sun Section -->
                <Border Style="{DynamicResource CardStyle}" Margin="{StaticResource CardMarginNone}">
                    <StackPanel>
                        <TextBlock Text="LIGHTING (3D VIEWS)" Style="{DynamicResource CardHeaderStyle}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                            <Path Data="{x:Static icons:Icons.Sun}" Fill="{StaticResource PrimaryAccentBrush}" Width="{StaticResource IconSizeMedium}" Height="{StaticResource IconSizeMedium}" Stretch="Uniform" Margin="{StaticResource IconMarginRight}" VerticalAlignment="Center"/>
                            <CheckBox Content="Configure Sun (Still Image, Noon)" IsChecked="{Binding ConfigureSun, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="{StaticResource ButtonRowMarginSmall}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Cancel" Grid.Column="0" Height="{StaticResource ButtonHeightMedium}" Click="Cancel_Click" Style="{DynamicResource SecondaryButtonStyle}"/>

            <Button Grid.Column="2" Height="{StaticResource ButtonHeightMedium}" Click="Apply_Click" Style="{DynamicResource PrimaryButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{x:Static icons:Icons.Sparkles}" Stroke="White" StrokeThickness="1.5" Width="{StaticResource IconSizeButton}" Height="{StaticResource IconSizeButton}" Stretch="Uniform" Margin="0,0,8,0"/>
                    <TextBlock Text="Apply" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</base:LecgWindow>
