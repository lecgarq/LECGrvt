<base:LecgWindow x:Class="LECG.Views.SexyRevitView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:LECG.Views.Base"
    xmlns:utils="clr-namespace:LECG.Utils"
    xmlns:icons="clr-namespace:LECG.Utils" Title="SEXY REVIT" Width="400" Height="550" MinWidth="350" MinHeight="450" UseLayoutRounding="True" SnapsToDevicePixels="True" WindowIcon="{x:Static icons:Icons.Sparkles}">

    <base:LecgWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LECG;component/src/Resources/Themes/LecgTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </base:LecgWindow.Resources>

    <Grid Margin="{DynamicResource ViewContentMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="{DynamicResource HeaderMarginSmall}">
            <Grid>
                <TextBlock Text="View Beautification" FontWeight="{DynamicResource WeightBold}" FontSize="{DynamicResource FontSizeTitle}" Foreground="{DynamicResource LecgTextPrimary}" Margin="{DynamicResource TitleMarginSmall}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Check All" Command="{Binding CheckAllCommand}" Style="{DynamicResource LinkButtonStyle}" Margin="0,0,10,0"/>
                    <Button Content="Uncheck All" Command="{Binding UncheckAllCommand}" Style="{DynamicResource LinkButtonStyle}"/>
                </StackPanel>
            </Grid>
            <TextBlock Text="Configure visual settings for a stunning view." FontSize="{DynamicResource FontSizeSmall}" Foreground="{DynamicResource LecgTextSecondary}"/>
        </StackPanel>

        <!-- Options -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="{DynamicResource HeaderMarginSmall}" Padding="0,0,4,0">
            <StackPanel>
                <!-- Graphics Section -->
                <Border Padding="16" Margin="{DynamicResource HeaderMarginSmall}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Border Width="3" Height="12" CornerRadius="1.5" Background="{DynamicResource LecgAccent}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="GRAPHICS" Style="{DynamicResource SectionHeaderStyle}" Margin="0"/>
                        </StackPanel>
                        <CheckBox Content="Consistent Colors (Flat shading)" IsChecked="{Binding UseConsistentColors, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="{DynamicResource CheckboxSpacingMedium}"/>
                        <CheckBox Content="Smooth Lines (Anti-aliasing)" IsChecked="{Binding UseSmoothLines, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="{DynamicResource CheckboxSpacingMedium}"/>
                        <CheckBox Content="Detail Level: Fine" IsChecked="{Binding UseDetailFine, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Hide Elements Section -->
                <Border Padding="16" Margin="{DynamicResource HeaderMarginSmall}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Border Width="3" Height="12" CornerRadius="1.5" Background="{DynamicResource LecgAccent}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="HIDE ELEMENTS" Style="{DynamicResource SectionHeaderStyle}" Margin="0"/>
                        </StackPanel>
                        <CheckBox Content="Levels" IsChecked="{Binding HideLevels, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="{DynamicResource CheckboxSpacingSmall}"/>
                        <CheckBox Content="Grids" IsChecked="{Binding HideGrids, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="0,0,0,8"/>
                        <CheckBox Content="Reference Points" IsChecked="{Binding HideRefPoints, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="0,0,0,8"/>
                        <CheckBox Content="Scope Box" IsChecked="{Binding HideScopeBox, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="0,0,0,8"/>
                        <CheckBox Content="Section Box" IsChecked="{Binding HideSectionBox, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Sun Section -->
                <Border Padding="16" Margin="{DynamicResource HeaderMarginSmall}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Border Width="3" Height="12" CornerRadius="1.5" Background="{DynamicResource LecgAccent}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="LIGHTING (3D VIEWS)" Style="{DynamicResource SectionHeaderStyle}" Margin="0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                            <Path Data="{x:Static icons:Icons.Sun}" Fill="{DynamicResource LecgAccent}" Width="{DynamicResource IconSizeMedium}" Height="{DynamicResource IconSizeMedium}" Stretch="Uniform" Margin="{DynamicResource IconMarginRight}" VerticalAlignment="Center"/>
                            <CheckBox Content="Configure Sun (Still Image, Noon)" IsChecked="{Binding ConfigureSun, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="{DynamicResource ButtonRowMarginSmall}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Cancel" Grid.Column="0" Height="{DynamicResource ButtonHeight}" Command="{Binding CancelCommand}" IsCancel="True" Style="{DynamicResource SecondaryButtonStyle}"/>

            <Button Grid.Column="2" Height="{DynamicResource ButtonHeight}" Command="{Binding ApplyCommand}" Style="{DynamicResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{x:Static icons:Icons.Sparkles}" Stroke="White" StrokeThickness="1.5" Width="{DynamicResource IconSizeButton}" Height="{DynamicResource IconSizeButton}" Stretch="Uniform" Margin="0,0,8,0"/>
                    <TextBlock Text="Apply" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</base:LecgWindow>

