<base:LecgWindow x:Class="LECG.Views.AlignEdgesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:LECG.Views.Base"
    xmlns:utils="clr-namespace:LECG.Utils"
    xmlns:icons="clr-namespace:LECG.Utils"
    xmlns:comp="clr-namespace:LECG.Views.Components" Title="Align Edges" Width="420" WindowIcon="{x:Static icons:Icons.Contours}">

    <base:LecgWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LECG;component/src/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </base:LecgWindow.Resources>

    <Grid Margin="{DynamicResource ViewContentMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="{DynamicResource HeaderMarginSmall}">
            <TextBlock Text="Align Toposolid Edges" FontWeight="{DynamicResource WeightBold}" FontSize="{DynamicResource FontSizeTitle}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="{DynamicResource TitleMarginSmall}"/>
            <TextBlock Text="Project target points onto the reference surface." FontSize="{DynamicResource FontSizeSmall}" Foreground="{DynamicResource TextSecondaryBrush}"/>
        </StackPanel>

        <!-- Selection Cards -->
        <StackPanel Grid.Row="1" Margin="{DynamicResource HeaderMarginSmall}">
            <!-- Target Selection -->
            <Border Style="{DynamicResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="1. TARGETS" Style="{DynamicResource LabelStyle}" Margin="0,0,0,4" Opacity="0.5"/>
                    <comp:SelectionControl DataContext="{Binding TargetsSelection}"/>
                </StackPanel>
            </Border>

            <!-- Reference Selection -->
            <Border Style="{DynamicResource CardStyle}" Margin="{DynamicResource CardMarginNone}">
                <StackPanel>
                    <TextBlock Text="2. REFERENCE" Style="{DynamicResource LabelStyle}" Margin="0,0,0,4" Opacity="0.5"/>
                    <comp:SelectionControl DataContext="{Binding ReferenceSelection}"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Actions -->
        <Grid Grid.Row="2" Margin="{DynamicResource ButtonRowMarginSmall}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Cancel" Grid.Column="0" Height="{DynamicResource ButtonHeightMedium}" Command="{Binding CancelCommand}" Style="{DynamicResource SecondaryButtonStyle}"/>

            <Button Grid.Column="2" Height="{DynamicResource ButtonHeightMedium}" Command="{Binding ApplyCommand}" IsEnabled="{Binding CanRun}" Style="{DynamicResource PrimaryButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{x:Static icons:Icons.ArrowUpDown}" Stroke="White" StrokeThickness="1.5" Width="{DynamicResource IconSizeButton}" Height="{DynamicResource IconSizeButton}" Stretch="Uniform" Margin="0,0,8,0"/>
                    <TextBlock Text="Run" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</base:LecgWindow>

