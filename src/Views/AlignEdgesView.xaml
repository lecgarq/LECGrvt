<base:LecgWindow x:Class="LECG.Views.AlignEdgesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:LECG.Views.Base"
    xmlns:utils="clr-namespace:LECG.Utils"
    xmlns:icons="clr-namespace:LECG.Utils"
    xmlns:comp="clr-namespace:LECG.Views.Components" Title="Align Edges" Width="420" WindowStartupLocation="CenterScreen" SizeToContent="Height" WindowIcon="{x:Static icons:Icons.ArrowUpDown}">

    <base:LecgWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LECG;component/src/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </base:LecgWindow.Resources>

    <Grid Margin="{StaticResource ViewContentMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="{DynamicResource HeaderMarginSmall}">
            <TextBlock Text="Align Toposolid Edges" FontWeight="{StaticResource WeightBold}" FontSize="{StaticResource FontSizeTitle}" Foreground="{StaticResource TextPrimaryBrush}" Margin="{StaticResource TitleMarginSmall}"/>
            <TextBlock Text="Project target points onto the reference surface." FontSize="{StaticResource FontSizeSmall}" Foreground="{StaticResource TextSecondaryBrush}"/>
        </StackPanel>

        <!-- Selection Cards -->
        <StackPanel Grid.Row="1" Margin="{StaticResource HeaderMarginSmall}">
            <!-- Target Selection -->
            <Border Style="{DynamicResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="1. TARGETS" Style="{DynamicResource LabelStyle}" Margin="0,0,0,4" Opacity="0.5"/>
                    <comp:SelectionControl DataContext="{Binding TargetsSelection}"/>
                </StackPanel>
            </Border>

            <!-- Reference Selection -->
            <Border Style="{DynamicResource CardStyle}" Margin="{StaticResource CardMarginNone}">
                <StackPanel>
                    <TextBlock Text="2. REFERENCE" Style="{DynamicResource LabelStyle}" Margin="0,0,0,4" Opacity="0.5"/>
                    <comp:SelectionControl DataContext="{Binding ReferenceSelection}"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Actions -->
        <Grid Grid.Row="2" Margin="{StaticResource ButtonRowMarginSmall}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Cancel" Grid.Column="0" Height="{StaticResource ButtonHeightMedium}" Command="{Binding CancelCommand}" Style="{DynamicResource SecondaryButtonStyle}"/>

            <Button Grid.Column="2" Height="{StaticResource ButtonHeightMedium}" Command="{Binding RunCommand}" IsEnabled="{Binding CanRun}" Style="{DynamicResource PrimaryButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{x:Static icons:Icons.ArrowUpDown}" Stroke="White" StrokeThickness="1.5" Width="{StaticResource IconSizeButton}" Height="{StaticResource IconSizeButton}" Stretch="Uniform" Margin="0,0,8,0"/>
                    <TextBlock Text="Run" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</base:LecgWindow>
