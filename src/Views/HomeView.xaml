<base:LecgWindow x:Class="LECG.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:LECG.Views.Base"
    xmlns:utils="clr-namespace:LECG.Utils"
    xmlns:icons="clr-namespace:LECG.Utils" Title="LECG HOME" Height="650" Width="900" WindowStartupLocation="CenterScreen">

    <base:LecgWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LECG;component/src/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </base:LecgWindow.Resources>

    <Grid Margin="{DynamicResource ViewContentMarginLarge}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="{DynamicResource HeaderMargin}">
            <TextBlock Text="LECG TOOLKIT" FontWeight="{DynamicResource WeightBold}" FontSize="{DynamicResource FontSizeTitle}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="{DynamicResource TitleMarginSmall}"/>
            <TextBlock Text="Select a tool to begin." FontSize="{DynamicResource FontSizeSmall}" Foreground="{DynamicResource TextSecondaryBrush}"/>
        </StackPanel>

        <!-- Tool Grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <UniformGrid Columns="4" Rows="3" Margin="0,0,8,0">

                <!-- 1. Align (Master) -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="Alignment &amp; Distribution Tools" Click="AlignMaster_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.AlignCenter}" Fill="{DynamicResource PrimaryAccentBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Align" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 2. Assign Material -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="Assign Material by Type" Click="AssignMaterial_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.Material}" Fill="{DynamicResource TextSecondaryBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Assign Mat." FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 3. Sexy Revit -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="View Beautification" Click="SexyRevit_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.Sparkles}" Fill="{DynamicResource PrimaryAccentBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Sexy Revit" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 4. Purge -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="Project Health" Click="Purge_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.Trash}" Fill="{DynamicResource AlertBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Purge" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 5. Offsets -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="Offset Elevations" Click="Offsets_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.ArrowUpDown}" Fill="{DynamicResource TextSecondaryBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Offsets" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 6. Reset Slabs -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="Reset Shape Edited Slabs" Click="ResetSlabs_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.ResetSlabs}" Fill="{DynamicResource TextSecondaryBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Reset Slabs" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 7. Simplify Points -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="Simplify Toposolid Points" Click="SimplifyPoints_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.Simplify}" Fill="{DynamicResource TextSecondaryBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Simplify Pts" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 8. Align Edges -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="Align Toposolid Edges" Click="AlignEdges_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.Contours}" Fill="{DynamicResource TextSecondaryBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Align Edges" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 9. Update Contours -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="Update Contours" Click="UpdateContours_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.Contours}" Fill="{DynamicResource TextSecondaryBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Contours" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 10. Change Level -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="Change Level" Click="ChangeLevel_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.Level}" Fill="{DynamicResource TextSecondaryBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Change Lvl" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 11. Clean Schemas -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="Clean Schemas" Click="CleanSchemas_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.Eraser}" Fill="{DynamicResource AlertBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Clean" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 12. Render Match -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="Render Selection Match" Click="RenderMatch_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.Palette}" Fill="{DynamicResource TextSecondaryBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Render Match" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 13. Convert Family -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="Convert Family" Click="ConvertFamily_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.Hammer}" Fill="{DynamicResource TextSecondaryBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Convert Fam" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 14. Batch Rename -->
                <Button Style="{DynamicResource DashboardCardStyle}" ToolTip="Batch Rename" Click="BatchRename_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="{x:Static icons:Icons.SearchReplace}" Fill="{DynamicResource TextSecondaryBrush}" Width="{DynamicResource IconSizeLarge}" Height="{DynamicResource IconSizeLarge}" Stretch="Uniform" Margin="{DynamicResource IconMarginBottom}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Rename" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

            </UniformGrid>
        </ScrollViewer>
    </Grid>
</base:LecgWindow>