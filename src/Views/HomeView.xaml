<base:LecgWindow x:Class="LECG.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:LECG.Views.Base"
    xmlns:utils="clr-namespace:LECG.Utils"
    xmlns:icons="clr-namespace:LECG.Utils" Title="LECG TOOLKIT" Height="650" Width="900" MinWidth="800" MinHeight="600" UseLayoutRounding="True" SnapsToDevicePixels="True" WindowIcon="{x:Static icons:Icons.Home}">

    <base:LecgWindow.Resources>
        <ResourceDictionary/>
    </base:LecgWindow.Resources>

    <Grid Margin="{DynamicResource ViewContentMarginLarge}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="LECG TOOLKIT" FontWeight="Bold" FontSize="24" Foreground="{DynamicResource LecgTextPrimary}" Margin="0,0,0,4"/>
            <TextBlock Text="Select a tool to begin." FontSize="14" Foreground="{DynamicResource LecgTextSecondary}"/>
        </StackPanel>

        <!-- Tool Grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <WrapPanel Margin="0,0,8,0">
                <WrapPanel.Resources>
                    <Style TargetType="{x:Type Button}" BasedOn="{StaticResource DashboardCardStyle}">
                        <Setter Property="Width" Value="200"/>
                        <Setter Property="Height" Value="100"/>
                    </Style>
                </WrapPanel.Resources>

                <!-- 1. Align (Master) -->
                <Button ToolTip="Alignment &amp; Distribution Tools" Click="AlignMaster_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/AlignMaster_32.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Align" FontWeight="Bold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 2. Assign Material -->
                <Button ToolTip="Assign Material by Type" Click="AssignMaterial_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/AssignMaterial.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Assign Mat." FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 3. Sexy Revit -->
                <Button ToolTip="View Beautification" Click="SexyRevit_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/SexyRevit.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Sexy Revit" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 4. Purge -->
                <Button ToolTip="Project Health" Click="Purge_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/Purge.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Purge" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 5. Offsets -->
                <Button ToolTip="Offset Elevations" Click="Offsets_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/OffsetElevations.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Offsets" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 6. Reset Slabs -->
                <Button ToolTip="Reset Shape Edited Slabs" Click="ResetSlabs_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/ResetSlabs.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Reset Slabs" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 7. Simplify Points -->
                <Button ToolTip="Simplify Toposolid Points" Click="SimplifyPoints_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/simplify_points_32.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Simplify Pts" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 8. Align Edges -->
                <Button ToolTip="Align Toposolid Edges" Click="AlignEdges_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/align_edges_32.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Align Edges" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 9. Update Contours -->
                <Button ToolTip="Update Contours" Click="UpdateContours_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/update_contours_32.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Contours" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 10. Change Level -->
                <Button ToolTip="Change Level" Click="ChangeLevel_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/change_level_32.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Change Lvl" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 11. Clean Schemas -->
                <Button ToolTip="Clean Schemas" Click="CleanSchemas_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/CleanSchemas.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Clean" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 12. Render Match -->
                <Button ToolTip="Render Selection Match" Click="RenderMatch_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/RenderMatch.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Render Match" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 13. Fix Points -->
                <Button ToolTip="Repair inconsistent edge and transition points on Floors and Toposolids" Click="FixPoints_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/simplify_points_32.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Fix Points" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 14. Split Boundaries -->
                <Button ToolTip="Separate multi-boundary elements into independent instances" Click="SplitBoundaries_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/ResetSlabs.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Split Bound." FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 15. Floor to Toposolid -->
                <Button ToolTip="Convert Floor elements to Toposolid elements" Click="ConvertFloorToToposolid_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/ConvertFamily_32.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Floor to Topo" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 16. Toposolid to Floor -->
                <Button ToolTip="Convert Toposolid elements to Floor elements" Click="ConvertToposolidToFloor_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/ConvertFamily_32.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Topo to Floor" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 17. Convert Family -->
                <Button ToolTip="Convert Family" Click="ConvertFamily_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/ConvertFamily_32.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Convert Fam" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 18. Batch Rename -->
                <Button ToolTip="Batch Rename" Click="BatchRename_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/LECG;component/src/Resources/Images/SearchReplace.png" Width="32" Height="32" Margin="0,0,0,8" HorizontalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Rename" FontWeight="SemiBold" Foreground="{DynamicResource LecgTextPrimary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</base:LecgWindow>
