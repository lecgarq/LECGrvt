<base:LecgWindow x:Class="LECG.Views.PurgeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:LECG.Views.Base"
    xmlns:utils="clr-namespace:LECG.Utils"
    xmlns:icons="clr-namespace:LECG.Utils" Title="PURGE" Width="350" WindowIcon="{x:Static icons:Icons.Trash}">

    <base:LecgWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LECG;component/src/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </base:LecgWindow.Resources>

    <Grid Margin="{StaticResource ViewContentMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="{StaticResource HeaderMargin}">
            <TextBlock Text="Purge Unused Elements" FontWeight="{StaticResource WeightBold}" FontSize="{StaticResource FontSizeTitle}" Foreground="{StaticResource TextPrimaryBrush}" Margin="{StaticResource TitleMargin}"/>
            <TextBlock Text="Remove unused styles, patterns, and materials from your project." FontSize="{StaticResource FontSizeSmall}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Options -->
        <Border Grid.Row="1" Style="{DynamicResource CardStyle}" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="OPTIONS" Style="{DynamicResource CardHeaderStyle}"/>
                <CheckBox Content="Unused Line Styles" IsChecked="{Binding PurgeLineStyles, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="{StaticResource CheckboxSpacing}"/>
                <CheckBox Content="Unused Fill Patterns" IsChecked="{Binding PurgeFillPatterns, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="{StaticResource CheckboxSpacing}"/>
                <CheckBox Content="Unused Materials" IsChecked="{Binding PurgeMaterials, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="{StaticResource CheckboxSpacing}"/>
                <CheckBox Content="Unused Levels" IsChecked="{Binding PurgeLevels, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="{StaticResource CheckboxSpacing}"/>
                <CheckBox Content="Unused Family Parameters ⚠" IsChecked="{Binding PurgeParameters, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}" Margin="{StaticResource CheckboxSpacing}" ToolTip="Opens each family and removes parameters with no formula, no constraint, and no association. This is destructive and slow."/>
                <Separator Margin="0,5,0,10" Background="{StaticResource BorderBrush}"/>
                <CheckBox Content="Deep Purge (3 Passes)" IsChecked="{Binding IsDeepPurge, Mode=TwoWay}" Style="{DynamicResource ModernCheckboxStyle}"/>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="{StaticResource ButtonRowMargin}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Cancel" Grid.Column="0" Height="{StaticResource ButtonHeight}" Click="Cancel_Click" Style="{DynamicResource SecondaryButtonStyle}"/>

            <Button Content="Purge" Grid.Column="2" Height="{StaticResource ButtonHeight}" Click="Run_Click" Style="{DynamicResource PrimaryButtonStyle}"/>
        </Grid>
    </Grid>
</base:LecgWindow>
