<base:LecgWindow x:Class="LECG.Views.RenderAppearanceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:LECG.Views.Base"
    xmlns:comp="clr-namespace:LECG.Views.Components"
    xmlns:icons="clr-namespace:LECG.Utils" Title="Sync Render Appearance" Width="400" WindowStartupLocation="CenterScreen" SizeToContent="Height" WindowIcon="{x:Static icons:Icons.ArrowUpDown}">

    <base:LecgWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LECG;component/src/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <icons:InverseBoolConverter x:Key="InverseBoolConverter"/>
            <icons:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisConverter"/>
        </ResourceDictionary>
    </base:LecgWindow.Resources>

    <Grid Margin="{StaticResource ViewContentMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="{DynamicResource HeaderMarginSmall}">
            <TextBlock Text="Sync Render Appearance" FontWeight="{StaticResource WeightBold}" FontSize="{StaticResource FontSizeTitle}" Foreground="{StaticResource TextPrimaryBrush}" Margin="{StaticResource TitleMarginSmall}"/>
            <TextBlock Text="Update graphic settings to match appearance asset." FontSize="{StaticResource FontSizeSmall}" Foreground="{StaticResource TextSecondaryBrush}"/>
        </StackPanel>

        <!-- Info Card -->
        <Border Grid.Row="1" Style="{DynamicResource CardStyle}" Padding="16">
            <StackPanel>
                <TextBlock Text="INFO" Style="{DynamicResource LabelStyle}" Margin="0,0,0,8" Opacity="0.5"/>
                <TextBlock Text="This command will process all materials in the project." TextWrapping="Wrap" Margin="0,0,0,8"/>
                <TextBlock Text="• Skips materials already synced." TextWrapping="Wrap" Margin="0,0,0,8" Opacity="0.8"/>
                <TextBlock Text="• Forces update of shading color to match texture." TextWrapping="Wrap" Margin="0,0,0,0" Opacity="0.8"/>
            </StackPanel>
        </Border>

        <!-- Actions -->
        <Grid Grid.Row="2" Margin="{StaticResource ButtonRowMarginSmall}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Cancel" Grid.Column="0" Height="{StaticResource ButtonHeightMedium}" Command="{Binding ExecuteCancelCommand}" Style="{DynamicResource SecondaryButtonStyle}"/>
            <Button Content="Run" Grid.Column="2" Height="{StaticResource ButtonHeightMedium}" Command="{Binding RunCommand}" IsEnabled="{Binding CanRun}" Style="{DynamicResource PrimaryButtonStyle}"/>
        </Grid>
    </Grid>
</base:LecgWindow>
