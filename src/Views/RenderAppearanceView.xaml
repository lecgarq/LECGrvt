<base:LecgWindow x:Class="LECG.Views.RenderAppearanceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:LECG.Views.Base"
    xmlns:comp="clr-namespace:LECG.Views.Components"
    xmlns:icons="clr-namespace:LECG.Utils" Title="RENDER MATCH" Width="380" WindowIcon="{x:Static icons:Icons.Palette}">

    <base:LecgWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LECG;component/src/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <icons:InverseBoolConverter x:Key="InverseBoolConverter"/>
            <icons:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisConverter"/>
        </ResourceDictionary>
    </base:LecgWindow.Resources>

    <Grid Margin="{DynamicResource ViewContentMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="{DynamicResource HeaderMarginSmall}">
            <TextBlock Text="Sync Render Appearance" FontWeight="{DynamicResource WeightBold}" FontSize="{DynamicResource FontSizeTitle}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="{DynamicResource TitleMarginSmall}"/>
            <TextBlock Text="Update graphic settings to match appearance asset." FontSize="{DynamicResource FontSizeSmall}" Foreground="{DynamicResource TextSecondaryBrush}"/>
        </StackPanel>

        <!-- Info Card -->
        <Border Grid.Row="1" Style="{DynamicResource CardStyle}" Padding="16">
            <StackPanel>
                <TextBlock Text="INFO" Style="{DynamicResource LabelStyle}" Margin="0,0,0,8" Opacity="0.5"/>
                <TextBlock Text="This command will process all materials in the project." TextWrapping="Wrap" Margin="0,0,0,8"/>
                <TextBlock Text="â€¢ Skips materials already synced." TextWrapping="Wrap" Margin="0,0,0,8" Opacity="0.8"/>
                <TextBlock Text="â€¢ Forces update of shading color to match texture." TextWrapping="Wrap" Margin="0,0,0,0" Opacity="0.8"/>
            </StackPanel>
        </Border>

        <!-- Actions -->
        <Grid Grid.Row="2" Margin="{DynamicResource ButtonRowMarginSmall}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Cancel" Grid.Column="0" Height="{DynamicResource ButtonHeightMedium}" Command="{Binding ExecuteCancelCommand}" Style="{DynamicResource SecondaryButtonStyle}"/>
            <Button Content="Run" Grid.Column="2" Height="{DynamicResource ButtonHeightMedium}" Command="{Binding RunCommand}" IsEnabled="{Binding CanRun}" Style="{DynamicResource PrimaryButtonStyle}"/>
        </Grid>
    </Grid>
</base:LecgWindow>

