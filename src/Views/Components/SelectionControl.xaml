<UserControl x:Class="LECG.Views.Components.SelectionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LECG.Views.Components" mc:Ignorable="d" d:DesignHeight="60" d:DesignWidth="300">


    <Grid Margin="0,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">
            <TextBlock Text="{Binding SelectionStatus}" Style="{DynamicResource BaseTextBlockStyle}" FontWeight="SemiBold"/>
            <TextBlock Text="Click to add or change" Style="{DynamicResource BaseTextBlockStyle}" FontSize="10" Opacity="0.6"/>
        </StackPanel>

        <Button Grid.Column="1" Content="Select..." Command="{Binding SelectCommand}" Style="{DynamicResource SecondaryButtonStyle}" Width="80"/>
    </Grid>
</UserControl>
