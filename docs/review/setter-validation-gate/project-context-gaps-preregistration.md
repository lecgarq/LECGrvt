# Remaining CASA project-context gaps

Revision and all four CASA EUCALIPTO model hashes are frozen by
`project-context-gaps-manifest.json`. Every attempt uses a fresh detached copy;
no source model may be saved or modified.

Classify `DisplacementPath.AncestorIdx` and `TableView.TargetId` across
architecture, topography, structure, and MEP. Autodesk Revit 2026 documents
their value types but exposes no eligible-set query or public validity method.
Therefore the generator must return no candidate and must not call either
setter. Record `no_existing_target_in_unmodified_sample` or
`no_proven_valid_alternative`, target counts, source hashes, and cleanup.

Expected validation yield: exactly zero. These outcomes improve project
applicability knowledge only; they do not change the MCP write contract.
