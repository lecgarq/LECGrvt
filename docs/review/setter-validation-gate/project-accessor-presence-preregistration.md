# CASA accessor target-type presence

Revision, the 2,216-operation accessor inventory, 243 unique declaring types,
and all four CASA EUCALIPTO model hashes are frozen by
`project-accessor-presence-manifest.json`.

Open only a fresh detached disposable copy of each model. Enumerate elements
once, count instances assignable to each declaring type, and emit all 243
counts. Start no transaction and invoke no getter or setter from the operation
catalog. This establishes fixture presence only—not read/write validity, MCP
capability, or universal applicability.

Every copy must close unsaved, be deleted, and leave its source hash unchanged.
