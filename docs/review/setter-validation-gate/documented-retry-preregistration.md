# Documented-constraint retry — frozen before execution

The four-case run started at `20260915T212311` timed out while opening the 274 MB
architecture project fallback during the assembly-naming case. Its TRX is failed and
cannot qualify the two earlier energy passes. This retry reproduces those two exact
first-model results and independently tests load-case subcategories on the frozen
structural-first Autodesk order.

Use the unchanged API-derived generators from the original preregistration. Do not
include `AssemblyInstance.NamingCategoryId`: its only writable attempt ended with an
uncommitted inner transaction, and the timed-out fallback is not evidence. Stop each
case after the first validation; otherwise exhaust only its listed models.

All fixtures are disposable detached copies. Unload links, roll back, close without
saving, rehash each source, physically delete each copy, and abort after any
isolation or cleanup failure. No family document and no MCP contract expansion.
