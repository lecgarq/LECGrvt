# Documented changed-value batch 2 retry

The first run was interrupted while opening the 274 MB CASA EUCALIPTO architecture disposable copy after three Autodesk samples had produced no qualifying landing result. The source hash remained unchanged and the exact copy was removed.

This retry preserves all five generators and the pre-registered **2-5 of 5** expected yield. It removes only the slow CASA EUCALIPTO architecture fallback from the four architecture/schedule cases. `WireType.MaxSize` retains the smaller CASA EUCALIPTO MEP model as its final fallback. Every attempt remains a fresh detached disposable copy with rollback, close-without-save, source-hash, and copy-removal gates.
