# CASA setter target-type presence

Revision, the 805-operation setter inventory, 181 unique declaring types, and
all four CASA EUCALIPTO model hashes are frozen by
`project-type-presence-manifest.json`.

Open only a fresh detached disposable copy of each model. Enumerate elements
once, count instances assignable to each declaring type, and emit all 181
counts. Start no transaction and invoke no getter or setter from the operation
catalog. This establishes fixture presence only—not edit validity, MCP
capability, or universal applicability.

Every copy must close unsaved, be deleted, and leave its source hash unchanged.
